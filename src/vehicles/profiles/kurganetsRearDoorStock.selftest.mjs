import assert from 'node:assert/strict';
import * as T from 'three';
import {createTank} from '../tankFactory.ts';
import {registerProfiledBuilders} from '../tankFactoryCore.ts';
import {ensureInteriorFills,hasInteriorFills} from '../interiorFills.ts';
import {getSpec} from '../specs.ts';
import {createTankState} from '../../sim/movement.ts';
import {isTrackShoeMesh} from '../../../tools/track-clip-classification.mjs';
import {KIT} from './kit.ts';
import {buildKurganets25X} from './kurganetsX.ts';
import {buildKurganets25X as original} from './kurganetsX.ts';
const id='kurganets25_x',front=new T.MeshBasicMaterial({side:T.FrontSide}),double=new T.MeshBasicMaterial({side:T.DoubleSide});
// Complete-source held-out first hits; assembled GLB 417ea9738b986a591ba049e25ef86fe2dd0f240048d3630d8f23551c17b7d793.
const witnesses=[[-0.57,1.6,-3.5820000171661377],[0.5,1.6,-3.5820000171661377],[-0.4,2.08,-3.5820000171661377],[0.4,2.08,-3.5820000171661377],[0,0.91,-3.6797402500895373],[-0.28,1.3,-3.5821518959971086],[0.25,1.3,-3.582120559322507],[-0.28,1.65,-3.5821852684020996],[0.24,1.9,-3.582167770005589],[0.044,1.84,-3.6431865240774464],[0,1.93,-3.6429853439331055],[0.1,1.78,-3.643343421426029],[-0.055,1.5,-3.5820855410456276],[-0.4864,1.16,-3.6119654229970797],[-0.4864,1.83,-3.6119654229970797],[0.15,1.604,-3.6052668409385307],[-0.2,0.80816,-3.637255534703238],[0.2,0.84816,-3.653155555946232],[-0.2,0.757712,-3.6197041841558404],[0.1,0.8992,-3.673914341491842]];
const seatRecipes=[{"name":"frame/hull","point":[0.52,1.5,-3.556],"roles":["hull","frame"]},{"name":"core/hull","point":[0,1.3,-3.55],"roles":["hull","door-core"]},{"name":"upper/core","point":[-0.25,1.65,-3.5605],"roles":["door-core","upper-skin"]},{"name":"lower/core","point":[-0.25,1.3,-3.5605],"roles":["door-core","lower-skin"]},{"name":"port/lid","point":[0,1.84,-3.58],"roles":["upper-skin","port-receiver"]},{"name":"neck/receiver","point":[0,1.84,-3.607],"roles":["port-receiver","port-neck"]},{"name":"cover/neck","point":[0,1.84,-3.6259],"roles":["port-neck","port-cover"]},{"name":"left fixed hinge/frame","point":[-0.54,1.16,-3.58],"roles":["frame","hinge-fixed-leaf"]},{"name":"left moving hinge/skin","point":[-0.42,1.16,-3.58],"roles":["lower-skin","hinge-moving-leaf"]},{"name":"upper fixed hinge/frame","point":[-0.54,1.834,-3.58],"roles":["frame","hinge-fixed-leaf"]},{"name":"upper moving hinge/skin","point":[-0.42,1.834,-3.58],"roles":["upper-skin","hinge-moving-leaf"]},{"name":"step mount/frame","point":[0.0067,0.936,-3.578],"roles":["frame","step-receiver"]},{"name":"step hook/mount","point":[0.0067,0.927,-3.587],"roles":["step-top-hook","step-receiver"]},{"name":"step hook/frame","point":[0.0067,0.909,-3.6628],"roles":["step-top-hook","step-frame"]},{"name":"step brace/tread","point":[0.0067,0.807,-3.628],"roles":["step-tread","step-center-brace"]},{"name":"step left tread/frame","point":[-0.282,0.808,-3.63],"roles":["step-frame","step-tread"]}];
function visible(hit){for(let o=hit.object;o;o=o.parent)if(!o.visible)return false;return !hit.object.userData.shadowOnly;}
function cast(root,origin,direction,far){return new T.Raycaster(new T.Vector3(...origin),new T.Vector3(...direction),0,far).intersectObject(root,true).find(visible);}
function contains(mesh,point){const d=new T.Vector3(0,0,1),hits=new T.Raycaster(new T.Vector3(...point).addScaledVector(d,-12),d,0,24).intersectObject(mesh).filter((h,i,a)=>!i||Math.abs(h.distance-a[i-1].distance)>1e-7),a=hits.filter(h=>h.distance<12-1e-6).at(-1),b=hits.find(h=>h.distance>12+1e-6);return!!a&&!!b&&a.face.normal.z<-.001&&b.face.normal.z>.001;}
function build(quality,mutation=null){const parts=[];registerProfiledBuilders({[id](P){const add=P.add;P.add=(bucket,g,...t)=>{const role=g.userData.kurgRearRole;if(mutation==='delete-port'&&role?.startsWith('port-'))return;if(mutation==='unseat-step'&&role?.startsWith('step-'))t=[t[0]??0,t[1]??0,(t[2]??0)-.12,...t.slice(3)];const stock=KIT.xform(g.clone(),...t),mesh=new T.Mesh(stock,double);stock.computeBoundingBox();mesh.updateMatrixWorld(true);parts.push({role,bucket,mesh,bounds:stock.boundingBox});return add(bucket,g,...t)};buildKurganets25X(P);if(mutation==='fill-step'){const g=KIT.box(.40,.12,.05);g.userData.kurgRearRole='negative-filled-step';P.add('hullDetail',g,0,.83,-3.65)}if(mutation==='intrude-track'){const g=KIT.box(.5,.5,.5);g.userData.kurgRearRole='negative-track';P.add('hullDetail',g,1.405,1.0,-3.30)}}});let tank;try{tank=createTank(id,null,{proceduralOnly:true,quality,camoSeed:4242,geometryReceipt:true,deferStaticBatch:true})}finally{registerProfiledBuilders({[id]:original})}tank.root.traverse(o=>{if(o.isLOD){o.autoUpdate=false;o.levels.forEach((l,i)=>l.object.visible=i===0)}if(o.isMesh){if(o.userData.shadowOnly||o.material?.colorWrite===false)o.visible=false;else o.material=front}});tank.root.updateMatrixWorld(true);return {tank,parts,dispose(){tank.dispose();parts.forEach(p=>p.mesh.geometry.dispose())}};}
function sourceHits(root){let max=0;for(const [x,y,z]of witnesses){const h=cast(root,[x,y,-5],[0,0,1],2);assert.ok(h,'source stock is visible in complete native scene');const residual=Math.abs(h.point.z-z);max=Math.max(max,residual);assert.ok(residual<=.002,`source first-hit stock at ${x}/${y}: ${residual}`)}return max;}
function seats(parts){for(const s of seatRecipes){for(const role of s.roles)assert.ok(parts.some(p=>(p.role??p.bucket)===role&&contains(p.mesh,s.point)),`${s.name}: ${role} has positive stock at the shared seat`)}return seatRecipes.length;}
function air(root){for(const x of[-.15,.15])for(const y of[.78,.83,.87])assert.ok(!cast(root,[x,y,-3.72],[0,0,1],.12),'source-real spaces between step members remain air');}
function moving(built,quality){const parts=built.parts.filter(p=>p.role),root=built.tank.root,shoes=[];root.traverse(o=>{if(isTrackShoeMesh(o))shoes.push(o)});assert.ok(shoes.length);const instance=new T.Matrix4(),world=new T.Matrix4(),state=createTankState(getSpec(id),new T.Vector3(),0);let count=0;for(const phase of[0,.023,.057,.101,.149]){state.trackScroll.l=phase;state.trackScroll.r=phase;built.tank.syncFromState(state,1);root.updateMatrixWorld(true);for(const shoe of shoes){shoe.geometry.computeBoundingBox();for(let i=0;i<shoe.count;i++){shoe.getMatrixAt(i,instance);world.multiplyMatrices(shoe.matrixWorld,instance);const bounds=shoe.geometry.boundingBox.clone().applyMatrix4(world).expandByScalar(.01);count++;for(const part of parts)assert.equal(bounds.intersectsBox(part.bounds),false,`${quality}: actual shoe ${i} clears new rear assembly at ${phase}`)}}}return count;}
await ensureInteriorFills([id]);assert.ok(hasInteriorFills(id));const results=[];
for(const quality of['high','low']){let built=build(quality);try{const maxResidualM=sourceHits(built.tank.root),finiteSeats=seats(built.parts);air(built.tank.root);let triangles=0;built.tank.root.traverseVisible(o=>{if(o.isMesh)triangles+=Math.min(o.geometry.index?.count??o.geometry.attributes.position.count,o.geometry.drawRange.count)/3*(o.isInstancedMesh?o.count:1)});assert.ok(triangles<80000);const shoeBounds=moving(built,quality);results.push({quality,sourceHits:witnesses.length,air:6,maxResidualM,finiteSeats,shoeBounds,triangles})}finally{built.dispose()}
for(const mutation of['delete-port','unseat-step','fill-step','intrude-track']){built=build(quality,mutation);try{assert.throws(()=>mutation==='delete-port'?sourceHits(built.tank.root):mutation==='unseat-step'?seats(built.parts):mutation==='fill-step'?air(built.tank.root):moving(built,quality),assert.AssertionError,mutation+' must fail its actual-stock gate')}finally{built.dispose()}}}
assert.ok(results[1].triangles<=results[0].triangles*.75);console.log('Kurg rear assembly complete-scene source/air/finite-seats/moving-clearance and four mutation negatives PASS',JSON.stringify(results));
