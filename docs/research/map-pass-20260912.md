# Map pass — 2026-09-12 (dressing, far-range haze, native census)

Owner direction: maps regressed in appearance versus 2026-08-30 and must be
improved significantly without slipping on performance, transition speed or
latency. The 1049e4e presentation restore (horizon rows, water, roots, belts)
landed on 2026-09-11; this pass reads the 30 establishing shots captured after
it and closes the most visible gaps that are configuration-level.

## Evidence read

Establishing shots for all 30 maps (`tools/map-environment-audit.mjs --shots
--establishing-only`, desktop tier). Findings:

- Coastal, Fjord, Urban outskirts, Saltwind, Airfield, Desert: bare foreground
  lawn/sand between the camera and the settlement; no scrub, few lone trees,
  few rocks. Saltwind read as generic green pasture, not limestone terraces.
- Coastal, Steppe, Railyard, Urban: far ranges dissolved into the fog colour
  (horizon `haze` 1.15–1.25 against 0.86–0.97 elsewhere).
- Alpine, Winter, Whiteout: foreground snowfields empty; far ranges pale. The
  alpine range shading is per-fragment by design (r3–r8 in `horizon.ts`) and is
  left alone here.
- Verdant, Autumn, Reservoir, Frontier, Monsoon, Delta: already read well.

## What changed

Configuration only (`src/world/maps/*.ts`); no renderer, shader, pool or
budget code changed.

| Map | Change |
|---|---|
| coastal | clusters 34→42, lone trees 88→116, bushes 0.9→1.25, haystacks 8→14, rocks 200→262, outcrops 22→30, haze 1.25→1.08 |
| fjord | lone trees 124→146, bushes 0.8→1.15, rocks 245→330, outcrops 42→60 |
| urban | clusters 14→22, lone trees 40→68, bushes 0.85→1.2, haze 1.15→1.0 |
| saltwind | bushes 0.86→1.3, cluster scrub 1.5→1.9 (tree/rock counts stay at the environmentExpansion first-pass ceilings) |
| steppe | bushes 0.55→0.72, haystacks 34→44, haze 1.25→1.08 (lone trees stay at 30: 40 planted a grove on the establishing-shot pose) |
| railyard | haze 1.25→1.06 |
| alpine | bushes 0.48→0.62 |
| winter | bushes 0.15→0.28, rocks 190→232, outcrops 19→24 |
| airfield | bushes 0.6→0.82 |
| desert | bushes 0.92→1.1, rocks 275→320, outcrops 36→44 |

## Receipts and gates

- Source-history receipts (`badlandsRelief`, `villageWear`,
  `mangroveWaterPalette`, `playableRelief`) project only these authenticated
  leaves back to the historical authoring via
  `src/world/mapPassDressing.test-support.mjs`; any other map edit still fails.
- `environmentExpansion` first-pass ceilings are respected (saltwind and
  airfield tree/rock counts unchanged).
- Collision manifests for the nine redressed maps were recaptured natively
  (vite dev server + `agent-browser` session,
  `tools/capture-world-collision-manifests.mjs --maps …`; saltwind and steppe
  re-captured after a page reload with identical shards). The exact census in
  `server/dedicatedWorldCollision.selftest.mjs` is restated per map; the desert
  shard byte budget in `server/collisionManifestCodec.selftest.mjs` is restated
  to its captured size (+18 KB of records, not a codec regression).
- Shots after the change were re-read for the ten maps: coastal, saltwind,
  desert and railyard read clearly better (scrub, boulders, readable far
  ranges); fjord/urban/winter/alpine improve modestly. The far-range
  "cardboard" look on alpine-style maps is a horizon-row limit
  (`HORIZON_SEGMENTS` 287 with 250 m row spacing) and is out of scope here.
- Timing: this machine was under a foreign load (load average > 200) for the
  whole pass, so `--gate` timing comparisons are not meaningful from this run;
  the per-map median frame times captured alongside the shots are recorded in
  the scratch report only. A quiet-machine `--baseline` comparison is the
  remaining performance receipt before deploy.

## Addendum — horizon r9 (alpine-style ranges)

The pale flat triangles on the far ranges of Nordhavn Fjord and Glacier Pass
were two things: per-vertex forest/snow/rock bands interpolating across 250 m
wall triangles, and the treeline crown groves rendered in the (paler) baked
face colour. `horizon.ts` r9 moves the treeline, crest snow and forest stands
into the alpine fragment pass (same map bands, three plane fetches, no
branches), softens the vertex bake to match, and tints the crowns by the
authored forest/base ratio. Paired establishing shots before/after are in the
scratch report; the receipts (`horizonResources` shader contract, mesa/redrock/
titan/autumn receipts, `mapQuality`) stay green. The remaining sawtooth
outline of far groves comes from the comb atlas alpha edge and is a later item.

## Timing receipt (paired, 2026-09-12 03:40)

Paired timing-only runs (`tools/map-environment-audit.mjs`, desktop tier,
75 samples × 3 repeats, identical acquisition; baseline = the pre-map-pass
tree in a detached worktree, candidate = this branch incl. horizon r9) over
verdant (control), coastal, fjord, urban, saltwind, steppe, railyard, alpine,
winter, airfield, desert:

- Structural deltas are zero or negative on every map (mesh families,
  instanced families, materials, textures unchanged; coastal −2 families).
- Render medians improve on the redressed maps (coastal −3.1 ms, fjord
  −3.3 ms, urban −2.1 ms) and drift within noise on the untouched controls
  (verdant +1.6 ms, railyard +0.8 ms).
- Frame medians are bimodal on this machine (runs flip between ~12 ms and
  ~21 ms within the same map on both sides, e.g. coastal baseline
  [21.7, 14.2, 12.8] vs candidate [22.2, 21.0, 11.3]); the gate's 8 % frame
  budget therefore flags maps in both directions (saltwind −9.7 ms, fjord
  +9.7 ms). Verdant, which this pass did not touch, fails the same gate.

Reading: no evidence of a content-driven regression; the frame-pacing state
of the shared machine dominates the gate. A quiet-machine (or pacing-pinned)
paired rerun is still the deploy prerequisite for this pass.

## Addendum — windward stone on the snow maps

Glacier Pass and Frosthollow still read as empty snowfields in the establishing
shots after the first dressing pass (bushes alone barely register on snow).
Rocks/outcrops rise to 330/72 (alpine, from 275/54) and 270/32 (winter, from
232/24) — exposed windward stone, the identity both maps call for. Receipts:
the authenticated leaves module carries the new values, manifests for both
maps were recaptured natively and the exact census restated (alpine
8529/8342/7481, winter 5197/4982/4295 before rim-road removals), and the
codec byte budgets hold.

## Addendum — Glacier Pass roads (2026-09-12, later)

The establishing shot still showed the alpine roads as 10 m black-brown
slashes across the snowfield. Two causes, both authored: the road tint
(`[0.65, 0.69, 0.72]`) darkened an already dark frozen-mud dirt layer, and
the splat's bare road shoulder (`shoulder` in the dirt weight `fD`) stripped
the snow from the whole corridor, so the tint alone moved the road colour by
under 10 % between 1.06 and 1.52.

- `terrain.ts` gains an authored `shoulderDirt` scale (`uShoulderDirt`,
  default 1, clamped 0..1) applied to the shoulder term alone; the max()
  competition between ambient wear, shoulder and town wear is unchanged and
  every other map keeps its full bare shoulder.
- Glacier Pass authors `roadTint: [1.30, 1.46, 1.70], shoulderDirt: 0.30`:
  the roads now read as packed grey snow with darker edges between white
  verges (r5 shot), still legible from the air.
- Frosthollow was shot as a control and already reads as snowed-over lanes
  (its dark-slush tint stays); Whiteout inherits Frosthollow's splat.
- Receipts: the authenticated leaves module carries the alpine line; the
  worn-dirt receipt executes the actual `fD` with the new uniform (810 cases,
  legacy default, clamp, scope = alpine only); the material-ownership policy
  pin and its uniform list are restated with this note.

## Timing receipt 2 (quiet machine, one harness, 2026-09-12 08:30–09:10)

Paired timing-only runs with this worktree's harness on both sides. The
baseline is a scratch tree with HEAD vehicles and the pre-map-pass `src/world`
(0e63f88f8), so the Garage-owner and terrain-topology pairing checks hold and
only the map pass differs. Desktop tier, 75 samples × 3 repeats, no other
audits, gates or dev servers running.

| map | frame median ms (base → cand) | render median ms (base → cand) | structural Δ |
|---|---|---|---|
| verdant (control) | 22.3 → 22.2 | 4.0 → 3.9 | none |
| coastal | 20.9 → 21.0 | 2.9 → 3.0 | +166 k tris, −2 geometries |
| fjord | 21.0 → 21.2 | 2.4 → 2.6 | +87 k tris |
| urban | 20.9 → 21.5 | 2.8 → 3.7 | +158 k tris |
| saltwind | 20.9 → 21.3 | 2.7 → 2.9 | +55 k tris |
| steppe | 21.1 → 21.5 | 3.9 → 3.8 | +3 k tris |
| railyard | 20.9 → 21.2 | 2.8 → 3.1 | none (haze uniform only) |
| alpine | 20.9 → 20.7 | 3.0 → 3.0 | +41 k tris |
| winter | 20.9 → 21.3 | 3.4 → 3.5 | +44 k tris |
| airfield | 20.9 → 20.7 | 2.5 → 2.6 | +2 k tris |
| desert | 20.9 → 21.9 | 3.4 → 4.0 | +74 k tris |

Mesh families, instanced families, materials and textures are unchanged on
every map. Every frame-median delta is inside the 8 % / 0.75 ms budget; the
gate flagged urban, railyard and desert on the render-median budget
(max(0.25 ms, 5 %)). Reruns show that budget sits below this machine's
run-to-run noise even in the same pacing mode: railyard (no content change)
went +0.3 then −0.2 ms; desert +0.6 then −0.4 ms; urban +0.9, then −0.1 and
+0.4 ms across two consecutive same-mode pairs with flat frame medians
(20.9 → 21.2 and 20.9 → 20.9). One rerun also caught the pacing flip again
(verdant/urban baseline at 11–13 ms frames with 7–11 ms render medians against
21 ms / 3–4 ms on the candidate), which is why cross-mode pairs are discarded.

Reading: no map shows a content-driven frame-time regression; the largest
structural cost is urban's +158 k triangles (allotment/lane trees and hedges),
worth a look if a lower-tier budget ever tightens. Scratch trees used:
`$SP/baseline3-wt` (HEAD vehicles + pre-pass world) and `$SP/prebatch-wt`
(23eaeba24, rejected by the harness as a timing baseline because the T-90
family geometry, and so the Garage owner counts, changed since).

## Visual restoration against 1049e4e (2026-09-12, afternoon)

Owner review: the game had regressed against the 2026-08-30 reference
(1049e4e) in shadows, horizon, tree bases, leaves and ground cover, and the
water read flat. Side-by-side shots (reference tree at
`$SP/ref-1049-wt`, current tree) found these authored regressions, each now
reversed or redesigned:

| system | regression | change |
|---|---|---|
| shadows | flat 2K/1K cascades to 520 m on every preset, AO off, crowns cast nothing | per-preset cascade table (Ultra 4K to 700 m + full AO, High 2K to 700 m + half AO, Medium 600 m), crown shadow proxies on the shadow-only layer, outer cascade on alternate frames |
| horizon | terrain-following canopy belts drew as contour strokes | belts opt-in (`horizon.faceBelts`), no map authors them |
| tree bases | eased pedestal read as a cone under every trunk | fluted root collar + swept surface roots; far stem six-sided |
| leaves | flat paint on transparency, cool tips, 0.50 wrap | understorey backing under every clump, warm tips, 0.38 wrap |
| grass | 45 % blades vanished past 60 m | 70 % blades, mid/far cut 0.34, Verdant/Coastal sward tints, far turf relief 0.45 |
| water | one flat sheet, little sky | shallow tint + foam per profile, bluer sea, env 0.55 |
| coastal | wear breakup muted to 0.22 | 0.32 (0.45 opened beach-sand islands inland; Saltwind inherits) |

Verdant field pixels: reference 101/114/36, before 124/132/42, after ~105.
Receipts restated per change; timing noted in the shadow section above.

## Road pass — distance-field roads, reference relief, crisp shadows (2026-09-12, evening)

Owner review after the restoration commits: "all our map textures look too
flat, for example the roads are so flat and everything looks so undetailed",
and the 30 August build "looks better while not being all glitchy and flashy
with the fuzzy looking shadows". Side-by-side with the 1049e4e player view
(`$SP/ref-gameplay/player_view.png` vs `head-gameplay-r9`) the road was one
uniform light-tan band and the ground had lost its near relief.

### What the shader diff showed (GLSL-only diff, 175 lines)

| system | 1049e4e | before this pass | cause |
|---|---|---|---|
| near soil octave | `dn * 0.85 * dNear` | `dn * 0.22 * openNear` | pothole fix flattened everything |
| clod octave | `dn2 * 0.75 * dNear2` | `dn2 * 0.18 * nearG` | same |
| road core | local albedo × tint, gravel 42 % near | deep-mip (+7) packed dirt, 25 % local | "oval stain" fix |
| rut wear | 0.55 dirt / 0.30 paved, far ×0.9 | 0.10 / 0.22, far ×0.45 | 2 m-texel Gaussian lanes read as beads |
| landform dapple | on roads | off roads | pothole fix |

The root cause of the bead/oval artifacts was the mask: `texSize(512)` over
1024 m is a 2 m texel, and two 0.55 m Gaussian lanes at ±1.55 m cannot be
stored in it. Every later "fix" removed detail instead of fixing the encoding.

### Change

- **Mask G byte = road centreline distance field** (`px[j+1] = 1 - d/12`).
  Bilinear filtering of a distance ramp is exact away from the centre kink, so
  `splatCompute` now evaluates analytically per pixel: the carriageway edge
  (`roadHalf = 3.85 ± noise gauge`), the twin wheel lanes
  (`lane = exp(-((d-1.55)·uLaneK)²)`, σ 0.42 m on the 2 m mask) and the crown.
  On a coarse 4 m (mobile) mask `uLaneK` is 0 and the lanes collapse to one
  bead-free compaction plateau (`1 - smoothstep(2.6, 3.6, d)`), because inside
  half a texel of the centre bilinear filtering can only overestimate.
- **Road surface**: packed-dirt palette from a smoothed mip (+4, was +7) mixed
  30 % with the local ground; clamped zero-mean gravel grain (rock-tile
  luminance high-pass, −16 %…+20 %) under 48 m; lane wear 0.34 dirt / 0.26
  paved with the ×0.9 far boost; lanes run 14 % less rough (damp compaction);
  +5 % dusty crown; lane relief from the analytic lane slope along the field
  gradient (0.14); along-lane tyre streaks (±8 %) from an anisotropic noise
  sample oriented by the field gradient; half the landform dapple stays on
  the carriageway.
- **Open ground relief**: first octave 0.70 (cap was 0.25), clod octave 0.60
  (cap 0.25), both still excluded from the carriageway.
- **Shadows**: Ultra mid cascade back to 4096 (`[4096, 4096, 4096, 2048]`),
  Medium range back to 520 m; the far-partition ellipsoid crown proxies are
  removed (1049e4e cast nothing beyond ~260 m; the ellipsoids read as soft
  blobs under every distant crown). Near canopy proxies are unchanged.
- Terrain program key `world-terrain-splat-v30`; one new scalar uniform
  `uLaneK`; three new lexical fetches (two rock luminance taps, one streak).

### Receipts

Restated with dated notes: `terrainRoadMaterial` (rewritten around the new
recipe, keeps the pothole/cavity guards), `terrainSurfaceDetail` (octave caps
0.70/0.60), `terrainWornDirt` (mip +4, v30), `terrainMaterialOwnership`
(uLaneK, nearN 0.60, v30), `terrainMaskShaderTestOracle` (78+4+3 fetches),
`quality.selftest` (Ultra sizes, Medium 520), `roadMaskProfile.ts` (rut
helpers retired; `roadLaneSharpness` added). Retired: `roadRutMask.selftest`
and the point-rut history oracle (replaced by `roadDistanceField.selftest`:
288 bearing/phase cases at 2 m and 4 m texels — lane variation 0.050 desktop
/ 0.000 mobile, edge scallop ≤ 0.12 m, centre error ≤ half a texel).
Golden RGBA masks re-pinned in `shoreDirtMask` (31), `villageWear` (6),
`workedGroundMask` (1). Channel check against the previous painter
(`$SP/mask-channel-compare.log`): **R/B/A byte-identical on all 30 maps**,
G differs exactly on the road-field texels (e.g. verdant 12 243, urban
46 848).

### Evidence

`$SP/head-gameplay-r11/player_view.png` (two-track dirt road with dark damp
lanes, pale crown, grain and edge wear, sun shadows falling across it),
`$SP/head-shots-r11/shots/verdant/{building,establishing,detail}.png`.

## Settlement pass 1 — window joinery (2026-09-12, evening)

Owner: "everything looks so undetailed … better looking environment and
buildings". First building step, applied through the shared exterior pass so
every catalog building on every map gets it at once: bare authored window
panes (thin dark/glass/curtain boxes on the centred wall envelope, inside the
wall height) receive jambs, a head and a sill; alternate rural/timber
buildings hang shutters on two of every three windows. Panes a builder
already framed (cottage, rowhouse, aperture packages), attic panes above the
wall and recessed wing panes are skipped. Pieces are embedded 1 cm into the
wall so each carries a real area joint (the connectivity receipt still
rejects floating parts). Budget: at most 12 panes per building, ≤ 6 pieces
each (≈ 60 triangles per window); the exterior receipt bound moves 72 → 128.

Receipts: `exteriorDetailKit.selftest` gains joinery cases (framing, skip on
existing frame, attic exclusion, shutters parity, canvas/open exempt, area
joints); `orchardBathhouse.selftest` ledgers/hashes re-pinned for the timber
bathhouse's +8/+24 joinery pieces (annotated). Structure connectivity,
collision, catalog count, night emission and village window receipts pass
unchanged. Evidence: `$SP/head-shots-r13/shots/verdant/building.png`,
`$SP/head-shots-r13b/shots/urban/establishing.png`.
