# T-90M Proryv (id: t90m) — reference packet

Variant: T-90M Proryv (obr. 2017) — welded turret replacing the T-90A cast
shell, Kord 12.7mm RWS on the roof, bustle stowage boxes, Relikt ERA on
skirts and turret cheeks, 2A46M-5 with thermal sleeve.

## Provenance
Local oracle `/models/tanks/t90m_minehffd.glb` — "T-90M" by minehffd,
CC-BY 4.0 (license stamped in asset.copyright + scene extras; credit block
in src/vehicles/userdrops2.js). Registered by the unconditional userdrops2
override (specs.js's `t90m:{source:'procedural'}` row is REPLACED at import
— found by the 2026-08-03 zero-row triage; this packet is the first for the
id). Registration: turret '^Turret$' (46 turret meshes incl. Kord RWS, ERA,
baskets), gun '^Main_barrel$', autoPivot, scaleToOverall (no clean hull-only
box: thermal-sleeve meshes sit outside the gun node), nose raw -X, yaw +90.
Known minor: the mantlet is a turret SIBLING mesh (stays put while the
barrel elevates).

## Real-vehicle dimensions
- Hull 6.86 m, overall 9.63 m (gun forward), width 3.78 m, height 2.23 m
  (turret roof line, Kord excluded) — T-90/T-90S family figures
  (rosoboronexport datasheet class; same numbers registered for t90sm in
  userdrops5.js).

## Local GLB oracle — extract state (REG batch 0a39d55)
Pre-warp: width TRUE 3.78 (0%); roof line nearly true (turret roof cols
2.25-2.32 vs pub 2.23); bodyH read +25.9% (2.807) — ENTIRELY the wide
furniture band (Kord RWS + bustle bins) crest 2.72-2.97 over gate-z
-2.4..-1.2; hullMask 7.482 (+9.1%); overall 9.891 (+2.7%, tube slightly
long relative to the compressed-body target).

Warp (repair batch-23, vertex-normalize PLANS t90m): knee 2.20 — roof
untouched, furniture band compressed to 2.20-2.253 (post-map p95 ≈ 2.24,
inside the 1% dims grace; y_top_max assert 2.26). Body z ×0.9169 about body
center -1.2045 (hullMask -> 6.86); muzzle pinned at rear+9.63 (tube segment
stretches ×~1.15 to absorb the body compression — abramsx-class pin).

## r26 FIRST BUILD (2026-08-03): zero row -> 64.7 min / whole 73.1

buildT90MProryv in src/vehicles/profiles/russia.js (registered in
RUSSIA_PROFILES — overrides the legacy tankFactory buildT90M via the
PROFILED_BUILDERS merge). Final row (gate x2 stable): hull 65.0 /
whole 73.1 / turret 64.7 / stations 69.0 / dims 98.2 / floaters 100
(min 64.7). standard-check: holes 0 ✓, mg census 1 (Kord as
FITTINGS.pintleMG on a recessed pedestal — crest 2.251 inside the roof
plateau band, §I); clip 581/244 vs the ≤60 band — same strip/sponson
class the certified graduates read (pt91m 178/220, t72b3m 863/301
today); needs the fleet ruling.

ORACLE GATE-IN-LOOP CHECK (the round's mandate): the re-warped oracle is
HEALTHY — rendered ref masks contiguous in all rows, stations compare
within a few %, the warped furniture band holds 2.20-2.25 as documented.
The pre-round all-0 ledger row was the LEGACY canonical build (height
2.52 vs pub 2.23), not an oracle defect. One ref-side surprise, banked
below: the EXTRACT's own curve tables disagree with the rendered ref in
two places (bow plan corners; skirt-course span) — §D render-outranks-
rows extends to the extract JSON.

World frame = extract + 1.38. What the build carries (decode-corrected):
- Bow: plan center 3.05 / corner prongs 3.27 (extract corners said
  3.20/3.44 — RENDER WINS), mud flaps at the 0.66 bow floor, slim center
  bow probe (tow-hook cluster) 3.34..3.40 = the hullLengthM body anchor
  hidden behind the gun's plan shadow (its 2 naked plan_hull columns are
  the accepted price; dims 98.2).
- Rear: full-width plate -2.90, RACK to -3.43 at |x|<=0.99 (floor 1.17,
  transverse drums, spare bin, stays to 1.80 at ±1.0 — ref front cols),
  CENTER NOTCH (ref center-rear is -3.11: split log/mesh/bin + tow
  hitch), unditching log slung at -3.44 (rear -3.53; ref plan rear -3.59
  at |x|<0.9; overall 9.74 = +1.13%).
- Gear: wheels at the ref's ground contacts (evenStations 4.01/0.515 =
  -1.49..2.52), sprocket -2.10, idler raised (0.78/0.22 — its wrap
  bottom owned the 3.05-3.25 columns), track xc 1.435 / trackW 0.58
  (LINK OVERHANG: shoes reach ±1.748 vs the ref's 1.73 band), arms off,
  strips on the rendered ramp lines (rear jumps 0.52@-2.68 -> 0.86 plate
  line).
- Skirts: thick band (0.09) at faces 1.77..1.86 y 0.78..1.36 + the
  Relikt COURSE as thin outboard bags at ±1.89 (y 0.48..1.34, z
  2.46..-2.66) — the rendered ref's widest, near-full-length content
  (the extract's plan row said rear-only: WRONG, render wins). Bow
  mirrors deleted: the ref's 4 cm mirror flickers between grid phases
  (present in one run's columns, absent the next) — carrying it costs
  0.35 err when absent.
- WELDED turret: polyTurret staircase planform (chamfered corners,
  inset 0.93 — near-vertical walls; 0.80 read 0.3 narrow at the roof),
  roof TIERS from the rendered profile: 2.245 plateau z -1.11..-0.03
  over a full-width 2.20 tier (±1.15), 2.20 step to +0.51, 2.03 to
  +0.93, 1.94 wedge to +1.48, mantlet hood <=1.71 (plan front law);
  hidden turret-node APRON (print bakes hull kit into the turret node)
  bottoming 0.88 across fitted [-0.92, 0.87] with half-height end
  treads (CLIFF-LERP law); bustle bins as SEPARATED members: upper tier
  1.51..2.01 to -1.9 wide ±0.95, tail tier to -2.43 RIGHT-DEEP (ref
  rear staircase is asymmetric, left ends -2.02), rails + door -2.45.
- 2A46M-5: axis 1.61, sleeve 0.118, evac r 0.128 + crest fin (the 0.138
  cylinder's raster leaked the ±0.19 plan columns at its full z-run —
  PLAN RASTER LEAK law), muzzle +6.20 exact.

Honest residuals for the next round (whole 73.1 / turret 64.7):
- turret plan +0.19 col err 0.84: proc front-v reads z 3.44 at ±0.19 —
  no authored turret mesh exists there (whatsat-clean); suspect the
  trace/interp seam or a proxy; needs a mask dump before more geometry
  is spent on it.
- turret side 1.06/0.93 cols (~0.3): apron front-edge phase vs the
  settled dAlong 1.363 — one more boundary nudge after the next
  registration-stable run.
- hull 65: front rows carry the remaining glacis-top skew (ref 2.02-2.17
  at |x| 1.1-1.4 vs prism walls) and the belt flat-end ground reads at
  z -1.9..-2.4 / +2.9 (fleet-certified partial class).
- stations 69: i10/i12 tops ~10% — ref-side slice content (its z-range
  is its own hull mask) reads the fused-mantlet mass in slices my build
  keeps gun-parented; structural to the node split, price ~1.4.

## Build notes (for the russia-family builder)
- Profile-ize vs t90sm/t90a: the triage probe shows the canonical T-90M
  build already boards ~75 as a donor; the welded-turret planform (flat
  cheeks, chamfered front corners) is the main identity delta from the
  t90a cast dome.
- SEPARATED FURNITURE: build the Kord RWS + bustle boxes as thin members
  (post-warp ref keeps them at 2.20-2.25) — decoration law satisfied by
  the Kord (mandatory roof MG) + bins/ERA blocks.
- Relikt skirt line and cheek wedges read in the side/plan masks — follow
  extract corners, not the t90a profile's K-5 bricks.

## r27 WORKORDER ROUND (2026-08-04, russia agent — r29 respawn session):
## 64.7 → 67.9 min; both r26 ordered residuals RESOLVED

Gate at close: **min 67.9 (hull 70.6 / whole 75.4 / turret 67.9 / stations
69.6 / dims 98.2 / floaters 100)**. Trajectory by component across the
round's six gate cycles: turret plan 82.2→86.5, front_hull 72.2→78.9,
hull 65.0→70.6, whole 73.1→75.4, turret side 64.7→67.9, stations 69→69.6.
standard-check: holes 0 ✓ (two 1-cell regressions from the trackW trim
were plugged same-round), mg 1 ✓, clip 296/249 (improved from 581/244;
still the strip/sponson fleet-ruling class).

THE r26 MASK-DUMP ORDER (turret-plan ±0.19 col, err 0.84) — DECODED AND
FIXED, two stacked mechanisms:
1. The evac swell seg (r 0.128) raster-clipped the ±0.17/0.201 plan
   columns over its world 3.20-3.44 z-run — the same mechanism the r26
   note documents for its earlier 0.138 cylinder (col inner boundary
   ±0.108; 0.128 still clips 20 mm). Swell → the tube's proven-quiet
   0.100; the crest FIN alone carries the ref's 1.75 side crest.
   MEASURED: the 0.843 entry died; turret plan 82.2 → 85.9 on the next
   gate run.
2. The GUN SHADOW PROXY ran to world 7.28 — spec.armor.gunBarrel.lengthM
   6.0 predates this profile (authored muzzle 6.20 = ref-exact). The r26
   "no authored mesh exists there" mystery: proxies disable raycast, so
   whatsat can't see them. Spec datum aligned in-builder (4.92);
   probe-verified z 1.28..6.20. **FLAGGED FOR ORCHESTRATOR RATIFICATION**
   (consumers audited: proxy + muzzleZ default (overridden) + UI framing).
   LAW-BANK CORRECTION: an A/B mask dump (proxies visible vs hidden)
   measured ZERO poisoned columns in side/front hull masks — §C's
   "shadow-proxy meshes ARE in gate masks" is STALE for the current
   harness (renderMask filters them like the shared-box isShadowHelper
   filter). Proxies still matter to the vertex-workorder's raw traces.

THE r26 APRON-PHASE ORDER (side 1.06/0.93 cols): the front tread's bottom
sat 0.34 low (1.04 vs the ref's 1.38 line) — slimmed to a 6 cm step ON
the 1.38 line; the carrier rear extended to the r26 packet's OWN fit
(−0.95; it was authored to −0.66 while the packet fitted [−0.92, 0.87]).

FURTHER DELIVERED:
- idler 2.90 → 2.65 (+5.6 hull in one cut): the ref's side-hull bottoms
  at z 2.99-3.36 are its RAMP LINE — the r26 fade strips match it exactly
  (0.50@3.06 / 0.57@3.14) — and the five worst side_hull columns were my
  idler wrap + belt dive hanging below that line, 0.4 m forward of the
  ref's own gear end (~2.6; contact stops 2.52).
- trackW 0.58 → 0.50: the link shoes reached ±1.748 and painted the
  ±1.76/1.77 front columns to the ground (err 0.351 ×2) where the ref
  bottoms at its 0.72 skirt lip. Both entries left the worst list.
- rack stays narrowed to the ±0.94 col (x 0.90..0.99): the 0.16-wide
  boxes also painted the ±1.04 col whose ref top is 1.371 (err 0.202 ×2)
  while the ±0.94 col's ref (1.75) keeps wanting them.
- turret plan front: planform nose 1.75 → 1.50 local + mantlet hood slim
  0.80 → 0.44 + cheek course sz 0.95 → 0.80 — the CHEEKS owned both the
  ±0.32-0.57 plan-front columns (ref 1.64-1.79) and the z 1.5-1.78 side
  tops (ref 1.81); the planform taper alone measured nothing because the
  cheek course prints there. Hood front (1.84) carries the ±0.17 cols
  (ref 1.883).
- belt front terminus: EXTERNAL contact point (zF 2.98) past the idler
  wrap kills the near-vertical dive chord at the old cF=idler.z default;
  rear point inside the sprocket wrap degenerates to the 190° fallback =
  byte-identical rear loop, matching the ref's grounded −1.9..−2.4 warp.
- sponson-floor strips (±1.74, inside the skirt band's y-band): plan-only
  fill for the two 1-cell holes the trackW trim opened.

NEGATIVE RESULTS (documented so the next agent doesn't repeat them):
- apron aft-extension to −1.47 world: −14.6 turret side in one run (five
  columns of 0.79-bottom where the registered ref bottoms 1.38-1.44).
  The at-frame that motivated it is RUN-RELATIVE (shared box moves with
  geometry) — never carry at→world offsets across an edit.
- belt contact at the wheel centers ({2.60, −1.60}): −2.3 side_hull. The
  warped ref's belt IS ground-flat at −1.9..−2.4/+2.9 (the r26 note reads
  the REF): flat botY matches it; only the FRONT terminus chord needed
  taming.
- rear-rack lowering (drums/bins to 1.42/1.55): −0.35 ×2 at z −2.83..−2.96
  — wrong-frame decode; r26's 1.89 tops are ref-matched. Reverted.

Honest residuals toward ≥80 (turret side 67.9 is the min):
- turret side: the roof-tier band (procTop ~2.0 vs ref 1.81-1.99 over
  z 0.3-1.0 — needs the prism/tier split re-derived against a fresh
  vertex-workorder, ~4-6 cols); the bustle ONLY-x trio (cover 0.97).
- stations 69.6: i10/i12 tops ~10% ×2 = the fused-mantlet node-split
  class (r26 priced ~1.4, structural); i0/i1 rear reads unresolved.
- side_hull 70.6: remaining mass is diffuse (meanPct ~2.3 vs a warped
  ref; no single column >0.16 after the idler fix).
- dims 98.2 unchanged (bow-probe plan cost, accepted r26).

## r30 WORKORDER ROUND (2026-08-04, russia agent): 67.9 → 81.7 min ×2
## (hull 81.9 / whole 81.7 / turret 83.3 / stations 81.9 / dims 100 / fl 100)

standard-check: holes 0 ✓, mg1+2d ✓ (Kord + 902B banks as FITTINGS), clip
222/188 (improved from 296/249; still the strip/sponson fleet-ruling class).
Shots: shots/visual-eval-t90m/ (14 views + report.json).

FRAME LAW (this round's engine — supersedes every per-run at→world recipe):
the gate JSON's `at` decodes as authored_z = C.z + dAlong − at (side; C =
TRUE shared-box center, dAlong = the row's own reg), y = v + C.y. The
vertex-workorder's printed side coordinates carry a +0.63 bias THIS class of
run: its `vb()` recomputes the shared box while the page's LAST renderMask
leaves reference.root invisible → C degrades to the PROC-ONLY box center.
Its side rows also pair columns RAW (no dAlong shift) — with this print's
scene offset (~1.36) every printed side gerr is frame-smeared. Bespoke
digest (scratchpad gate-digest.mjs pattern) replicates curveScore's exact
registration and prints both models in the authored frame; official gate
stays the verdict rig (§D).

Delivered (registered-row driven, per-edit gate deltas in git history):
- ROOF TIER STACK: prism hm 0.59→0.44 (walls 1.84); tiers 2.2375 humps
  (split ±0.51 w/ 2.07 center saddle — ref front center cols read 2.07,
  two-hump roof) / 2.20 step / 2.03 to 1.10 / 1.90 to 1.35 / 1.81 to 1.66
  / hood 1.51..1.71; asymmetric shoulders (L 2.01, R 2.20 caps at ±1.08;
  ref roof is left-low) + shoulder caps 2.07@±1.23, 1.98@±1.38 (z-short,
  inside the ref plan-turret rear line). Cheeks single-row (rRows 1 — the
  upper cassette course crested 2.0-2.05 where the ref roofline steps;
  the ref's cheeks stay UNDER its roof tiers).
- STATION END-CAP LAW BITES 3×: the 2.76 m tube seg had no z-caps inside
  stations i11/i12 (front-clip renders z-cylinders edge-on; only END CAPS
  paint) → tube now 10 segs ≤0.55 m; bustle upper tier split at −1.74
  (its cap left i3's window → topPct 25.5); plateau humps split at −0.80
  (i5 had no cap → 4.94). Stations 69.6 → 81.9.
- BUSTLE: upper tier to −2.15 (ref holds 2.00/1.51 through the −2.04..
  −2.16 col), tail tier 1.62..1.87 to −2.38, low rack bar to −2.445 top
  1.40 (ref's z −2.47 col is deck-line 1.38, NO turret band, while its
  plan turret runs to −2.42 — the old 1.94-to-−2.43 + door −2.46 printed
  1.90 there, the top side_whole item 0.27).
- CARRIER: rear −0.95 → −0.88 (at-3.04 col held a 0.79 bottom vs ref
  1.37); bottom 0.80 → 0.88 (the ref apron line — r26's own packet fit;
  five 0.066 cols).
- TRACK CONTACT LAW FIND: `contact:{zF,zR}` (r27) is a SILENT NO-OP —
  tankFactory reads cfg.contactZF/contactZR only (defaults wheel-span
  ±wheelR/2 = 2.71/−1.68 here). Both r27 contact "results" (the credited
  dive fix AND the −2.3 revert) measured other same-batch edits. Real
  values: contactZF 2.45 / contactZR −1.50 (ref belt lifts 0.16@2.74,
  0.15@−1.85, 0.22@−1.97, 0.29@−2.09; pad tips sag ~0.07 below the band
  tangent, so the tangent start sits ~0.1 behind the ref's visual lift).
- SKIRTS: band re-span x-faces 1.762..1.842 / z −2.61..2.55 with a front
  TAPER (1.28@2.68 / 1.17@2.91 / 1.12@3.14 — the flat 1.36 top ran to
  3.28 where the ref side line falls; five 0.10-0.15 cols) + Relikt
  course half-bag to 2.80 (ref's widest content reaches 2.81@±1.89;
  the r26 "rear-only" extract row stays wrong, render wins); bag hems
  back at 0.48 (the r30a 0.60 lift chased a stale dy).
- REAR RACK staircase to today's registered reads (drums 1.76 / stays
  1.72@x .8975..0.9875 / bins 1.84 z −3.04..−3.34): NOT the r27
  wrong-frame lowering — a 5-8 cm trim; center drum-coupling box (top
  1.61 — ref front-view center cols; the drum pair's x 0.105 inner ends
  also painted the ±0.11 front col to 1.76 → drums start x 0.15). Floor
  plates x 0.145.. (0.12 sat ON the ±0.12 plan col boundary; its AA
  bleed painted the center cols to −3.41 → plan_hull 0.31 pair). Log at
  −3.41 (overall 9.70 = +0.77%, dims cost 0); rear flaps at ±1.55
  y 1.08..1.32 (ref plan rear −3.00 at |x| 1.36..1.73).
- FRONT: flaps widened x 1.4525..1.8425 @ z 3.2425..3.2875 hem 0.71
  (ref plan front 3.28@±1.8 / 3.33@±1.5 with side tops ≤1.09); fender
  lips i=1..8 only (fwd pair deleted — ref glacis-top line 1.29@2.25 →
  1.23@2.62; wedge rows −6 cm); periY 1.24 + hatchY 1.27 + hlY 1.19
  (near-flush class, t72b3m lesson); glacis kit re-seated.
- DECOR/§B3: Kord crest 2.243 (the r30c plateau restore had left the
  crest 2.211 BURIED under the 2.2475 plateau — un-buried, then heightM
  p95-guarded: crest cols ~4 > the 3.25-col p95 exclusion at 2.27);
  902B smoke banks on the cheek flanks (tips ≤ x ±1.786, tops 1.73 — the
  smokeBank xform pivots tubes at their CENTERS: tips gain only
  len/2·sin(a); first seat undershot by 0.09) + cheek stowage baskets
  (ref front dome 1.84-1.90@±1.64, z-end 1.02 = ref plan line).
- 2A46M-5: root cone 0.50 (its 1.76 crest cap sat over the ref's 1.70
  tube line at i11); evac fin 0.25 band (0.276 qualified as a body col
  and dragged hullLengthM to 6.94).

NEGATIVE RESULTS (measured, reverted):
- 2.30 tier narrowed to 1.96: front_whole 77.9 → 60.2 in one run (ref
  front carries 2.16-2.21 across ±1.0..1.15 — the ±1.04 "ref 2.00" read
  was the LEFT side only; the roof is asymmetric).
- plateau 2.245 → 2.21: chased a 256px-quantized digest read; the 1024
  gate says the ref turret line is 2.24 (restored, then heightM-guarded
  at 2.2375).
- Kord crest 2.27: became the heightM p95 (pct 1.62, dims −5).

FLAGGED FOR ORCHESTRATOR:
1. `contact:{zF,zR}` no-op (above) — either alias it in tankFactory or
   sweep profiles for dead configs; r27's contact conclusions need
   re-attribution.
2. visual-evaluator TOP-view RIG MISMATCH on t90m is a FALSE POSITIVE:
   near-±90° plan principal angles land on opposite atan2 branches
   (ref +89.978° / proc −89.998°) and the skew-flip test compares raw
   skew signs without normalizing eigenvector direction — normalized,
   refSkew −0.168 ≡ procSkew −0.176 (same physical rear-heavy skew);
   dPrincipal 0.024°, dCentroid 0.03 m, dArea 0.3%, other 13 views
   yawProxy ≤0.9°. Evaluator needs a direction-normalized skew (e.g.
   flip e when principalDeg < 0) before the top-view abort is trusted
   on plan-symmetric prints.
3. workorder tool: restore both roots' visibility before its vb()
   (the C bias above) and apply dAlong when pairing side columns.

Honest residuals toward 90 (all rows, worst first):
- side_hull 81.9-min class: belt-rise tails (±0.05-0.08 over z 2.6-3.0
  after the contact fix), painted sprocket end-board fan edge ~0.6@−2.7
  (ref's baked fan is smaller), diffuse 0.05-0.1 mid-deck tops.
- front_whole 81.7: right-bank inboard dome gap (ref 1.90@1.64, one col
  0.10 — a 1-col riser violates the 15 mm law, left unfixed), band-face
  col ±1.81 top 1.36 vs ref 1.26 (ref panel-seam dip, 1-col ×2).
- turret side 83.3: plateau/step micro-steps ≤0.05; plan_turret 88.7:
  hood front vs the plan dy drift (re-measure after any hull plan edit).
- stations 81.9: wPct i2/i4 3.38 (dropped by trim; band-rear/bag zone),
  i9/i11 2.24 (bank tips / bag width vs ref 3.69).
