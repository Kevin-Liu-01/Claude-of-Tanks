# Warrior side ledges: source stock and real air

Source study and subsequent bounded correction against canonical Warrior source SHA-256 `e568badc436980b9f2b718db9786120fcc7527b7fe6465c3efa66889fa208c04`. Parent's complete-source FrontSide check classifies the existing 663 continuity cells as 307 source-air and 356 missing source-stock. The stock consists of 282 `Object_5` ledge hits, 62 `Object_27` upper armor-flange hits and 12 `Object_21` forward hull-edge hits. Permission to retain source-real gaps does not excuse the missing stock.

Private reproducible measurements are `.qa-dev/tank-run/warrior-side-ledges/{study.mjs,study.json,sections.py,sections.json}`. Component IDs below refer to this study's 10 µm position weld, not a stable GLTF node index. Source face indices and world coordinates are recorded. `guard-witnesses.json` contains 31 complete-source held-out down-rays: 18 stock and 13 air, with exact first-hit points. They are independently chosen non-raster positions, not silhouette-mask samples. The source-only sections are measurements for scalar construction; they are not runtime mesh data.

| Missing stock | Measured shape | Bounded procedural proposal |
|---|---|---|
| Right inner ledge, `Object_5` component 459 | Main web X `1.5820..1.6133`, bottom Y `1.1515`; flange reaches X `1.7275`, main top Y `1.9005`, underside `1.8888`; outer return X `1.7168..1.7275` ends at Y `1.8673`. Rear end Z `−2.7656`, rear chamfer `−2.7148..−2.6504`; forward top slopes from `(Z1.4639,Y1.8995)` to `(Z2.6660,Y1.4083)`. | Author an open stepped section with thin flange, inner web and short return; preserve the source rear chamfer and forward slope. Do not fill the hollow underneath or extend into the stand-off gap. |
| Left inner ledge, `Object_5` component 1023 | Web X `−1.6172..−1.5977`, bottom Y `1.1583`; outer flange X `−1.7314`, return inner X `−1.7217`; same main top/underside/return heights. Forward end is asymmetric: outer edge begins taper at Z `1.4902`; inward web continues to Z `1.9697`. | Use a separate measured left section and taper. In particular X `−1.68`, Z `1.83` is real air, while the right counterpart is stock at Y `1.749905`. Mirroring the right extension would invent left stock. |
| External armor upper flange, `Object_27` components 422/450/478/506 | Left inner edge `(X−1.88184,Y1.86902)` slopes outward to `(X−2.10264,Y1.76282)`. Right inner extent is X `1.88160`. Main left sections cover Z `−2.72667..1.54070`, then the front edge tapers. | Correct the existing broad armor's upper cross-section and forward taper. Its current inner X `±1.91` and high flat outer top lose the source's inward sloped flange. Avoid overlaying a second full plate or widening the gap exemption. |
| Left forward hull ledge, `Object_21` component 7818 | Outer edge near X `−1.5938`: Y `1.5538` at Z `1.4697`, declining to Y `1.3585` at Z `2.7578`. Receiving bevel reaches X `−1.5518`, Y `1.5773` at Z `1.4854` and X `−1.5527`, Y `1.5050` at Z `1.9609`. Source hull underside is near Y `1.1476`. | Restore the narrow solid receiving hull-edge section using those independent stations. The current forward half-width shrinks early and misses it. Keep the existing gun, gear, rear mudguards and rest of the hull unchanged. |

The main stand-off corridor remains real air: complete-source down-rays at X `±1.80` and Z `−2.20, −1.55, −.65, .45, 1.23, 1.83` have no hits. At X `±1.68`, the first five matching positions hit the ledge top near Y `1.9005`. Five left flange witnesses at X `−1.895` hit Y `1.862690`. The left forward witness X `−1.57`, Z `1.70` hits `Object_21` at Y `1.533710`; at Z `2.15`, a real `Object_9` fitting is first, above the hull-edge stock. Full-source evaluation must retain that occluder instead of selectively raycasting only the expected mesh.

Sixteen further finite full-source inward rays, from X `±1.80` at Y `1.30/1.70` and Z `−2.20,−.65,.45,1.23`, are preserved in `web-witnesses.json` and appended to the guard recipe (47 total witnesses: 34 stock / 13 air). Their first hit is the actual hanging inset panel before the inner web: left X `−1.7061`, right X `1.7012`, except the short right panel's rim at Z `.45`, X `1.7148`. The next Object_5 hit is the ledge web at X `−1.6172/+1.6133`. The source's separate recessed panels are therefore part of the receiving stock, not a reason to accept a top-only cap. Their envelope is Y `.7955..1.8800`; regular left panel X is `−1.7188..−1.6182`, right `1.6143..1.7148`. The source has 15 mm raised perimeter borders around 12.7/13.6 mm recessed faces and small inter-panel gaps. Any private construction draft must retain these source-supported panels and their measured hinge seats as well as the J-channel; it must not fill the entire channel or close the corridor.

For the parent's opening validator, finite comparison domains are left X `−1.91..−1.55`, Z `−2.766..2.14`, and right X `1.55..1.91`, Z `−2.766..2.666`, both Y `.79..1.91`. These are **comparison domains, not air exemptions**: every current cell still needs complete-source classification and every source-stock hit still needs actual native stock. Separate held-out finite web rays should reject a top-only patch, and the air witnesses should reject filling the corridor. Keep the original 663-cell report and all source hashes unchanged.

Source and native final-fender `top.png` and `hero-toptilt.png` were inspected; their visible long corridor difference agrees with the measured missing flange/web stock. After the parent released its completed baseline-validation freeze, this source-backed correction was applied only to `src/vehicles/profiles/fv510MilanX.ts`. The canonical source and historical failed receipts remain unchanged. The geometry below has focused native evidence; final regenerated assets, source scores and independent rendered qualification are owned by the parent.


## Applied stock and finite assembly

The authored open J sections retain all three right outer-flange seam notches. Separate hanging inset panels retain their raised perimeter, broad recessed face and small panel gaps. The right forward panel retains a real circular blind recess with a back plate. Only the existing left external armor upper contour was replaced; no second broad plate was overlaid. The left forward hull shoulder supports the measured Object_9 folded service cover and two retainer blocks. Source `fitting-quarter.png` and `fitting-top.png` were actually inspected before its scalar construction.

The supplied panel upper rims stop laterally 2.0 mm (right) / 2.9 mm (left) short of their overhanging returns. The authorized finite native seat extends **only the concealed upper border** outward 2.7 / 3.5 mm, leaving 0.7 / 0.6 mm real overlap. Visible broad faces, open corridors and all held-out source rays are preserved. Folded cover feet overlap the actual hull shoulder by about 1.42 mm at the checked seat. Concave primitive containment uses actual ordered entry/exit triangles, so an enclosing box cannot falsely establish contact across the open J cavity.

## Focused results before final generation

- Loaded HIGH and LOW both restore all 356 original missing-stock raster locations and preserve all 307 source-air locations. The original 663-cell failure remains historical evidence.
- All 47 independent complete-scene first hits pass a 2 mm limit. Maximum residual is **0.908048 mm** at hull witness `[-1.57, 6, 1.70]`: source Y `1.533710276`, native `1.532802228`. Object_9 first occluder residual is **0.636288 mm**: source Y `1.521810022`, native `1.521173734`. Every other stock residual is at most 0.005418 mm; all 13 air rays remain empty. Full per-guard source/native coordinates are in private `guard-residuals.json`. The 2 mm limit covers rounded measured dimensions and a bounded planar approximation, not centimeters of missing stock; it does not waive the independent source score or seating requirements.
- `warriorSideLedgeStock.selftest.mjs` passes both qualities: 47 source first hits, five finite seats, open J undersides, flange seams, real blind recess, deliberately deleted ledge / filled corridor / retracted-panel negatives, and 1,500 actual moving-shoe conservative bounds per quality at five phases with a 10 mm margin.
- Actual selected geometry is **65,620 HIGH / 39,312 LOW** (59.91%), below the fixed 80,000 IFV cap and 75% LOW ratio. This is native selected stock, not GPU timing.
- Exact private before/after replay preserves **189 of 190 prior `P.add` calls** in each quality; only the measured left external armor primitive is replaced. All original registered fender shapes/transforms are exact. With the supported `deferStaticBatch:true` diagnostic option, 35 unaffected visible native meshes preserve position/normal/UV/color/index buffers, instance matrices, material descriptors and world placement. Two derived hull markings move; their own geometry/material bytes remain exact. The first LOW diagnostic used an unsupported option and therefore saw the new cover inside `mobileStaticBatch_0`; that failure is retained and explained, not silently accepted.

Private receipts: `draft-check-r1.json`, `draft-check-r2.json`, final `draft-check.json`, `finite-pilot.json`, `guard-residuals.json`, `parity.json`, `focused.log`, `final-focused.log`. No comparison recipe, source transform, threshold, track path or gear datum changed.

The existing `warriorRearFenderSeating.selftest.mjs` also passes after this correction: both qualities retain 10 registered seats, 16 source roof rays and 120 conservative moving-shoe bounds, including the original translated-flap failure check. Final frozen hashes and receipt scope are in private `freeze.json`.
