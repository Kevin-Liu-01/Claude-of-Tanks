# Leopard 2 Revolution native re-certification

The active Revolution is the detailed first-party procedural build. The
comparison model is an isolated visual/measurement oracle only; none of its
geometry is shipped or transformed into runtime content.

## Final receipt

- Frozen candidate: `3ead2acc` (76 meshes / 100,401 vertices).
- Fresh standard-order first-party live appraisal:
  `[9.4,9.4,9.2,9.1,9.1,9.1,9.2,9.4,9.4,9.4,9.3,9.4,9.4,9.4]`, floor
  **9.1**, mean **9.30**. This is a live visual/physical score, not a
  reconstructed source-mask result.
- Components: hull 96.41, turret 83.41, gun 95.32, tracks 97.85; the required
  overall and every-view gates pass.
- Physical course: smooth band front/rear 0/0; individual shoes front/rear
  0/0.
- Evidence: 15 procedural appraisal + 15 yaw0 + 15 yaw90 PNGs, **45 files /
  45 distinct hashes**, in
  `/private/tmp/leopard-revolution-final-r4/leo2_revolution`.

Removing the redundant 2 cm continuation card did not completely close the
owner's live defect. Fresh renders found two independent vertical fore-wall
caps still projecting beyond the continuous AMAP face as square cheek blocks.
Those caps are now gone: the left wall ends in a buried inward-tapering wedge,
and the right raked wall contracts in width and reach into the crown. Direct
front, front quarters, close and elevated profile now show continuous faceted
cheeks with no replacement patch, open seam or empty-air element.

The clearance repair narrows only the load-bearing beak and nose fill into the
inter-track corridor. The separately authored over-track mudguards retain the
Revolution shoulder envelope. Yaw evidence shows the complete AMAP turret,
mantlet, gun and roof package rotate together while the deck, skirt armor,
native seven-wheel course, and rear service structure remain fixed. No
unsupported panel, fused turret mass, winding wound, or yaw-dependent hole is
visible.

The parent audit's two hull-overlap candidates are backed engine/service deck
material groups. Fresh yaw frames show
them staying with the hull while the complete AMAP turret moves away. The tiny
five-pixel winding-yaw candidates are likewise supported fixed rear-deck/service
pieces and produce no duplicate turret mass or floating decoration.

## 2026-08-13 final owner cheek correction

The previous correction removed the paper-thin continuation card but retained
a short rectangular left-outboard wall followed by a separate tapered cap.
The shared end face was still visible as the reported square in the elevated
profile. Both pieces are replaced by one continuous four-sided cheek course
whose upper, outboard and nose boundaries taper into the primary shell. Its
broad inboard overlap remains load-bearing and no independent face or patch
survives.

Fresh deterministic freeze: `5c68747e`, 76 meshes / 100,077 vertices. Exact
band/shoe clearance is 0/0, winding is clean, the geometry gate remains 90.2,
and `/private/tmp/leopard-defects-final-r1/leo2_revolution` contains 45 unique
authored/yaw frames including the elevated profile. The complete cheek,
mantlet, gun, AMAP package and equipment rotate together; the five-pixel yaw
candidate belongs to supported fixed hull service material.
