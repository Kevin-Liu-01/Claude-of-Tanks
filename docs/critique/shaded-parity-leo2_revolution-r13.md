# leo2_revolution shaded-parity r13 — ROUND-3 ADJUDICATION (2026-08-04)

Rig: fresh 14 pairs `node tools/tmp-tank-critic.mjs --id=leo2_revolution` →
shots/critic-leo2_revolution/ (this session, zero console errors, favicon
404 only; vite 7472, FIFO lock free at start, tickets clean). Byte
discipline: `tmp-hashgeo.mjs` leo2_revolution = **eb04115c** (64 meshes /
113033 verts — exactly the r13 landing hash from ee1c4d5) verified BEFORE
rendering and re-verified AFTER all evidence runs (bookend clean;
leopard.js working tree clean at ee1c4d5 throughout — the concurrent uk.js
builder never touched leopard bytes on my watch, no STOP condition).
Graduates frozen both bookends: leo2a5 **bc9bad30**, leo2a6 **80b76338**,
kf51 **3ae9b70c** — exactly the r12/r13 freeze lines.
Official gate re-run by me: **min 90.7 PASS ×2 BIT-IDENTICAL** (hull 91.4 /
whole 90.7 / turret 91.8 / stations 90.8 / dims 99.5 / floaters 100 —
cmp-clean between runs; exactly the r12/r13 landing line).
`visual-evaluator.mjs`: exit 0, **RIG PARITY OK** (max yawProxy 0.8°
@close-front, |dCentroid| 0.191 m — the r9/r12/r13 line), evidence at
shots/visual-eval-leo2_revolution/ (my run). Measurements:
tools/tmp-rev-critic-r9-measure.py (the standing §D refined mask: maxch ≤13
AND B−R ≥ +8) + tmp-rev-r13-chainflag.py (hero-void closed-flag
re-derivation, NEW this round) + tmp-rev-r13-crop.py (crops
DIAGNOSIS-ONLY) + tmp-rev-critic-strips.py (§H.4).

## HEADLINE: FAIL — floor 8.1 (hero-frontleft), ceiling 8.5 (rear/top/toptilt), mean ≈8.3; no view at the 9.0 bar, every view up from r9 (floor +0.4, mean +0.4). The r12 order book (all 9) and the r13 §B4 kill are VERIFIED DELIVERED on my rig — several to the digit. What holds the floor now is no longer deliverable polish: the LEDGER-PARKED GEOMETRY CLASSES (bow strata / cheek verticality / plan toe / hem+wheel-row) are the top hold on EVERY sub-9 view, and under the fresh §B1 NO-STAIRCASES law the bow read is now a NAMED failing class. **This round ends in an OWNER PRICING QUESTION, not a build order book** (below). Round 3 of the projected 3-4.

front 8.3 · frontleft 8.2 · left 8.3 · rearleft 8.4 · rear 8.5 ·
rearright 8.4 · right 8.3 · frontright 8.2 · top 8.5 · hero-fl 8.1 ·
hero-rr 8.4 · toptilt 8.5 · close-front 8.2 · close-roof 8.4

## Claims audit (§D — every r12+r13 packet done-gate re-derived on MY rig)

- Gate 90.7 ×2 bit-identical, components to the decimal: **CONFIRMED**.
- Hash 9249c794 → eb04115c (64/113033): **CONFIRMED** both bookends.
- Evaluator parity 0.8°, no RIG MISMATCH: **CONFIRMED** (verdict OK).
- **§B4 KILL TO THE DIGIT**: standard-check clip **0/0 ✓** (exit 0 — the
  first all-machine-gates PASS in this tank's history: gateMin 90.7 |
  clip 0/0 | contig 0 | decor mg1+4d) AND my own
  `track-clip-audit --exact` standalone run: front **0** rear **0**.
  The 98/429 carry priced since r2 is dead.
- **§B2 flood ALL TEN VIEWS to the pixel** (refined mask, PROC halves):
  front 92, rear 101, rearleft 92, rearright 92, frontleft 92,
  frontright 92, left 119, right 125, top 122, toptilt 94 — EXACTLY the
  r12/r13 packet digits (base ≈92 px = label glyphs y13..21 x16..104).
  REF's own halves for scale: front 3320, top 4752, toptilt 1417 — the
  proc decks remain cleaner than the print's.
- **D1 tail panel WITH THE X ACROSS IT**: proc [190:447]×[338:372] med
  **77.3** p95 89.6 **sd 12.31** vs ref slats med 78.6 p95 95.6 sd 13.65
  (gates 70..85 / ≥10) — the r12 claim TO THE DIGIT; the lattice cert
  holds under the crossing.
- **B1 band strip**: view-left proc [120:500]×[372:392] med 56.4 **p5
  51.4** (gate p5 ≥40, med 48..58) — the banked digit exactly; right
  [140:520] med 51.1 p5 44.3. The tone cert RODE the r13 per-side gear
  split unchanged, as the per-side-split-prices-zero law predicts.
- **F1 jacket**: proc med **68.0** vs ref 67.4 same-rect (Δ+0.6) —
  CONFIRMED to the digit.
- **HERO-VOID CLOSED-FLAG RE-DERIVED** (the r12 border-clip law, my own
  method): tmp-e3-maskprobe mask (same heroFor camera) + an INDEPENDENT
  border-flood (tmp-rev-r13-chainflag.py): **enclosed regions = 0** in
  the hero-rr proc mask; the proc body EXITS the 1024-frame border
  (right edge, rows y 524..661, 138 border px). The evaluator's
  0.742 m² @(−2.34, 1.98, −1.02) is an OPEN chain virtually closed
  across the frame cut — **measurement artifact CONFIRMED by a second
  method**; no geometry order can or should chase it.
- Evaluator void set: hero-rr 0.742 (artifact, above), toptilt 3.528
  (ref-matched projection class, certified) + **0.294** (the sprocket
  void — CONFIRMED SHRUNK from the certified 0.311, the r13 claim),
  top 0.002 sliver. front Δbot −0.703 @x 1.55 — the fleet-kit shoe read
  digits, unchanged since r7 (adjudication below).
- **§B5 stranded 6 / abutting 0 / dangling 0**: CONFIRMED, and the six
  boxes decompose exactly into the r13 packet's classes — driver
  periscopes (−0.68..−0.29 × 2.00..2.03 × 1.72..1.85), merged hull
  (±1.42, 50%), merged hullDark whole-bucket (±2.00, 30%), plus THREE
  F2b deck tint plates (y 2.01..2.11 hull-deck skin at ~+4 mm — hull
  furniture that must NOT yaw; casting-envelope AABB smear). The r12
  note's "3" vs today's 6 is instrument drift over the same bytes, as
  the packet documents. PASS (adjudicated false-positive classes).
- standard-check exit 0 on my run — no discipline flags (npm 166 is the
  packet's claim, not re-run here; outside the critic rig set).

## §B standing checks

- **§B1 FRONT SLOPES + NO-STAIRCASES (fresh law, adjudicated on the
  render per the brief)**: the flank AMAP courses are REAL plate
  courses — co-planar panels with engraved seams and staggered r12
  organics, NO step offsets at the joints: **PASS on the jacket**. The
  BOW is the violation: at close-front/frontleft the beak nose reads as
  3-4 stacked horizontal courses with discrete silhouette step-offsets
  where the ref carries ONE dominant wedge + a single fender line —
  evaluator re-measures the parked digits exactly (shelf run 176.9° vs
  ref 163.6°, **Δ+13.3°** @z 2.67..3.04 y 1.88..1.91; nose corner
  **Δ+12°** hero-fl; close-front worst **Δ−14.4°/+14.5°**; hero-fl
  lower-front **Δ+15°**). Under NO-STAIRCASES this is now a NAMED
  failing read — but the geometry is pinned (r7 body-span dAlong law:
  the 1.005 toe try flipped dAlong and smeared every side row; whole
  90.7 binder; dims 99.5 thin). E1's three engraved courses are
  delivered and visible at 1× — shading has now been spent twice on
  this class and cannot move it further. **Owner ruling required**
  (the question below). Same for the turret CHEEK VERTICALITY (front
  cheeks read near-vertical vs the ref's raked faces) and the plan-toe
  flat (top Δ−10.5° @x 1.09..1.50 z 3.77) and fore-roof line
  (Δ−6.9..−7.4°, frontright/close-roof).
- **§B2**: PASS — digits above; residual blobs are label glyphs + the
  documented rear 9 px hairline class, sub-visible at 1×.
- **§B3**: census mg1+4d machine-PASS. Weapon-read: the RWS now carries
  the roof — tub + head + OPTIC GLASS + pedestal + (r12) the barrel/
  muzzle-ring dark hardware left of the optic + arm shadow: at 2× the
  head parses as a WEAPON (at 6× the barrel articulation is coarse —
  refinement tier, not a miss; the done-gate read is met). The stowed
  MAG remains marginal from the ordered close-roof window: a pale
  pintle post + partial receiver mass at the wing edge (the roof plane
  occludes the block from this camera); the §C allowance is spent,
  turret 91.8 unmoved — as-ordered, cited for the LAST polish notch.
- **§B4**: 0/0 — see the claims audit. The close-front and hero/quarter
  undercut windows now READ contained: pads wrap the idler with the
  plank step above (the wrap crests into the open like the real fender
  line); the rear undercut corridor shows track + wheels instead of a
  camo wedge crossed by shoes. The r9 "dip ledge" hold is DEAD in the
  renders, not just the audit.
- **§B5**: PASS (boxes verified above).
- **§B6**: PASS — \________/ reads both sides; both end wheels raised,
  ramps at both ends; gear geometry byte-stable this round (gate row
  equality ×2 + side dAlong 0 confirm).

## §H.4 VARIANT VARIETY (three graduates, fresh strips
shots/critic-leo2_revolution/crops/h4r13-rev-vs-{a5,a6,kf51}.png)

- vs **leo2a5 (bc9bad30)**: PASS — a5: scalloped skirts + seven-wheel
  row, raked cheek faces + mantlet dead-front, slatted bustle rack +
  LOWER-wall cable-X + Y-508 markings; rev: closed jacket cliffs,
  corner lattice cards, pale lattice tail band with the X ON it, RWS
  tub deck. Both carry an X — different zone, different hardware, no
  re-badge read.
- vs **leo2a6 (80b76338)**: PASS — a6: seven-wheel row, proud blister +
  L/55 mid-step, louvre grille + V splash board; rev as above.
- vs **kf51 (3ae9b70c)**: PASS — different silhouette family (hex-arch
  skirts, muzzle brake, SEOSS tower, black ERA pads).
- Distinctiveness survived rounds 2-3; every tell is geometric and
  held byte-frozen.

## Per-view justifications (bar ≥9.0; r9 score in parens)

- **front 8.3** (7.9): A2b delivered — the RWS head parses barrel-side
  dark hardware + ring + optic at 2×; D1b delivered — pale grid bars
  READ on the left corner card (clean lattice at 4×+) and a grid post
  at the right (the 47 mm rail window, priced); B1 towers, D3 slit,
  boards all hold. Held by: **cheek verticality + bow strata (parked
  §B1 classes)**, MAG sub-read, right-grid width bound.
- **frontleft 8.2** (7.9): E1b third course visible at 1× — the beak
  now carries three varied plate lines; F2b organics read on the
  flank; idler containment clean. Held by: **bow shelf Δ+13.3° +
  jacket nose Δ+12° (parked)**, plan-edge serration.
- **left 8.3** (8.1): band certs to the digit (51.4/56.4); jacket
  Δ+0.6; containment; hem line cleaner post-split. Held by: **the
  wheel-row window** — TWO hub discs read vs the ref's SEVEN pale
  rims (the strongest side foreignness; the r9-flagged material-only
  pale-hub-dot candidate was §B4-queued but r13 was containment-only —
  the window is now UNOWNED), dead-straight hem (parked), no evacuator
  (priced).
- **rearleft 8.4** (8.0): the r9 dip-ledge hold is DEAD — the undercut
  corridor shows track + wheels; the X reads from the quarter; panel
  organics landed. Held by: panel planarity at magnification, hem.
- **rear 8.5** (7.8): **D2b DELIVERED — the cable-X reads at 1×** (and
  cleanly at 3×: two dark runs crossing corner-to-corner with the
  shackle blocks ON the lines); D1 window cert holds under it
  (77.3/12.31); clusters + shackles + flaps read; D3 hairline only.
  Held by: the X rides the lattice band (y 1.19..1.66) where the
  ref's X crosses the LOWER wall (~y 0.75..1.15) — option B exactly
  as ordered, so a parity nuance, not a miss; turret rear busier than
  ref's bustle-rack read.
- **rearright 8.4** (8.0): as rearleft + the corner lattice reads
  edge-on; sprocket containment clean at 4×.
- **right 8.3** (8.1): mirror of left; band 44.3/51.1 inside gates.
- **frontright 8.2** (7.9): mirror of frontleft (Δ+11.8° class);
  fore-roof Δ−7.4° carried (parked).
- **top 8.5** (8.0): C1b delivered — the fans read as CHORDED
  slat-screen arches (wagon-wheel dead; hinge plates read); C2b
  delivered — a dark cable line reads across the deck at 1× (raw
  shadow-clone, Δ11-23 under deck med); r13 toe tongues closed the
  ±1.05 slots at the digit (122). Held by: fan r 0.36 size bound
  (~65% of ref arches — certified deck-carry), **plan toe flat
  (parked)**, tab serration runs, draped-cable coverage bounded by
  the riser law.
- **hero-frontleft 8.1 — FLOOR** (7.8): quilt + staggered seams +
  courses + containment carry it above the r9 read. Held by: **bow
  strata at hero magnification (worst Δ+15°) + one-plane flank
  residue + nose corner Δ+12° — all parked geometry**; this view is
  now almost purely a parked-class read.
- **hero-rearright 8.4** (7.8): E3 resolved BOTH ways — my independent
  re-derivation confirms the 0.742 m² is a border-clip artifact
  (closed-flag law applied: zero enclosed regions), AND the real
  E3a/E3b fills close the visible under-wing slot; undercut
  containment + X + lattice + clusters read. Held by: largest flats
  at hero magnification, fan style.
- **hero-toptilt 8.5** (8.2): chorded fans + clusters + organics +
  tint plates; sprocket void measurably shrunk (0.311 → 0.294).
  Held by: camo blobs finer than the ref's broad flow (carried
  class), fan style/size.
- **close-front 8.2** (7.7): the r12 cap on this view is RELEASED —
  containment READS at maximum magnification (pads wrap the idler,
  plank step above, wrap crests into the open); E1 courses visible on
  the beak faces; RWS relief present. Held by: **THE BOW STRATA — at
  this magnification the 3-4 stepped courses vs the ref's single
  dominant wedge are the dominant read (now a named §B1 NO-STAIRCASES
  class)**, clamp-jaw pale plank, cheek verticality behind it.
- **close-roof 8.4** (8.0): F2b delivered and READS — staggered seam
  segments, varied louvres, rivet lines, two tint plates break the
  last big flats; RWS full parse (tub/optic/arm/pedestal); hatch
  rings round. Held by: MAG weapon-legibility from this camera (post
  + partial mass), fore-roof line Δ−6.9° (parked), residual
  print-grain gap.

## Residuals certified this round (no new orders)

- hero-rr 0.742 border-clip chain — artifact, double-derived (r12
  probe-box + my flood/closed-flag). Stands until the harness frames
  per-model overflow.
- front −1.010 column / Δbot −0.703 @x 1.55 — fleet-kit shoe read
  (trackShoeGeometries inner pin caps), priced inside the passing
  90.7 since r7; moving it is kit surgery outside this tank's lane.
- rear 9 px flood hairline; toptilt 3.528 + 0.294 m² certified
  classes; whip-stub bistability; muzzle cover 0.56 / st11-12 /
  pod-line gate-priced carries — all unchanged, gate row equality ×2
  is the proof.
- X-on-lattice vs ref X-on-lower-wall: option-B nuance (done-gate
  met). An option-A re-lay to y 0.75..1.15 is legal polish IF it
  re-prices to zero (gate ×2 + dims held) — optional, not ordered.

## ROUND-3 DISPOSITION — the owner pricing question (explicit)

The deliverable order book is EMPTY in the lanes this program owns:
all nine r12 orders and the r13 containment kill are delivered and
verified; shading has been spent twice on the bow (E1, E1b) and once
on every other parked window. **Every remaining sub-9.0 hold is
dominated by the four ledger-parked geometry classes** (bow
strata/wedge, cheek verticality, plan toe, hem+wheel-row), all
measured at the same digits since r7 and all pinned by owner-law-level
constraints (whole 90.7 binder, dims 99.5 thin, r7 body-span dAlong
law). Under the fresh §B1 NO-STAIRCASES law the bow class is now a
NAMED failing read on every tank — the law and the ledger park are in
direct tension on this vehicle.

**THE QUESTION FOR THE OWNER** (verbatim, per the r9 flag):
"leo2_revolution's last mile is geometry the gate currently prices:
do you (A) AUTHORIZE a bow/cheek re-plane round — re-author the beak
as the ref's single dominant wedge (inside the certified toe band per
the dAlong law) and rake the turret cheek faces, accepting gate
movement inside the 90.0 floor (whole 90.7 → risk ~90.0-90.5, dims
99.5 must hold, full re-gate ×2 + re-critic) — or (B) CERTIFY
bow-strata / cheek-verticality / plan-toe / hem as priced identity
deltas, capping this tank at ~8.5 ceiling (no §G graduation under the
current dual gate) — or (C) schedule an orchestrator ORACLE-WARP
batch (batch-37 precedent) moving the print toward the proc planes —
noting my r7 measurement found the ref rakes are real-vehicle truth,
so (C) is the weakest option here?"

If (A) is granted, the round-4 order book is: A-1 bow wedge re-plane
(one raked surface glacis-to-toe + single fender line, §B1-conformant,
toe band 0.90..0.965 held); A-2 cheek rake (front faces to the ref's
leading-edge angle class); A-3 the top-view toe angle rides A-1 free;
plus the two non-parked polish items that survive any ruling:
P-1 WHEEL-ROW READ (left/right): the r9-flagged MATERIAL-ONLY
pale-hub-dot treatment on the visible wheel faces (two-point-measure
against the band first; B1 strip gates 51.4/44.3 must hold) — worth
~+0.2 on both side views; P-2 MAG top-cover pale plate on the
camera-facing face (tone-only, close-roof window). Neither P item
alone moves any view to 9.0.

## Verdict

FAIL — floor 8.1 (hero-frontleft), ceiling 8.5, mean ≈8.3, +0.4 over
r9 across the board: rounds 2-3 delivered everything they claimed
(clean claims audit, several certs to the digit, the §B4 kill
verified 0/0 three ways, the hero-void law applied and independently
confirmed). The projection gap vs r9's ~8.5-8.8 floor estimate is
exactly the parked-geometry share of every view's hold: the program
has run out of deliverable orders on this tank until the owner rules
on the parked classes. Round 3 of the projected 3-4 — the schedule
holds ONLY via ruling (A) or (C); ruling (B) ends the ladder at a
certified ~8.5.
