# m26_pershing — INDEPENDENT GRADUATION ADJUDICATION (2026-08-05)

Graduation critic verdict for the program's 26th candidate: the r3
post-warp re-anchor (74.8 → 90.4, the print's first-ever gate pass) on
the batch-42 warped print, candidate hash **f348ecd5**. Adversarial
adjudication per the §J critic-lane laws; chieftain5-anchor severity;
the m46/m47 graduation history is the family calibration set (same
T26-family rig, same builder lane, both graduated at 9.0 ×14).

## HEADLINE: **GRADUATION FAIL floor 8.8** — the geometric gate holds
## (90.4 PASS ×2 EXACT, every standing check green, identity never in
## doubt in any view) but the visual gate does not: the turret ships the
## SLAB loft (patton.js `t26Cast` — `T.loft` carries no `smooth: true`),
## and the per-panel flat-normal facet patchwork reads on every
## casting-dominant view at the exact severity the m46-r7 critic priced
## it (8.8–8.9) before m46/m47 each landed the smooth roll to graduate.
## Two orders, both family-proven and silhouette-identical/mask-priced;
## no re-author, no warp, no dims motion. This is a retune round, not a
## rebuild.

## Provenance (§D discipline — everything below measured by this critic)

- State: clean-tree HEAD ec90f18 (only foreign WIP: ledger.json refresh
  + two untracked shoeaudit html — neither touches m26). patton.js is
  the committed r3 landing (6a78b6c), byte-untouched by me.
- Hash bracket (`tmp-hashgeo`, BEFORE and AFTER all renders):
  **m26_pershing f348ecd5 (43 meshes / 69796 verts)** both runs.
  Frozen sibs byte-exact both runs at the 9bf2a6d mass-re-freeze
  registry: m46 90ebf864, m47 53b6123a, m60a1 fbf9f4cc, m60a3 051c454c.
- GATE ×2 (official `geometry-gate.mjs`, runs consecutive): **min 90.4 |
  hull 93.1 whole 90.4 turret 94.2 stations 90.6 dims 100 floaters 100
  PASS — both lines EXACT**, = the packet claim to the decimal. Dims
  rows measured: heightM 3.09/3.08 (+0.36%), hullLengthM 6.34 (+0.19%),
  overallLengthM 8.66 (+0.12%), widthM 3.51 (+0.05%).
- `track-clip-audit --exact`: **band front 0 / rear 0 AND shoe front 0 /
  rear 0**, no blind spot (the m1a1ha class clean).
- `turret-parent-audit`: **stranded 0 / abutting 0 / dangling 0.**
- `tank-standard-check`: gateMin 90.4, clip 0/0 ✓, **contig 0 ✓, decor
  mg1+0d ✓** (stowed FITTINGS 'mag' carries the census; the measured
  m2Station stays the gate-driven roof gun — the §I justification is in
  the packet and holds).
- `visual-evaluator` (camoSeed 4242): **RIG PARITY OK — max yawProxy
  0.9° @frontright, max |dCentroid| 0.039 m, no flips** → scoring valid.
  Report + overlays: shots/visual-eval-m26_pershing/ (my run).
- Pairs rendered FRESH by this critic on the official path
  (tmp-b1b3-critic-batch.mjs, ONE FIFO ticket batching
  m26+m45+m46+m47): 14/14 pairs, zero console errors. Scored ONLY
  shots/critic-m26_pershing/*.png. Prior sibling critic dirs archived
  to critic scratchpad before the batch.
- §J yaw-pair at the verdict hash: my own rest/yaw-90 14-view sets
  (tmp-b5-shots → shots/critic-m26-gradyaw/{rest,yaw90}). At yaw 90 the
  casting, M2 + cradle + cans, rack, cupola, loader ring, vent, stow
  roll, antenna pot and pods all rotate with the ring; hull hoods, caps,
  grille, lights, shackles, flaps stay hull-side; nothing sweeps mid-air,
  nothing drags the deck. Floaters 100 ×2 in the gate covers pose
  separation.

## Standing-check verifications (the brief's hunt list)

- **§B1 bow (certified print-class near-vertical glacis)**: HOLDS as
  certified — the bow reads ONE raked face (knee → toe cliff) with real
  furniture carrying the silhouette: fender platforms, paired tow eyes
  with cross-pins, the left clevis tab, front flaps, lights + guards.
  No staircase anywhere on the re-anchored lofts (deck steps follow the
  ref's own measured courses; rear ramp falls through the ref's own
  step stations). Slope-motivates-mass reads: the glacis wings fold
  into the fenders on the rake's own lines. I did NOT order a true-46°
  bow — the packet's pre-warp-extract proof (same cliff in the
  un-warped print) is verified reasoning and the certified cost
  (+0.37 ×12 plan-front + 4 side cols) is correctly priced as
  unreachable against this print.
- **§B3 m3 brake flank boxes (adjudicated double-baffle windows)**: the
  read HOLDS — the gunDark inserts sit INSIDE the brake body silhouette
  (x ±0.16..0.22 vs body half-width 0.24, between the two proud baffle
  rings): they read as the brake's dark side windows, not floating
  cuboids. Side-ortho A/B: proc drum + rings + window band vs the ref's
  notched wedge — same device family, muzzle-collar residual 0.063 ×1
  col certified (verified in my gate JSON). Polish headroom only: the
  window contrast is weaker than the ref's notch (the m46 C5
  baffle-slot-bar lane fits if a later round wants it). No mystery-box
  violation found ANYWHERE at 1×–6×: cheek pods read as flush casting
  steps with bolt-dot faces (attached, no hover shadow), tail tiers as
  the duckbill/pintle stack, bowTabs as the final-drive shelf, hoods/
  caps/eyes/shackles/siren all identifiable.
- **M2 two-band split**: verified on the pairs — cover step (3.105)
  riding the receiver band (3.06) over the ~3.03 barrel line, receiver
  reads as MASS (MG PHYSICS pale two-tone, sky-backed), cradle + twin
  cans + dark pintle post attached (the close-front "floating bar" at
  1× resolves at 4× as the attached cradle arm). The ref's own receiver
  spikes at z −1.83/−1.93 are covered; heightM carrier verified in dims.
- **Fender hanger bumps**: present at the ref's own wide slices —
  stations alternate wPct 0/0.21/0.44 across i0–i13 in my gate run (no
  full-width smear; the ≥10 mm boundary law respected — no 4.64-class
  spike on i4/i8/i10).
- **§H.4 distinctness (fresh four-up, proc pixels)**: m26 = plain-olive
  scheme + double-baffle brake + bustle-rear M2 with the barrel forward
  over the crest + bow fender PLATFORMS + tow eyes + clevis tab + 5
  return rollers + rear tension idler + duckbill tail tiers + NO fender
  mufflers. m45 = stub-muzzle short 90 mm, front-left roof M2, single
  tab bow, narrow tail. m46 = camo scheme, fat evacuator +
  single-baffle drum (dark slot bars), fender mufflers, forward M2
  station. m47 = m36 blast deflector, needle-nose turret + long bustle,
  AA pedestal cluster, red-band wheels. No 'same tank re-badged' read
  at a garage glance; the tells do not converge.
- **§B6 track run**: \________/ verified both ends — raised idler
  (wrap under the 1.008 platform floor) and raised toothed sprocket
  with the tension idler pressing the shallow ramp start; ground run is
  the short trapezoid base. Bow/stern wraps genuinely open to air;
  flaps clear of the shoes (the pale-gray flap tone is the certified
  darkGearFit family bucket).

## §B2 flood (maxch ≤13 AND B−R ≥ +8; §J label band excluded; my tool
## tools/tmp-m26grad-flood.py on my pairs)

REF halves: 0 px except its own wheel-pocket classes (view-left 427 px,
view-right 435 px, close-roof 277 px, close-front 3 px). PROC:

| view | px | adjudication |
|---|---|---|
| front | 536 | 2×210 px mirrored under-sponson corridors @\|x\|≈0.92–0.94, y 0.62–0.87 (real air between belly wall and track inner face, below sponson floor, above wheel tops — undercarriage sightline, not hull interior) + 87/26 px M2-underside daylight (pintle-gun class) |
| frontleft / frontright | 97 / 89 | M2-underside + rear-wheel pocket slivers |
| left / right | 119 / 142 | 102/109 px = the EMPTY STOWAGE RACK side window (z −2.0..−2.3, rails + posts, attached both ends — the sepv2 rack-gap class; ref reads solid only because its rack is loaded); rest = wheel pockets (ref's own class is 3× larger) |
| rearleft / rearright | 41 / 45 | sprocket-wrap pockets |
| rear | 213 | the front-view corridors seen from astern |
| top | 8 | tail-notch pair (plan taper vs fender ends) |
| hero-fl / hero-rr / toptilt | 140 / 155 / 36 | under-belly + wheel-gap + rack-window at perspective (m46-graduation-certified blue-signature outside-silhouette air classes) |
| close-front | 58 | idler-wrap-to-flap daylight (containment air, §B4-legal) |
| close-roof | 1334 | dual-wheel gaps under the sponson (4×~200 px; ref carries 277 px of the same class) + 2×~180 px M2-underside band |

**No through-hull, through-turret, or enclosed-interior sky in any
view.** The evaluator's hero "enclosed-void" hits decode to the same
under-belly classes (centroid y −0.56 = below-hull sightline).

## Per-view scores (all fourteen, graduation bar ≥9.0 every view,
## chieftain5-anchor severity, m46-r7/r10 family calibration)

| view | score | named reads |
|---|---|---|
| view-front | **8.9** | HELD by the facet family head-on: recessed shield face + hard cheek creases read as a welded wedge vs the ref's rolled casting (evaluator: shield crown edge Δ−14.8° class on the overlay); floods legal; certified M2 crown-strip poke = +0.239 ×1 col at x −0.06 verified; bow plate + MG square + eyes + guards read. |
| view-frontleft | **8.9** | Facet family at quarter pitch (the m46-r7 8.9 class verbatim); fender-line flatness Δ+8.6° ×0.81 m @z 0.69..1.58 (evaluator, priced inside hull 93.1); platforms/eyes/flap/lights read; M2 daylight legal. |
| view-left | **9.0** | Side ortho flattens the facet read; §B6 trapezoid + tension idler + 5 rollers + duckbills + rack; M2 two-band split verified; certified turret_side residuals at their stations (basket −1.02 = 0.063, mantlet rotor-lane trade); rack window honest. |
| view-rearleft | **8.9** | Bustle-flank facets + the plain ramp band; tail tiers + duckbills + contained corner; rack rails read attached. |
| view-rear | **8.9** | Plainer rear face than the print (no pintle-bracket tell, no corner X-braces) over honest structure (bolt rows, grille, transmission shelf, tail stack); ramp band flat; floods legal. |
| view-rearright | **8.9** | Mirror of rearleft; flap = darkGearFit bucket (family recipe); sprocket wrap honest. |
| view-right | **9.0** | As view-left mirrored; shelf pods read flush casting steps; antenna pot at dome-rear right. |
| view-frontright | **8.9** | Mirror of frontleft; same facet + fender-line classes; evaluator Δtop +0.724 @z −1.14 is the labeled M2-cliff offset class, not a height error. |
| view-top | **8.9** | HELD by the deck-slat crown gap: ref's transverse slat ARRAYS + fender cleat runs vs proc's two ribs + one under-quantum grille bay (the m46-r7 view-top holder; its r10 R5 delivered slat crowns AT ref stations to clear it). Plan registration 0.1°; tail ladder + taper + brake read; 8 px flood. |
| hero-frontleft | **8.9** | Facet family at hero pitch; slope-motivates-mass reads on the bow; furniture-rich glacis; floods legal. |
| hero-rearright | **8.9** | Facet family + plain ramp/deck at hero pitch; rack window honest; tail contained; ref's dome-shoulder arc (r 0.213) unmatched by the proc panels. |
| hero-toptilt | **8.8** | JOINT FLOOR — the deck texture-density gap is loudest here and pairs with the roof facet patchwork (exactly the m46-r7 toptilt-8.8 pairing); decks FILLED, cupola/hatch circles read, no voids under perspective. |
| close-front | **9.0** | The bow money view holds the bar: one-rake glacis + platforms + pinned eyes + clevis + flap + guards + light cluster; brake drum + rings + window band; M2 mass with attached cradle; only the shield flat face betrays the loft at crop rear. |
| close-roof | **8.8** | JOINT FLOOR — the definitive A/B: ref shades the casting as ONE cast roll with the grouser grid; proc is a flat-quad patchwork (front face, 2–3 cheek planes, shoulder step, crown) with hard-edged (attached, legible) trays — the m47-r4 "dome panel-seam rectangles" / m46-r7 "facet-patchwork vs continuous cast roll" read, amplified by the parity-true plain-olive scheme (no dapple to break the planes); M2 cluster + cupola + pods individually legible; floods legal. |

Floor **8.8** (close-roof, hero-toptilt), mean **8.91**. Identity: same
M26, same stations, every tell present, in EVERY view — the misses are
tier reads (shading continuity + deck texture density), not vehicle
reads.

## Why FAIL and not pass-with-residuals (calibration record)

The m46-r7 critic priced this exact slab-loft facet family at 8.8–8.9
across quarters/heroes/closes WITH camo dapple softening it; both
graduated siblings shipped the fix before graduating (m47 r6-B8, m46
r9/r10 R4 — "facet family GONE", every view 9.0). The m26 presents the
same geometry class NAKED (plain-olive parity scheme) — passing it at
9.0 would be a family calibration regression on the loudest surface of
the vehicle. §D numbers corroborate: the ref fits smooth arcs
(sub-pixel sagitta) the proc contours never fit (proc arcs 0 in 10 of
11 arc-bearing views; the one proc arc is its own round cupola), and
the evaluator's biggest non-cliff edge deltas ride the shield crown
(−14.8°) and cheek panels. The fix is one opt-in param, proven
silhouette-identical in-gate ×2 on this exact rig family.

## ORDERS (patton lane, one retune round — no re-author, no warp)

1. **`loft.smooth: true` on the m26 turret** (m26_pershing entry,
   `turret.loft`): smoothLoft re-emits the SAME ring corners as one
   indexed grid (shared-vertex computeVertexNormals) so the casting
   shades as one roll — silhouette-identical by construction (m46 R4 /
   m47 B8 lineage, verified in-gate ×2 on both). Expect gate lines to
   hold ~bit-identical; hash WILL move (new vertex layout) → new
   candidate hash, fresh 14-view re-cert (the casting shows in every
   view).
2. **Deck-slat crowns at the ref's own array stations** (the m46 r10 R5
   in-profile recipe): pale crest rows on the engine-deck bays at the
   ref's own stations and pitch (m46 delivered ~0.199 m pitch,
   dash-grammar, proc crest peaks in the ref's own luma band), heights
   inside the side-column p95 budget (≤4 columns law) and station-top
   discipline. Kills the view-top/hero-toptilt density gap. Keep the
   1.5781 trace quantum honest — crowns are allowed to READ without
   moving masks (m46 proved the mechanism; gate ×2 must hold).
3. OPTIONAL same-landing polish (headroom, not blocking): (a)
   rear-plate pintle bracket + corner X-brace tells within the
   certified silhouette (rear/rearleft/rearright 8.9s carry it); (b)
   brake side-window contrast bars (m46 C5 lane: dark bars ≤3 mm proud
   inside the certified plan band).

Re-round protocol: builder lands 1+2 (+3 if taken) → gate ×2 + battery
+ packet section → fresh independent 14-view critic at the NEW hash →
graduation §10 on PASS. Per the m46 r10 precedent these two orders
clear all ten sub-9.0 views; nothing else in this adjudication is
blocking.

## Honest residuals (verified priced — each re-measured in MY gate run)

- side_whole @4.34: tier3 under-band (proc bot −0.72 vs ref −0.43, err
  0.142) — the law-1 body-filter tail price; @−2.0: clevis-tab depth
  (err 0.081–0.085) — the law-1 counterweight price; @−4.34:
  muzzle-collar band 0.063. All three = the packet's certified list.
- turret_side @−1.02 basket/casting 0.063; turret_plan @0.83 rotor-lane
  trade 0.119 ×1 col (the zF 0.89 plan-vs-side certified trade).
- front_whole @−0.06: the fixed-width M2 crown strip pokes ONE column
  +0.239 (packet: +0.24 — exact).
- stations i1 topPct 1.81 (rear-ramp window) — unchanged, and its
  visual cousin is order 2's deck lane.
- Certified print-class bow (near-vertical glacis) — carries; not an
  order (owner-law oracle-matching, packet-certified with measured
  true-rake cost).
- Empty-rack side windows + undercarriage corridors — honest open
  structure (flood table above); no fill order (§B2 legal classes; the
  ref's own pockets are larger).
- Ref M2 canted vs proc level barrel — sub-column, inside turret_side
  94.2 (M2-STATION TWO-BAND law seating verified).

## Law-bank notes (for BUILD-STANDARD, orchestrator lane)

1. **PLAIN-SCHEME FACET AMPLIFICATION** (proposed §H.2 corollary): a
   slab-loft facet family that scored 8.9 under camo dapple reads a
   full step louder on a parity-true plain-olive variant — plain-scheme
   family members should take `loft.smooth` (or equivalent shared-vertex
   normals) FROM BIRTH; camo is not a structural softener the family
   rig may rely on.
2. **CLOSE-VIEW EVALUATOR WORLD-DECODE artifact** (§D adjudication
   note): close-crop findings can print world spans beyond the model's
   own extents (here close-front "y 3.00..3.57" on a 3.105-top build —
   no geometry exists there in either model); adjudicate close-view
   digest lines on the overlay pixels, never on the printed world
   coords alone (BURIED-FURNITURE corollary cousin).

## Verdict

**GRADUATION FAIL floor 8.8** at f348ecd5 — dual gate NOT met (geometry
90.4 PASS ×2; visual floor 8.8 < 9.0 on close-roof + hero-toptilt, with
eight further views at 8.9). Deliver orders 1–2 (patton lane, single
retune round, both family-proven); re-gate ×2, re-hash, fresh
independent 14-view re-adjudication at the new candidate hash, THEN §10.
The geometry work of r3 is certified sound — every standing check green,
every certified residual honestly priced, identity beyond dispute. The
print's first gate pass survives intact; the casting just has to shade
like a casting. Independent critic: m26 graduation lane, 2026-08-05.
