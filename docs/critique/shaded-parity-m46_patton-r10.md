# m46_patton shaded-parity r10 — THIRD ADJUDICATION, GRADUATION CANDIDATE (2026-08-05, post-08c9cd4)

Rig: fresh 14 pairs `node tools/tmp-tank-critic.mjs --id=m46_patton` →
shots/critic-m46_patton/ (vite :7472, zero console errors, favicon-404
only) + `visual-evaluator --id=m46_patton` (exit 0, **RIG PARITY OK**, 11
ortho views, max dYawProxy 1.0° @frontleft, max |dCentroid| 0.061 m —
the r5/r7/r9 numbers EXACTLY, 0 flips; camoSeed 4242, evidence
shots/visual-eval-m46_patton/, prior round's evidence archived to
scratchpad per the §10 amendment).

BYTE-STABILITY BRACKET: m46 geometry hash **dfacd57c** (100 meshes /
90 354 verts — the r10 packet line exactly) verified BEFORE evidence and
AT CLOSE; HEAD **08c9cd4 the whole round** (no mid-round landings);
patton.js / kit.js / materials.js / tankFactory.js byte-clean vs HEAD
throughout. STRONGEST POSSIBLE RIG PROOF: my fresh pairs are
**PIXEL-IDENTICAL (diff bbox None)** to the r10 landing archive
shots/patton-r10/c3/ on every sampled view (close-front, hero-toptilt,
view-top, view-left) — two independent renders of dfacd57c agree to the
byte; the scored build IS the adjudicated commit. CONCURRENT-LANE LOG:
the only working-tree src drift is modern3.js (AFV lane, +147/−37,
different import graph — the pixel-identity above proves zero m46
effect). Family bracket: m47 **70941de0 FROZEN** (verified at open AND
close — the graduate never drifted), m26 **2621292c** / m45 **e103a2dc**
/ m60a1 **81e69e34** / m60a3 **efcde5c4** packet-exact, do-not-gate
respected.

Official gate re-run on my watch: **91.2 PASS ×2 bit-identical** (hull
91.9 / whole 91.8 / turret 91.2 / stations 92.5 / dims 100 / floaters
100) — reproduces the r10 packet line to the decimal; with the r10
landing's own ×2, that is four identical lines on this geometry.
`tank-standard-check`: clip **0/0 ✓**, contig **0 ✓**, decor **mg1+2d ✓**.
`track-clip-audit --exact` **0/0 ✓**. `turret-parent-audit` stranded
**0** / abutting 0 / dangling 0 ✓ — the r10 InstancedMesh crown emission
passes the audit clean on my watch (the merged-mesh stranded-1 hazard the
packet measured is confirmed absent in the landed state). Measurements:
banked scanners (tools/tmp-r7-merkava.py, tmp-m46r5crit-scan.py,
tmp-m47r4crit-crop.py diagnosis-only) + tools/tmp-m46r10crit-rows.py
(new, per-row crest profiler) on MY fresh pairs; every number below
derived this round on this rig.

## HEADLINE: **GRADUATION PASS — 9.0 EVERY VIEW** (floor 9.0, 14/14 at the bar); dual gate met at dfacd57c — the ladder 8.2 → 8.8 → 9.0 closes with the r7 order book EMPTY (R1–R4 verified delivered from r9, the R5 deck-slat hold delivered in-profile at r10), zero regressions anywhere, and every remaining read a banked/certified class that presents as a hair at 1×, not a tier break (the m47-r8/pt91m precedent)

front 9.0 · frontleft 9.0 · left 9.0 · rearleft 9.0 · rear 9.0 ·
rearright 9.0 · right 9.0 · frontright 9.0 · top 9.0 · hero-fl 9.0 ·
hero-rr 9.0 · toptilt 9.0 · close-front 9.0 · close-roof 9.0

IDENTITY never in doubt since r5, all tells present at 1×: proud round
T26 casting, 90 mm with fat evacuator + single-baffle muzzle drum (dark
baffle-slot bars read on the flanks), mid-roof M2 + tall AA pedestal at
the correct station, bow MG ball under brush guards + lights, fender
mufflers with strap rings, 6 road wheels + 5 return rollers, raised
idler AND raised sprocket with the TENSION IDLER reading as a distinct
pale-ringed wheel with a real wrap dip on BOTH flanks (3× crops,
left + right stern).

## r7-order delivery verification (every order re-measured on MY pairs)

- **R1 ALBEDO RE-BASELINE ✓ (condition confirmed dead)**: the committed
  ref state reads the archive class — A3 front track columns ref
  **62.8/62.8**, proc med **59.9/60.1** → Δ2.9/2.7 ≤ 5L (the >5L notch
  trigger never fires); A1 view-left gear band [60..580]×[358..427] proc
  sub-30 **0** (ref-class; r5 was 7481), p5 **54.1** (bar ≥35), med 66.6
  vs ref 63.2 (Δ3.4 ≤ 6L), sd 7.49 ≤ ref+4 (12.71), p95 76.0 ≤ ref+4
  (81.5); A2 wheel band p75 **72.6** (bar ≥66), p95 75.1 ≤ ref+4; A5
  hero-rr gear window sub-25 **0**, p5 41.8 / med 56.7 / sd 9.23 vs ref
  44.0 / 58.6 / 10.85 — in-class, highlight tail kept, no overshoot.
  N1 hue: proc gear r/g 0.996 vs own hull 1.010 → split **0.014** (bar
  ≤0.03; the ref's OWN split on the same windows is 0.045) — no tan.
- **R2 RACK-LOAD TEXTURE ✓**: the load reads two-tone at 1× — pale
  bleached over-wraps + olive mottle + near-black strap webbing + roll
  shadow lines — at rear, rearleft, top and hero-rr (crops); the r7
  uniform-cloth-slab read is dead.
- **R3 CURTAIN-LEG CONTINUITY ✓ BETTER THAN THE RECORD**: close-front
  curtain-core columns (y 378..396, proc x 20..400) med-range spread
  **4.5L** (done-gate ≤12; r9 claimed 7L), largest adjacent column step
  6.9L — one continuous graded band, no discrete dark verticals at 1× or
  2×. The quarters keep their pass.
- **R4 ROOF CAST CONTINUITY ✓**: close-roof at 1× and 2× shades the dome
  as ONE cast roll — the r7 facet-patchwork family is dead in close-roof
  AND both front quarters; crest pods stay hard-edged gate carriers
  (certified; front_whole rows held — gate ×2 bit-identical on my watch).
- **R5 DECK SLATS ✓ — THE r7 FLOOR-HOLDER DELIVERED (the r10 order,
  in-profile per the orchestrator ruling, usKit untouched — sibling
  hashes prove it)**: view-top crest scan (tmp-m46r10crit-rows.py,
  x 250..390): proc pale crest rows at y **187/188, 198/199, 212/213/214
  (+226)** vs ref rows y **185–188, 199/200, 212/213** — AT the ref's own
  stations at the ref's own ~13 px (0.199 m) pitch, 4-dash grammar with
  spine gap reproduced; proc crest peaks p75 **81–87** dead in the ref's
  81–97 band. Ordered windows: rear-deck band [260..380]×[110..170] med
  **58.8 vs ref 59.0 EXACT** (r7 read: 56.0 vs 61.9 — closed); grille
  zone [250..390]×[174..227] med **56.2 vs 60.5** (packet-exact);
  tracker [250..390]×[180..480] sub-50 proc **5234 vs ref 3190** (r10
  packet 5184 — 1% rig noise, its own r9 cross-rig delta was the same
  size). RESIDUAL-ATTRIBUTION VERIFIED by band: the excess is turret-roof
  y 260..300 (+966; packet +941), bow y 380..400 (+471 EXACT), grille
  zone +503 (includes dome-edge blotch px the deck lane cannot reach),
  and the ref is ITSELF darker at y 400..440 by −677 (EXACT) — the
  m47-r4 B3 camo-blotch MATERIALS class, not deck-grille. Dome-top tone
  is in-class (med 59.9 vs 59.3): the delta is blotch DISTRIBUTION
  (fewer/larger/harder vs the ref's finer mottle), a hair at 1×.

## Standing checks (§B + §H.4)

- **§B1 FRONT SLOPES / NO-STAIRCASES: PASS** — front view 29 matched
  edges, ONE flag >1.5° (Δ+9.7° @ x −0.96, y 2.23..2.68 = the certified
  pedestal-mast dims-carrier band, r5-exact); glacis rake, cheek slopes,
  dive line inside noise; p95 front Δtop 0.143 / Δbot 0.134 m (r5 class
  exact). Every slope reads as ONE raked surface at 1×: glacis clean,
  bow plate courses read as authored course lines (C1 ribs, co-planar),
  roof dome one cast roll (R4), track ramps tangent — no stepped
  quantization anywhere. Round T26 casting reads round; close-front
  cheek edge Δ−3.2° on a 3.40 m edge (certified casting class).
- **§B2 NO EMPTY AREAS: PASS** — machine scan 0 enclosed cells ✓; decks
  filled at top/toptilt. Void inventory re-adjudicated WITH
  blue-signature on MY renders: toptilt 4.193 m² @ (1.24, 1.29, 0.34) =
  **90.6% blue-signature** real sky in the barrel/cheek/bow projection
  triangle (the adjudicated r5 class, same coords); close-roof 0.066 m²
  @ (0.20, 3.08, −1.01) = **67.6%** blue-signature air inside the
  M2/pedestal cluster (the lawful MG-PHYSICS sky window — stays open);
  close-roof 0.025 m² toe class = **0% blue-signature** (warm shadow,
  NOT sky — the revolution-r7 law rejecting it correctly); hero-rr
  0.036/0.022 m² fender-overhang + under-belly = blue-signature
  outside-silhouette air at the r5 coordinates. No through-hull sky.
- **§B3 DECORATION: PASS** — roof M2 mandatory ✓ pale top-lit two-tone
  (B1 rod med **76.8**, ytop-med **223 = ref 223** — the r7 number to
  the decimal; close-roof cluster med 60.6 vs ref 57.5, stepped receiver
  + dapple + taper + collar read in the crop); census **mg1+2d** ✓
  (tow-cable coil on the rear plateau, spare links on the right shelf).
- **§B4 TRACK CONTAINMENT: PASS** — exact audit 0/0 reproduced; wraps
  clear of bow/stern solids at 3–4× both ends; two-layer shoe system
  (pale inner chain + dark shoes + guide horns) reads at close-front.
- **§B5 TURRET PARENTING: PASS** — audit 0/0/0 machine-clean on the
  LANDED state; the r10 crown set emits as ONE InstancedMesh (hull deck
  furniture, not turret content — no yaw-pair required; no re-parent
  this round); floaters 100 ×2 in the gate; rack/M2/pedestal turret
  parenting standing per r5 evidence, geometry hash-continuous since.
- **§B6 TRACK RUN: PASS** — \\________/ trapezoid both sides (raised
  idler + raised sprocket, tangent ramps); left/right profile p95 Δbot
  **0.080/0.080 m** (r10-exact); the TENSION IDLER identity read holds
  at 1× and is unmistakable at 3× on BOTH flanks: distinct painted wheel,
  pale end-rings, real wrap dip (crops from view-left/right sterns).
  Packet SIZE note (sprocket drum tension-idler-sized) carried.
- **§H.4 VARIANT DISTINCTNESS: PASS** — vs m47 **70941de0** (its own
  graduation pairs, structure only; hash re-verified on my watch this
  round): round short-bustle T26 casting vs needle-nose long turret;
  fat evacuator + single-baffle DRUM vs capsule deflector; mid-roof M2 +
  tall AA pedestal vs bustle-rail M2; tension idler + fender mufflers
  (m46-only); bow ball under brush guards vs glacis ball; and each mark
  carries its OWN dressed loadout (m46: canvas rack load + turret-flank
  spare links + rear cable coil + ribbed bow vs m47: tail-tray tarp +
  pioneer row + whip) — side-by-side proc flanks read as two different
  marks at a glance. vs m60a1/m60a3: different generation entirely
  (hashes packet-exact; standing structural verdict unchanged since r5).

## Per-view justifications (bar ≥9.0 "same vehicle, same tier", 1×)

- **view-front 9.0** — held its r7 bar: track pillars olive with link
  texture (A3 Δ2.9/2.7), M2 cluster pale two-tone, bow ribs + shackles +
  ball + guards read, turret paint in-class, curtain band continuous
  under both fenders. Residuals: certified mast-band flag (Δ+9.7°,
  dims-carrier) + casting-texture depth — the same texture-hair class
  m47 carried at 9.0 from its r6 on.
- **view-frontleft 9.0 — +0.1, the r7 holders are dead** — roof/cheek
  facet family GONE (R4 smooth roll reads in the quarter), curtain one
  graded band (R3), gear tone at the re-based target (A1 bars). What
  remains: certified plan-edge + gear classes at their digits — hairs.
- **view-left 9.0** — A1/A2/B1 all reproduce to the decimal (sub-30 0 /
  p5 54.1 / p75 72.6 / rod 76.8+223); evacuator, brake, muffler + strap
  rings, trapezoid, READING tension idler; graded under-fender band.
- **view-rearleft 9.0 — +0.1** — the two r7 holders delivered: load
  reads textured two-tone with strap rhythm (R2), curtain graded (R3);
  tail louvres + deck-edge crest dashes read. Residual: banked corner-
  vertical hints + the rectilinear-grammar trade — certified, hairs.
- **view-rear 9.0 — +0.1** — plate carries slat rhythm (my rowSD 7.16 vs
  ref 4.57 — rhythmic rows present, texture-plain read dead since r7);
  masts pale, rack loaded+textured, columns olive, deck-edge dash row
  new this round. Residuals ALL banked at their exact digits: corner
  verticals 88.9°/91.1° len 0.57 @ x ±1.68 (evaluator-exact
  reproduction, shared track-band face — the m47 graduated carrying its
  same class), procOnly 29 vs refOnly 9 rectilinear trade (r7-banked).
- **view-rearright 9.0 — +0.1** — mirror of rearleft; drums camo'd with
  dots, no starburst, louvres read; same banked rear-quarter set.
- **view-right 9.0** — mirror of left; '123' reads (visible at the
  turret flank in close-roof too); baffle-slot bars on the drum flank;
  spare-links fitting on the shelf wall; tension idler + pale rings ✓.
- **view-frontright 9.0 — +0.1** — as frontleft mirrored; facet family
  dead, curtain continuous; certified classes only.
- **view-top 9.0 — +0.1, the R5 tone+structure delivery** — plan
  registration excellent (yawProxy 0.1°); the rear deck now carries the
  ref's own crest-row grammar AT its stations (rows within ~1 px, peaks
  in-band, 4-dash + spine-gap segmentation), rear-deck med EXACT
  (58.8/59.0); rack load textured; muffler capsules + straps ✓.
  Residuals, all named and banked: aft-band louvre density (ref rows y
  103–156 p75 80–86 vs proc flat — tone-matched to 2L mean, rowSD 3.51
  vs 4.85; a density hair at plan pitch — the m47-r8 "mid-deck furniture
  density (hair)" class which graduated at 9.0), certified notched-rail
  plan-edge trade (ref continuous 5.81 m fender lines vs proc rails @
  ±1.68 — stations/whole carriers), turret planform polygon read
  (turret_plan certified, ONLY-REF sliver banked), dome-blotch
  distribution (materials class).
- **hero-frontleft 9.0 — +0.1** — the r7 holders (facets, curtain,
  split) all dead: dome one roll, band continuous, gear↔hull split
  0.014 (ref's own 0.045). Drums camo'd, no starburst, sponson graded.
  Certified plan/casting hairs remain.
- **hero-rearright 9.0 — +0.1, the r5 floor at +0.8 over the ladder** —
  sub-25 **0**, split 0.014, rack load textured, masts pale, tail
  louvres + crest dashes read at hero pitch; corner verticals at the
  shell edge = the banked class at hero pitch, reads as an edge hair.
- **hero-toptilt 9.0 — +0.2, THE r7 FLOOR DELIVERED** — the order's
  done-gate reads on the official rig: the mid-deck carries the ref's
  transverse slat rhythm at 1× (crest dash rows visible at hero pitch,
  verified in the same-window 2× crop), tone parity everywhere on the
  deck (rear-deck med exact), roof one cast roll, load textured, cable
  coil reads, projection triangle 90.6% real sky (adjudicated class).
  Residuals: the aft-band density gap (the "much reduced" banked class —
  ref's louvre field runs ~5 rows further aft; at 1× it reads as a
  quieter patch of the SAME deck grammar, no longer "the loudest single
  read on the vehicle") + dome-blotch distribution + certified classes.
  Same vehicle, same tier at 1×: yes — the pt91m/m47 precedent (banked,
  documented residuals with a same-tier read do not hold the bar) applies
  exactly.
- **close-front 9.0 — +0.1** — R3 delivered at its loudest view (4.5L
  spread, one band); drums painted with bolt dots, two-layer ground run,
  bow ribs + clevises, mantlet rotor + wings, baffle slot, M2 pale.
  Residual: casting-texture depth vs the ref's chevron-ring grammar —
  the certified band m47 graduated with at this same view.
- **close-roof 9.0 — +0.1** — R4 delivered where it was ordered: the
  dome shades as ONE cast roll at close pitch; receiver stepped +
  dappled (B1/B2 digits hold), rack loaded+textured, '123', cupola ring,
  crest dashes on the deck beyond. Residual: M2 group slab-stack mass
  (certified dims-carrier band) + casting texture — hairs at 1×.

## Builder-claims audit (§D; r9+r10 packets vs MY rig)

Every load-bearing claim reproduced or verified in kind: gate line ×2 to
the decimal (four identical lines including the landing's own); hashes
dfacd57c/70941de0/2621292c/e103a2dc/81e69e34/efcde5c4 all packet-exact;
clip/contig/parent zeros; A1 quadruple (0 / 54.1 / 66.6 / 7.49 / 76.0 —
r9-exact), A2 72.6 EXACT, A3 59.9/60.1 vs 62.8/62.8 EXACT, A5 0 + stats,
B1 76.8/223 EXACT, N1 split in-bar with my own windows (0.014); R5
rear-deck med 58.8/59.0 EXACT, grille med 56.2/60.5 EXACT, tracker
5234 vs packet 5184 (1% rig noise, the packet's own cross-rig delta),
residual-attribution bands +966/+471/−677 vs packet +941/+471/−677;
crest stations + pitch + peak band verified with my own scanner. TWO
in-kind-only items (no discipline flag): (1) the r10 crest-row mean
quadruple (73.2/72.7/… vs ref 74.1/73.3/…) is a dash-segment window I
did not relocate exactly — my station/band/med-exact measurements carry
the same claim; (2) "toptilt window 8594 → 8292" — window undefined in
the packet; my own toptilt verification (visual rhythm at 1× + view-top
structure) stands independent. The r10 packet's residual attribution and
honest-residuals sections are accurate and complete on my rig.

## BANKED RESIDUALS carried to the graduate record (no orders — none
holds the bar; listed for the §10 record and any future graduate-change
round): aft-band deck louvre density (ref rows z ≈ −2.3..−3.4 at the
same 0.199 m pitch, p75 80–86 vs proc flat — tone-matched; the natural
extension of cfg.deckSlats if a polish round is ever scheduled);
turret-roof/bow camo-blotch distribution (m47-r4 B3 materials-owner
lane); C4 corner verticals @ x ±1.68 (evaluator-exact 88.9°/91.1° len
0.57, shared trackBandGeo face — family lane, m47 carries the same
class); plan-edge notched-rails vs continuous fender lip (certified
stations/whole carriers); M2 group slab-stack mass + pedestal head
(certified dims band); chopped rear-track print zone ~1 col @ z ≈ −4.17;
turret_plan ONLY-REF sliver x ≈ −1.09; front idler wrap z 1.64 (law-2
boundary); the five adjudicated void classes (projection/MG-window/toe/
overhang/under-belly — coordinates re-verified this round);
hullLengthM grid-phase watch (dims 100 ×2 on my watch).

## Verdict

**GRADUATION PASS — floor 9.0, EVERY view at the bar** (dual gate met:
geometry 91.2 PASS ×2 bit-identical at dfacd57c + critic ≥9.0 all 14
views, same commit state 08c9cd4, pixel-identity proven against the
landing archive). No machine gate broken anywhere: clip 0/0, contig 0,
parenting 0/0/0, trapezoid + reading tension idler, slopes clean with
NO-STAIRCASES honored, RIG PARITY OK, §H.4 distinct against both the
m47 graduate and the m60s, hashes byte-stable through a live
concurrent-lane window. The ladder closed exactly as the program's
record predicted: r5 diagnosed one asset-tier driver (8.2), r7 measured
it dead and named five small families (8.8), r9 delivered four of them
with zero razor spend, r10 delivered the last one in-profile — and the
deck now carries the reference's own louvre grammar, row for row, tone
for tone. Every surviving read is a banked or certified class that
presents as a hair at 1× — the identical acceptance basis on which m47
and pt91m graduated. **RECOMMENDATION: graduate m46_patton — the
program's 23rd, the patton family's SECOND. Orchestrator executes §10
at dfacd57c**: retire the registration into the THREE override maps
(procedural-fidelity LOCAL_REFERENCE_OVERRIDES, tmp-tank-critic
CRITIC_REFERENCE_OVERRIDES, visual-evaluator-page
CRITIC_REFERENCE_OVERRIDES — the committed one), hash-freeze
**dfacd57c**, and add m46_patton to the graduate/do-not-gate roster in
the same commit. Any geometry edit after this verdict invalidates it.
