# leo2_revolution §B5 DE-FUSION graduate-change RE-CERT (independent critic, 2026-08-05)

Full 14-view re-certification of the COUPLED working-tree state (oracle
vlo-bake drop applied to the recovered print + staged proc re-lay in
src/vehicles/profiles/leopard.js, per packet §B5-r16 LANDING ORDER). I am
adversarial to the builder's claims; every number below is my own run.

## Provenance (all commands mine, this session)

- Tree: certification ran on HEAD a6c01ed + the coupled dirty set
  (M leo2_revolution.glb, M leopard.js, M repair_oracles.py batch-41
  chain-EXTEND `.append` — exact tmp-leo-defuse-mkglb literal,
  M docs/geometry-gate/leo2_revolution.json). Source audit: every
  leopard.js hunk lives inside buildLeo2Revolution(P) (documented lane);
  batch-41 never flat-assigns (§E law held).
- MID-SESSION LANDING: the orchestrator committed the identical coupled
  set as **c9ddba0** (six files: repair + GLB + leopard.js + gate JSON +
  ledger + packet) at 17:47 while this re-cert was in flight, followed by
  the takeover sweep decec28. POST-LANDING RE-VERIFICATION on the
  committed tree (working == HEAD): `tmp-hashgeo` → leo2_revolution
  **1993cfb1 (82/110365)** + all three frozen sibs exact, and
  `geometry-gate` → **88.9 | 88.9/90.4/91.8/91.2/99.5/100** — byte/line
  identical to the certified state (my third identical gate run). The
  committed bytes ARE the certified bytes; this verdict covers c9ddba0.
- Byte discipline, `node tools/tmp-hashgeo.mjs` ×2 BRACKETING all renders:
  leo2_revolution **1993cfb1 (82 meshes / 110365 verts)** both bookends —
  exactly the packet's re-freeze candidate. Frozen siblings byte-identical
  both bookends: leo2a5 **bc9bad30**, leo2a6 **80b76338**, kf51
  **3ae9b70c**.
- Official gate, `node tools/geometry-gate.mjs --ids=leo2_revolution` ×2:
  **min 88.9 | hull 88.9 / whole 90.4 / turret 91.8 / stations 91.2 /
  dims 99.5 / floaters 100 — BIT-IDENTICAL ×2** (gate JSON cmp-clean
  between runs), reproducing the orchestrator's ×3 coupled line EXACTLY.
  Floaters 100 in both runs. The sub-90 hull row is the ORCHESTRATOR-
  ADJUDICATED de-bake debt (the old 90.7 measured the vlo-polluted mask);
  per the round brief it is priced with a retune round queued and is NOT
  subject to this visual verdict.
- Renders: fresh 14 shaded pairs `node tools/tmp-tank-critic.mjs
  --id=leo2_revolution` → shots/critic-leo2_revolution/ (zero console
  errors, favicon 404 only). `node tools/visual-evaluator.mjs
  --id=leo2_revolution` exit 0, **RIG PARITY OK** (max yawProxy 2.3°
  @close-roof, no flips; every other view ≤1.4°) →
  shots/visual-eval-leo2_revolution/. `node tools/tank-standard-check.mjs`:
  clip **0/0 ✓** | contig **0 ✓** | decor **mg1+4d ✓** (gateMin 88.9 is the
  adjudicated row). `node tools/turret-parent-audit.mjs` → §B5 below.
- Yaw-90 proof: my own rig tools/tmp-defuse-recert-yawpair.{html,mjs}
  (FIFO self-ticketed, one hold) → shots/defuse-recert-yaw/ — 4 cameras
  (rear-left + front-right perspective, ORTHO side + top) × yaw 0/90 + a
  DIRECT subtree vertex census (instanced meshes included), census.json.
- Measurements: tools/tmp-rev-critic-r9-measure.py (standing §D refined
  mask: maxch ≤13 AND B−R ≥ +8) on the official pairs;
  tools/tmp-defuse-recert-crop.py crops DIAGNOSIS-ONLY;
  tools/tmp-leo-whodis.mjs world-box mesh probe (identification only).

## HEADLINE: RE-CERT PASS (re-freeze 1993cfb1) — all fourteen views hold 9.0 at graduation severity; the owner's "fused turret" is measurably dead

The de-fusion is REAL and PROVEN three independent ways on my rig: (1)
geometry census — rig_hull vertex tops across the ENTIRE turret footprint
(z −2.81..+2.90) never exceed **1.728** (honest deck bands 1.619/1.624 mid,
1.566–1.588 fore, 1.672–1.701 engine, matching the packet's novlo lines to
the millimeter); the only hull mass above 1.75 anywhere aft of the bow is
the hull-TRUE mast stack **2.465 @ z −3.83** (the print's own novlo
2.17–2.47 hull columns). The old bake-mirrors (deck 2.06, shelf 1.99, bow
humps 2.02–2.19, riser stair 2.13–2.22, engine course 2.21, corner posts
2.33, 2.03 fender strips, clamp tower) are GONE from the hull subtree. (2)
Yaw-90 pixel proof — the ENTIRE AMAP wedge superstructure (cheeks, shelf
wings, bustle, tail stubs, RWS, ring-gap shadow fills) rotates as one mass
in all four cameras; the deck left behind is the honest flat chassis +
mast; elevated (>1.78) side-silhouette content moves 95.5% between yaw
states (14215 → 9596 px footprint). The 636 pixel-stable elevated px sit
INSIDE the rotated-turret ring zone (z −1.92..+1.33) where same-camo turret
faces coincide across yaws — the geometry census (hull ≤1.728 there) proves
they cannot be hull remnant. (3) Official audits — §B5/§B2/§B4 below.

front 9.0 · frontleft 9.0 · left 9.0 · rearleft 9.0 · rear 9.0 ·
rearright 9.0 · right 9.0 · frontright 9.0 · top 9.0 · hero-fl 9.0 ·
hero-rr 9.0 · toptilt 9.0 · **close-front 9.0 (floor)** · close-roof 9.0

## Standing-check table (brief §1–5)

| # | Check | Required | Measured (mine) | Verdict |
|---|-------|----------|-----------------|---------|
| 1 | geometry-gate ×2 | 88.9 \| 88.9/90.4/91.8/91.2/99.5/100, floaters 100 ×2 | exactly that, bit-identical ×2 | PASS |
| 2 | tmp-hashgeo ×2 bracketing + sibs | 1993cfb1; bc9bad30/80b76338/3ae9b70c | 1993cfb1 (82/110365) ×2; sibs exact ×2 | PASS |
| 3 | yaw-90 proof | wedge rotates; no deck stays; no bake remnant | census + 4-cam pairs + static-pixel test (above) | PASS |
| 4 | §B5 parent audit | stranded ≤2, both mast-union AABB class | stranded **2** / abutting 0 / dangling 0; boxes [−1.42..1.42, top 2.46, z to −3.88] (hullDetail-merged, 61%) and [±2.00, top 2.46, z to −3.88] (hullDark whole-bucket, 31%) — both are merged-bucket AABB unions whose 2.46 top IS the hull-TRUE mast line; per the packet census zero turret-mass pieces are hull-parented | PASS |
| 5 | §B2 flood | toptilt = 94; no NEW holes vs packet | ALL TEN PROC digits EXACT to the packet: front 176 / rear 125 / rearleft 631 / rearright 313 / frontleft 143 / frontright 205 / left 410 / right 460 / top 131 / **toptilt 94** (≈92 px of every count is the label-glyph base) | PASS |

REF-side floods for scale (honest print, my run): front 2984 / rear 4148 /
top 4752 / toptilt 1417 / left 2426 — the de-baked print opens the same
sight-lines the proc now carries; every proc view remains far cleaner than
its ref. The r13 flood baselines are confirmed INVALID for the coupled
state, as the packet predicted; the digits above are the re-derived
honest-pair baselines.

## Claims audit (§D — packet r16 done-gates re-derived on my rig)

- Gate line + floaters ×5-poses claim: **CONFIRMED** (×2 bit-identical).
- Hash chain 7175fbf0 → 1993cfb1 (+3 ring fills, −1 merged): **CONFIRMED**.
- §B4 track-clip: standard-check **0/0** (the plank re-plane + 3.20
  open-crest held containment): **CONFIRMED**.
- Post-flip §B5 stranded 6 → 2 (mast-union class only): **CONFIRMED**.
- r16-e flood digits (all ten): **CONFIRMED TO THE PIXEL**.
- Deck re-lay lines (1.619 flat / 1.542 dip / 1.585–1.600 aft / 1.655–1.672
  risers / 1.694–1.701 engine / 1.540–1.440 bow / 1.588 hump / 1.566
  hatch): **CONFIRMED** via vertex census z-band maxima.
- Ring-gap fills mask-excluded, render-only, turret-parented: **CONFIRMED**
  (they yaw with the mass in my pairs; §B5 audit clean; gate row equality).
- Rig parity: max yawProxy 2.3° < 10 gate, no flips: **CONFIRMED** (the
  honest ref re-registers cleanly; scoring was legal).

## §B standing checks on the changed content

- **§B1 slopes / NO STAIRCASES / slope-motivates-the-mass**: the r15 bow
  kill HOLDS on the re-planed bow — at close-front the arch-to-beak lower
  silhouette reads ONE shallow diagonal with the certified single plank
  step (the print's own falling fender line); beak upper plane at the
  honest 1.44. Jacket course tops now vary per segment (segRunXT honesty)
  with co-planar joints — plate courses, not quantization steps. The turret
  wedge remains a motivated mass: raked cheeks (r14 A-2 carried), shelf
  and roof lines follow the rake; the new under-turret separation is the
  print's own ring gap, not a mass break.
- **§B2**: PASS — table above; the top/toptilt decks are filled; residual
  blobs are the glyph base + the honest ring-gap band class the ref itself
  carries at 2–6× the proc's size.
- **§B3 decoration / NO MYSTERY BOXES**: census mg1+4d. The turret-side
  furniture (tarp box, corner tabs, bustle tail stubs, RWS tub) reads
  camo'd and attached at 1×; the bustle stubs sit inside the rails'
  overlap exactly as authored. ONE new sub-bar read found (P-1 below): the
  fore-ring deck cluster (KIT periscope bodies + hatch-riser furniture,
  probed at x −0.55, y 1.50–1.55, z 1.65–1.75 world) presents flat
  grey faces that now stand against the NEW black ring fill at close-front
  / hero-fl magnification — at 3× they read as unidentified pale cuboids
  (they read as "pale hardware in the dark slot" at 1×). Pre-de-fusion
  this cluster read against camo bake-deck; the honest window exposed it.
  Sub-dominant at every official framing — named polish order, not a
  blocker.
- **§B4**: 0/0 ✓ and the close-front/hero windows read contained (pads
  wrap the idler under the plank step; wrap crests into the open past
  z 3.20 like the print).
- **§B5**: PASS (table); the two AABB unions decode against source +
  renders as the mast-stack/full-length-dressing merge classes.
- **§B6**: \________/ reads both sides; both end wheels raised; running
  gear untouched this round (dims 99.5 EXACT + side gate rows equal).
- **§H.4 variant distinctiveness** (vs frozen a5/a6/kf51, re-affirmed on
  fresh renders): revolution's tells all present — closed AMAP jacket
  cliffs + corner lattice cards + pale lattice tail band with the X ON it
  + RWS tub deck + (new, honest) visible ring-gap shadow line under the
  wedge; a5 keeps scalloped skirts/seven-wheel row/lower-wall X, a6 keeps
  blister + L/55 step + louvre grille, kf51 is its own silhouette family.
  No re-badge read; sibling bytes frozen (hash proof).

## Per-view justifications (bar ≥9.0, chieftain5 graduation severity)

- **front 9.0**: the turret reads as the ref's raked-wedge class (cheeks
  carry r14); ring band reads as the deep under-turret shadow the honest
  ref also carries (proc band structured by the clamp/gun assembly
  exactly like the ref's); bow lower + clamp pedestal/jaw parse as the
  travel-lock group; wing grids, towers, boards hold. Residual: fill end
  faces read crisp black at ±1.18 vs the ref's softer corner taper —
  sub-visible at 1×.
- **frontleft 9.0**: black slot honest (ref same class, broken by its
  wedge nose; proc broken by clamp + fore furniture); jacket courses
  co-planar; plank/idler containment clean; grey fore-ring cluster is a
  ~15×8 px sub-read at this framing.
- **left 9.0**: ring band med **7.1** (my rect [150:390]×[292:315]) vs the
  ref's open gap (53% sky at bg luma ≈26 + structure med 66.7) — both
  parse as the dark under-turret slot at 1×; the V-stair fill caps
  survive (certified read); segment-top honesty steps read as plate
  courses; mast + whip present. Residual: proc slot INKIER than the
  ref's see-through — polish note only (SHADOW-NAMED design).
- **rearleft 9.0**: the 539 px ring-gap blob is the honest sight-line
  above the fill caps (ref's same band 1220 px); tail stubs read as the
  basket tail; X + lattice + undercut corridor (track + wheels) all hold.
- **rear 9.0**: lattice band med **77.3 sd 12.91** (r13-certified digits
  ride unchanged) vs honest ref slat zone med 68.6 sd 13.59 — Δmed +8.7,
  same equipment class, inside the certified 70..85 gate. Residual: at
  dead rear the proc bustle reads a notch boxier than the print's tapered
  basket (stubs match the print's tail plate; the wings' airiness is the
  print's remaining edge) — gate-priced (turret 91.8 / stations 91.2).
- **rearright 9.0**: mirror of rearleft; lattice edge-on in family.
- **right 9.0**: mirror of left; band-edge strip bottom at the honest
  0.0885 (the 0.011 ground read is dead with the vlo).
- **frontright 9.0**: mirror of frontleft.
- **top 9.0**: THE EMPTY-DECK FAIL WAS HUNTED AND NOT FOUND — the honest
  1.619 deck carries the chorded fan arches, hatch ring + periscope hump
  plates, louvre cluster + cable backer, draped cables, tint plates,
  corridor seams; deck mottle med 52.1 vs ref 59.0, in family. Residuals:
  one dark tint rect (med 33.8 = the deck's own dark-camo shade) has
  ruler-straight edges at 3×; proc deck rowmean-sd 3.19 vs ref 5.84 (the
  carried print-grain gap).
- **hero-frontleft 9.0**: the r15 R5-1 ramp read SURVIVES the re-plane
  (one shallow diagonal, no serration); wedge + jacket dominate the
  frame; the grey fore-ring cluster + fill edges are the held residual
  (P-1) — visible at hero zoom, parses as hardware-in-shadow at 1×.
- **hero-rearright 9.0**: turret rear furniture camo'd + attached; aft
  fill band honest; louvres/deck bands/lift brackets read; lattice pale
  band carries its in-gate digits.
- **hero-toptilt 9.0**: flood 94 EXACT (2 px above the glyph base — the
  cleanest deck in this tank's history); fans, tabs, stubs, deck
  furniture all present; busier-than-print micro-relief is the carried
  grain class.
- **close-front 9.0 — FLOOR**: everything ordered in r12–r15 still reads
  (containment at max magnification, single-diagonal bow, cheek rake,
  engraved courses, clamp group identifiable). The floor read is NEW to
  the coupled state: the fore-ring deck cluster (periscope bodies +
  hatch riser + post) presents unpainted grey faces at high contrast
  against the black ring fill, and the fill's rectangular corner is
  visible left of the clamp (med 40.1 boxes on med ~5.5 black vs ref's
  one smooth wedge over med 63.2 shadow). Sub-dominant (~60×35 px of a
  640² frame), same residual tier as the r15 floor's clamp-plank/MAG
  holds — P-1 names the fix.
- **close-roof 9.0**: roof program carries (tub/optic/bins/periscopes
  with glass); deck strip at the honest height with re-derived mottle in
  family; the evaluator's 0.093 m² "void" at (−0.25, 2.37, 1.93) is
  ADJUDICATED warm-dark furniture interior + open chain — my pixel test:
  the dark population there is RGB (27.1, 28.7, 25.9), B−R = −1.2,
  FAILING the blue-signature sky term; contig 0 and the flood table
  stay clean.

## Residuals certified this round (owner-visible list, none blocking)

1. **P-1 (polish order, next dressing round)**: fore-ring deck cluster —
   KIT periscope bodies / hatch riser / post at x ≈ −0.7..+0.4,
   y 1.50..1.80, z 1.6..2.4 — give them their identifying tells in the
   deck material family (periscope hood + smoked-glass slit per the
   existing #1 driver-bank grammar; hatch riser gets its lid seam), or
   drop their tops ≤ the local fill-cap shadow line. Read exists at
   close-front/hero-fl against the new black fill only.
2. **P-2 (polish)**: top-view dark tint rect (PROC local [267:310]×
   [335:385]) — organic/soften its ruler edges toward the mottle grammar.
3. Ring fills render inkier (med 7.1) than the print's open-gap read
   (bg ≈26 through the true void) — intentional SHADOW-NAMED design,
   certified; revisit only if the owner reports the slot as "painted".
4. Rear-view bustle breadth vs the print's airier basket taper —
   gate-priced identity delta (turret 91.8 / stations 91.2), stubs match
   the print's tail plate planform.
5. Deck micro-grain rowsd 3.19 vs ref 5.84 + lattice Δmed +8.7 vs the
   honest ref — carried print-grain/tone classes (pre-existing).
6. §B5 audit shows stranded 2 mast-union AABB false-flags — standing
   instrument limitation (per-piece above-ring resolution would zero it).
7. Adjudicated by orchestrator, outside this verdict: hull 88.9 de-bake
   debt (retune round queued; worst columns are the certified −1.01
   shoe-pin 0.156, the +1.01 §B6-frozen 0-stripe 0.055, two ~0.03
   ground-line cols, then the ~0.02 banded-deck tail).

## Law discoveries (for the bank)

- **DE-BAKE CONTRAST WINDOW** (generalizes to the t64bv1/t72*/t90*
  vlo-class candidates): dropping a whole-vehicle LOD bake opens honest
  dark windows (ring gaps, under-overhang voids) — previously-certified
  furniture bordering the window inherits a NEW high-contrast read and
  its material/tell debt becomes visible (this round's grey fore-ring
  cluster). Any vlo-drop round must re-audit furniture adjacent to every
  new shadow window at close/hero range even where the furniture bytes
  did not change.
- **YAW-PROOF STATIC-PIXEL FALSE-FLAG**: a rest-vs-yaw90 pixel-diff can
  read same-camo turret faces as "static furniture" inside the rotated
  ring zone (636/14215 px here). Adjudicate static-pixel findings
  against a rig_hull/rig_turret subtree vertex census (instanced meshes
  included) before ordering geometry — the census is the authority on
  what is hull-parented.
- Blue-signature term (revolution-r7) again converted an evaluator void
  into warm-dark furniture (B−R = −1.2 tub interior) — third confirmed
  instance; the evaluator's void reporter is the remaining consumer that
  could adopt it.

## Verdict

**RE-CERT PASS (re-freeze 1993cfb1)** — floor 9.0 (close-front), all
fourteen views at 9.0, mean 9.0. The owner's complaint is killed at the
root: the turret is a real rotating mass over an honest flat deck, proven
by geometry census, yaw-90 pairs, and every official audit; all five
standing checks land EXACTLY on the packet's coupled numbers (flood table
to the pixel, gate to the decimal, hashes to the byte). The adjudicated
88.9 hull row is the honest price of de-baking a polluted instrument and
is queued for retune outside this verdict. The coupled state landed as
**c9ddba0** mid-session and re-verified byte-identical on the committed
tree — re-freeze **1993cfb1** stands certified; P-1/P-2 ride the next
polish round, non-blocking.
