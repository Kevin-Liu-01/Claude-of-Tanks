# t90m — INDEPENDENT GRADUATION ADJUDICATION, SECOND SITTING (2026-08-06)

Fresh 14-view graduation verdict on the r8 order round (7e8a694, all four
first-sitting orders claimed DONE) for buildT90MProryv
(src/vehicles/profiles/russia.js, RUSSIA_PROFILES). Candidate proc hash
**e345ee8a (49 meshes / 111241 verts)**. State certified: CLEAN-ROOM
WORKTREE detached at 7e8a694 (the live tree carried active foreign WIP —
fleet gate JSONs + sim selftests churning under a parallel session, files
changing between snapshots — so every measurement below ran from a
byte-clean checkout; untracked official drivers snapshot-copied in,
sha256-recorded, and every page they drive is git-tracked at HEAD).

## HEADLINE: **GRADUATION PASS (dual gate met at e345ee8a → orchestrator
## runs §10; the program's 27th)** — gate PASS ×2 at the r8 line to the
## decimal (90.7 | 90.7/91.6/93.4/94.1/100/100), every machine gate green,
## and the 14-view shaded-parity bar is met: **14/14 views ≥9.0, floor
## 9.0, mean 9.10** (first sitting: 2/14, floor 8.4, mean 8.76). All
## three mask-blind FAIL classes are resolved with measured reads; the
## two r8 attribution corrections were honored and judged fresh.

## Provenance (§D discipline — everything below measured by this critic)

- Hash bracket (tools/tmp-hashgeo.mjs BEFORE and AFTER all renders):
  **e345ee8a (49/111241)** both times. Frozen sibs byte-exact both
  checks: pt91m 2cf10e23, t72b3m 1e1ca4b8, t84 4707a9c (t72b3m also
  exercises the eraRuCheeks relikt branch without rChev — the guard's
  live proof). Every render and measurement in this verdict is inside
  the bracket, in the clean room, at committed HEAD bytes.
- GATE ×2 (consecutive official runs, clean room): **min 90.7 | hull
  90.7 whole 91.6 turret 93.4 stations 94.1 dims 100 floaters 100
  PASS** — both runs identical to the decimal, = the r8 claim exactly.
  Registration anchors UNMOVED: dAlong 1.363 / dy −0.003 (bow/stern
  extremes held per REGISTRATION-ANCHOR law).
- Pairs rendered FRESH by this critic (tmp-tank-critic driver on the
  HEAD-tracked page, own FIFO ticket, zero console errors); scored ONLY
  my own shots/critic-t90m/*.png in the worktree.
- `visual-evaluator --id=t90m`: exit 0, **RIG PARITY: OK** (11 ortho
  views, max dYawProxy 1.1° @close-front — the baseline value — max
  |dCentroid| 0.047m). The r30 top-view false positive did not fire.
- `track-clip-audit --exact`: **band 0/0 AND shoe 0/0, blind spots 0**.
- `standard-check`: gateMin 90.7 fresh, clip 0/0 ✓, contig 0 ✓, decor
  **mg1+4d** ✓.
- `winding-audit`: mode 1 **rev 0 / mix 0 / FrontSide deficit 0px**
  (census 697 pieces, 0 flagged). Mode 2 HARD = **candidatePx 1584
  exactly** — rig_hull mesh#21 (1491px) + mesh#19 (93px), worldXZ
  z −3.42..−2.92 / |x| ≤ 0.99 = the adjudicated hull-correct rear
  drum-rack negative, indices re-shuffled by the r8 re-buckets, content
  identical (1330+161+93 = 1491+93 = 1584). Nothing else strands.
- §J yaw pair at e345ee8a (HEAD-tracked yawpair page): hatch rings,
  T05BV-1/RWS cluster, bustle bins, and the 527-marked flank ALL sweep
  through yaw-90; hull skirts, flaps, and the rear drum rack stay
  pixel-static. Evidence: worktree shots/merkava-b5/yawpair-grad2-t90m.png.

## The three first-sitting FAIL classes — resolved, with measurements

1. WHEEL COUNTABILITY (§B8.1.1, was floor 8.4 both sides): **six wheels
   countable at 1x in BOTH side views**, judged at NATIVE tone per the
   NATIVE-TONE law. Programmatic verification (per-column median-luma
   segmentation over the wheel band): left side six dark segments with
   five pale gap notches (x142-392 panel), right side six segments/five
   notches (x249-498). Wheel-window dark-px p50: left proc 14 vs ref 16
   (same black class); right proc 32 vs ref 25 (key-side lighting on
   the dish faces — the documented residual; circles unambiguous).
   The hem mechanism matches the oracle decode: pale hem at the
   constant line over an OPEN run (no curtain), front taper + rear
   half-bag pair sitting at the ref's end-zone hem depths. Quarters
   read the run too.
2. REAR/TOP EQUIPMENT TONE (attribution correction honored — the old
   "tan drums" were the LOG; judged the actual tones fresh): the whole
   rack renders GREEN. Drum zone RGB p50 **(69,86,58) luma 80** vs ref
   drum zone **(73,85,63) luma 81**; log zone (64,74,55) — green,
   consistent with the oracle's own green rear-plate family read, NOT
   a defect (the "log MAY read wood-tan" clause is moot: the oracle
   renders its log green; the proc matches the oracle). **Warm census
   (R>G+12 & R>90) = 0 px in ALL FIVE flagged views** (view-rear/top/
   rearleft/rearright/hero-rearright; first sitting measured
   5098/641/623/556). Strap/end-ring details read.
3. ROOF GRAMMAR (§B3/§B2): **two bold circular hatch reads** at 1x in
   view-top and at 2-4x in close-roof/hero-toptilt — ring wall + pale
   rim + lid + dark seam + periscope studs + hinge on each; the
   T05BV-1 pedestal/sensor drum/bin cluster rises from INSIDE the
   commander ring with the Kord on top (grammatical, and it sweeps
   with the turret in the §J pair). Relikt relief: glacis wedge
   V-crests + tile separators read at 1-2x (visible even in view-top);
   cheek cassette course seams + shoulder ribs read at 2-4x as thin
   dark diagonals with crest strips — present and grammatical, but
   lighter than the print's baked arrays (the r8's own honest
   residual; SSC sub-half-pixel discipline caps the proudness).

## Per-view scores (fresh pairs vs the CC-BY minehffd oracle, graduation severity)

| view            | r1  | r2  | Δ    | named reads |
|-----------------|-----|-----|------|-------------|
| view-left       | 8.4 | **9.2** | +0.8 | Six wheels countable at 1x, native tone (p50 14 vs ref 16); hem at the oracle's constant line; roof tiers/bustle staircase/skirt taper/gun run all hold. Residual: wheel exposure a shade shallower than ref (dark-run tops higher), pad faces read dustier than the ref's dark run (median identical (21,27,32), mean +12L). |
| view-right      | 8.4 | **9.1** | +0.7 | Same mechanism mirrored; key-side dish faces brighter (32 vs 25, documented class) — circles unambiguous, count clean. |
| view-front      | 9.0 | **9.1** | +0.1 | §B3.1 intact (bore disc, collar, boot); both cheeks raked (§B1.1); glacis wedge V-relief now reads; front towers still a shade proud (priced 0.03x convex-ramp class, rows byte-identical to r1). |
| view-rear       | 8.7 | **9.1** | +0.4 | Rack green end to end; drum p50 (69,86,58) vs ref (73,85,63); log green = oracle's read; warm census 0 (was 5098). Residual: bin/basket faces still flat-dark vs the print's bolted-plate bake (r1-priced class). |
| view-top        | 8.8 | **9.2** | +0.4 | Two circular hatch reads at 1x; stern rack green; staircase planform excellent (plan rows unchanged); glacis chevron reads. Residual: gunner ring r 0.27 vs ref ~0.41 (documented hump-band interior constraint); roof relief still lighter than the bake. |
| view-frontleft  | 8.9 | **9.1** | +0.2 | Wheels read through the run at 1x; cheek cassette inset + course seams; hem/flap grammar right. |
| view-frontright | 8.9 | **9.1** | +0.2 | Mirrored; key-side wheels read; cheeks both raked. |
| view-rearleft   | 8.7 | **9.1** | +0.4 | Green drums end-on + bins; wheels countable; rack/log silhouette correct. |
| view-rearright  | 8.7 | **9.1** | +0.4 | The r1 "loudest drum tone" view now reads green; bustle tail-tier asym (right-deep) matches ref. |
| hero-frontleft  | 8.9 | **9.0** | +0.1 | Masses/kit/wheels strong; glacis V + separators read at hero zoom; cheek courses read as thin seams — the one remaining soft class vs the ref's bold baked arrays (mechanism-capped, priced). |
| hero-rearright  | 8.7 | **9.0** | +0.3 | Green drums at zoom; wheels read; bustle walls still flat vs the ref's mesh-basket bake (r1-priced texture class, now the only read left in the view). |
| hero-toptilt    | 8.8 | **9.1** | +0.3 | Both hatch circles + lids/studs read; RWS/pedestal at the commander ring; cable conduits; roof busier than r1 but still cleaner than the print's bake. |
| close-front     | 9.1 | **9.2** | +0.1 | §B3.1 showcase unchanged (tube/sleeve/evac/crest/boot/bored muzzle, no prism); glacis wedge relief adds the missing front-plate read. |
| close-roof      | 8.6 | **9.0** | +0.4 | r1's three named defects all present now: both rings (rim/lid/seam/studs at 3x), T05BV-1 cluster grammatical at the ring, chevron course seams + shoulder ribs at 2-4x. Residual: course boldness vs bake; near-flush ring relief (≤1.2 cm budget) reads subtler than the print's deep-drawn cupola. |

Floor **9.0** (hero-frontleft, hero-rearright, close-roof) · mean
**9.10** · ≥9.0: **14/14**.

## Priced residuals re-verified (fresh gate JSON, both runs)

Worst rows BYTE-IDENTICAL to the first sitting's decode — no new class
at e345ee8a: side_hull 0.036 @5.27 (muzzle-run) / 0.034-0.033 @−0.43/
−0.56 (front-ramp convex 0.03x); turret side 0.051 @1.06 (apron);
plan_whole ±0.06 → 0.076 ×2 (muzzle/notch spec-datum); plan_hull ±0.06
0.174/0.169 and turret plan corners 0.161/0.127 (certified baseline
classes, identical at 4acdebe4 and e345ee8a); teeter family ≤0.036 this
phase. Stations 94.1 both runs; i5/i7 topPct 0.18/0.71 (unmoved class).

PACKET-TEXT DISCREPANCY (named, non-blocking): the r8 claim "all
stations ≤1.42 wPct" is wrong for station **i9: wPct 5.45** (topPct
1.42) — present in the committed r8-close JSON and reproduced exactly
in both my runs; it IS the deleted-hem slice-width effect the packet
attributes the 94.4→94.1 drop to, and the component number was claimed
honestly. Discipline note for the landing, not a gate issue.

## Honest residuals (beyond the priced classes — none score-moving)

- Shoe-pad faces read dusty (belt median (21,27,32) == ref; mean +12L
  from lit pad tops) — at 1x a slightly warm-looking track bottom rim;
  warm census 0, not the owner's warm class.
- Small blue-gray sensor-window elements (roof front, turret flank)
  read distinctly blue at 2x+ — family-kit tone, few px at 1x.
- Bin/basket faces and bustle walls: flat-dark vs the print's baked
  plate/mesh texture (r1-priced bake class, unchanged).
- Cheek course arrays and roof relief lighter than the print's bake
  (mechanism-capped: SSC sub-half-pixel + heightM budget).
- Right-side wheel windows brighter than ref (key side, documented).
- Gunner ring r 0.27 vs ref ~0.41 (hump-band interior constraint).

## §H.4 distinctness (standing check — PASS)

t90m vs t90sm/t72b3m/pt91m: welded staircase-planform turret + twin
roof-hump hatch rings with T05BV-1 RWS + rear drum rack + full-length
Relikt skirt line with open wheel run — vs t90sm's shroud-backed
turret, t72b3m's cast dome + K-1 cheeks + snorkel rack, pt91m's ERAWA
cheeks + side bins. The r8 changes (exposed dark wheels, hatch rings)
move it TOWARD family norms without erasing any tell. No re-badge read.

## Law-bank candidates (this adjudication)

- CLEAN-ROOM ADJUDICATION: when the live tree carries foreign WIP, a
  detached worktree at the candidate commit + node_modules symlink +
  snapshot-copied untracked drivers (sha256-recorded, pages tracked at
  HEAD) reproduces the full official battery bit-exact — gate line,
  winding candidatePx, evaluator parity all landed to the decimal.
- DONE-GATE TEXT AUDIT: builder done-gate NUMBERS can be right while a
  summary CLAIM is wrong (stations 94.1 honest, "all ≤1.42 wPct" false
  at i9 5.45) — critics re-verify the claim text against the row data,
  not just the component line.
- MODE-2 INDEX DRIFT: re-bucketing recomposes merged-mesh indices; a
  stable candidatePx TOTAL with matching world-XZ footprint is the
  identity check (1584 == 1491+93 == 1330+161+93), not mesh numbers.

FOR THE ORCHESTRATOR (no builder action):
- Run GEOMETRY-GATE §10 at e345ee8a (dual gate met; this doc + the r8
  packet section are the graduation evidence pair).
- The winding mode-2 certified-class annotation for the t90m drum rack
  (merkava-tail-pack pattern) is still worth landing so fleet runs
  stop re-flagging 1584px.
- Record the i9 wPct packet-text discrepancy with the landing note.
- The r30 evaluator top-view skew flag remains open fleet-wide (did
  not fire here).

VERDICT: **GRADUATION PASS** — gate ×2 at the certified line, every
machine gate green at e345ee8a with sibs byte-frozen, and 14/14 views
at or above 9.0 against the oracle at graduation severity. The three
mask-blind classes that failed the first sitting are fixed in the
render truth, measured, and yaw-proven. The program's 27th graduate.
