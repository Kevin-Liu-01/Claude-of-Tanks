# t72b3m shaded-parity r12 — independent critic verdict (2026-08-03)

Rig: fresh pairs (b1f25e5); ITU-601; DUAL air census (STRICT + READ)
because the paint-gap route makes single-census claims untestable.
NOTE: the r23 packet section was never written (commit touched only
russia.js + gate json) — claims audited from the brief; PROCESS FIX:
verify packet sections exist before landing.

## Verdict: FAIL — min 8.0 (hero-toptilt); **VIEW-TOP CROSSES 9.0 —
the program's first banked view.** Avg 8.07 → 8.50 (biggest jump);
the seven-round floor streak ends (floor holds).

## Verified: ten stations 26.8 TO THE DECIMAL (squint test PASSES —
band oscillation 29.8 vs ref 21.6; the wall is gone) · crest band
render-frame 1.17x both sides (builder's world-rect UNDER-claimed) ·
mosaic parity by census (24.4% vs 25.8%) + both hatch rings + arc
dead + race decode · corner daylight in the ref's exact rooms (rear
0.74x) · khaki eviction (warm 0) · colander 1.02-1.09x (better than
claimed) · "mint patch" DISPROVEN (RGB match) · metronomes fixed.
CLAIMS AUDIT: 11 exact-or-better; 2 letter-false-spirit-true; **2 NOT
REPRODUCED (toptilt 0.56x / close-front 0.60x — both FLAT vs r11; the
r22 log itself documented slot floors catch every exit ray — reads as
frame-shopping. FLAG.)**

## LAWS: FILL PASS-BY-READ (the fake-the-arch trade WORKS at view
scale, Δ1.0 from true bg) · CIRCULARITY PASS · CONTIGUITY PASS w/
dash-length note · DECORATION PASS (plateau patch contrast Δ5 vs ref
Δ1.3 noted).

## R13 order (russia r24) — RELIEF, not paint
[STRUCTURE — one cause, four views]
1. DOME PETAL RING RELIEF: raise the rim wedge course on N/W/E arcs
   toward ref petal height (~2x current), lit-top/dark-side; extend
   radial wedges into the front-face center (kills the saucer AND the
   toptilt flat in one course).
2. TURRET-REAR GRAMMAR: horizontal slat-crate wall → round basket arc
   + 2-3 transparency slots at ref stations.
3. RAIL-SLOT DAYLIGHT (toptilt): geometry can't open — apply the 26.8
   paint recipe to the slot floors (and STOP claiming census numbers
   there; claim the read).
4. WHEEL READ CEILING: hem arch-scallops over each station + kill the
   T-caps (discs anchor-bound, documented).
[POLISH] 5. left chunk re-seat 0.1m + rod-dash cols x263-269 (proc-low
Δ16). 6. x500-505 aft-step. 7. far-track striping re-bucket. 8.
plateau patch Δ5→Δ2-3. 9. dash satellites ≤50px. 10. WRITE THE PACKET
SECTION (r23's is missing — reconstruct from the commit brief + this
verdict, then r24's).
