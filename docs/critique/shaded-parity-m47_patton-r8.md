# m47_patton shaded-parity r8 — FOURTH ADJUDICATION, GRADUATION CANDIDATE (2026-08-04, post-914bdb0)

Rig: fresh 14 pairs `node tools/tmp-tank-critic.mjs --id=m47_patton` →
shots/critic-m47_patton/ (zero console errors) + `visual-evaluator
--id=m47_patton` (exit 0, **RIG PARITY OK**, max yawProxy 1.303° @front,
|dCentroid| ≤0.047 m, camoSeed 4242, evidence shots/visual-eval-m47_patton/).

BYTE-STABILITY BRACKET (concurrent-r9-builder hazard, LIVE again this round):
m47 geometry hash **70941de0** (109 meshes / 103 383 verts — the packet's
exact post-S1 line) BEFORE renders and AFTER all evidence capture. Timeline:
14 pairs + evaluator 17:30, gate ×2 + standard/clip/parent audits + frozen-
hash proofs ≤17:41, §B5 yaw pair 17:45 — then at **17:47:49** the r9 builder
(m46 polish) landed working-tree edits on patton.js. Adjudicated SAFE for
this verdict: (1) every m47 evidence artifact predates the edit; (2) the diff
is §F.2-conformant (T.loft.smooth default OFF; m46-scoped rack texturing —
m46 tree-hash 8cf23500 vs committed 99a3b0b4 confirms the edits are LIVE for
m46 only); (3) re-hash AGAINST the modified tree still prints **70941de0**
for m47; (4) my close-roof pair is **pixel-identical (0 differing px)** to
the builder's committed-state shots/patton-r8/close-roof.png — two
independent renders of 914bdb0 agree to the byte. The brief's stop condition
(m47 hash drift) never fired. NOTE: my fresh §H.4 m46 pairs (17:48) postdate
the edit and show the r9 WORK-IN-PROGRESS m46, not committed 99a3b0b4 — the
distinctness verdict below is stated for both states (tells are structural
and invariant; the committed m46 is strictly plainer, hence strictly more
distinct).

Official gate on my watch: **90.5 PASS ×2 bit-identical** (hull 90.5 / whole
91.0 / turret 91.6 / stations 93.6 / dims 100 / floaters 100) — reproduces
the r8 packet line to the decimal, turret UP 91.4 → 91.6 (the S-group spend
is net NEGATIVE, as claimed). `tank-standard-check`: clip **0/0 ✓**, contig
**0 ✓**, decor **mg1+1d ✓**. `track-clip-audit --exact`: front 0 / rear 0.
`turret-parent-audit`: stranded **0** / abutting 0 / dangling 0. §B5 yaw-90
pair RE-RUN by me with the NEW turret content (banked harness
tmp-merkava-b5-yawpair.mjs → shots/merkava-b5/yawpair-critic-r8-m47_patton
.png): bustle grid, tarp, '123', whip, M2 mast AND the new throatNotchShadow
all sweep with the turret; nothing stranded, nothing dangling (the r8 packet
omitted this pair — discipline note only, my run supplies the evidence;
source audit confirms `P.turretG.add(nm)`). Graduates m60a1 **81e69e34** /
m60a3 **efcde5c4** hash-verified packet-exact (do-not-gate respected). m46
committed **99a3b0b4** verified pre-drift. Measurements: banked scanners
(tmp-r7-merkava.py + tmp-r6-m47.py) on MY fresh pairs; zoom crops
diagnosis-only (scratchpad). All r6 numbers re-derived this round.

## HEADLINE: **GRADUATION PASS** — floor 9.0, ALL 14 views at the bar; every r6 order delivered on its done-gate or adjudicated inside its declared window; both flagged bar risks adjudicated NOT bar-holding (procOnly-28 decomposes to certified/other-lane content + the ref's own cheek grammar; the 87.8° carry is the priced throat-notch class, render-plugged by a lawful §C proxy); the albedo lane's knob-on state reads ref-parity print class; zero regressions anywhere. DUAL GATE MET — the patton family's first graduate. §10 runs on this verdict.

front 9.0 · frontleft 9.0 · left 9.0 · rearleft 9.0 · rear 9.0 ·
rearright 9.0 · right 9.0 · frontright 9.0 · top 9.0 · hero-fl 9.0 ·
hero-rr 9.0 · toptilt 9.0 · close-front 9.0 · close-roof 9.0

Ladder: r3 8.3 → r4 8.5 → r6 8.8 → **r8 9.0 floor**. The r6 structural
driver (bustle planar-wall vs cast shell) is dead: corners grade, the ref's
own r0.119 cheek-roll arc is PAIRED (proc r0.088, dCenter 0.033), B2 windows
beat their bars, battens read cloth. The r6 floor (top 8.8, albedo field) is
dead: census 2334 → 1620 vs ref 1160, and the residual ~460 adjudicates on
the zoom as fittings/spare-track shadow content, not bare-plate blotch.
IDENTITY never in doubt (needle nose, blister pods, long tarp'd bustle, bow
ball, six dished wheels, capsule deflector, mast M2 + whip).

## r8 delivery verification (every claim re-measured on MY pairs)

- **S1 bustle cast shell ✓ (arc gate + crops; procOnly residual ADJUDICATED)**:
  rear arcs ref 2 / proc 1 / **PAIRED 1** — the fitter pairs the ref's own
  cheek roll @ (0.78, 2.53), proc r 0.088 vs ref 0.119, dCenter 0.033 m
  (was 0/0 at r6). B2 full [175..465]×[313..352] med **71.4** (bar ≥68; r6
  67.2), cavity med **75.3 vs ref 73.8**, cavity sub-45 **0** (full-window
  19, all at the x≤191 gear edge; r6 23 — improved). Corner wraps grade
  continuously on my 3× crops; the remaining flat expanse is the CLOTH TARP
  (ref-class own-grammar). THE FLAGGED RISK — evaluator rear procOnly **28
  vs the ordered ≤18** — decomposed edge-by-edge on my run: whip pair
  0.473/0.468 @ y 3.17 (certified D1 corridor), fender/muffler horizontals
  ×3 (179.6°/0.470, 0.3°/0.419, 179.3°/0.322 — hull lane, r6-carried),
  skirt tabs 90.0°/0.448 ×2 @ ±1.71 (r6-carried; cycle-3 slim attempt
  reverted, negative banked), tailstack corner pair, dome/pod chords ×4
  (y 2.15-2.59), ~10 M2-corridor fragments (y ≥2.74, certified band), and
  the cheek diagonals 74.3°/0.275 + 108.1°/0.206 + fragments 0.196/0.142 —
  the ref prints the SAME grammar class at the SAME stations (refOnly
  112.9°/0.194 L + 66.5°/0.214 R). Segment count is composition-shifted,
  not defect mass (law-bank discovery (1) confirmed independently).
  ADJUDICATION: not bar-holding — the ordered INTENT (kill the planar-wall
  read) is delivered and measured. NEW POLISH-LANE NOTE (no order): the
  proc cheek diagonals lean OPPOSITE to the ref's at the same stations
  (74.3 vs 112.9 left; 108.1 vs 66.5 right) — sub-0.28 m, sub-visible at
  1×, logged for any future texture round.
- **S2 tangent verticals ✓ ORDERED WINDOW CLEAN BOTH QUARTERS**: proc-only
  ≥0.4 m 88-93° in z -1.55..-1.70: **0 on frontleft AND frontright** (r6:
  91.9°/0.65 fr + 88.4°/0.50 fl). The fr trailing vertical is DEAD.
  Residuals, all named: fl **87.8°/0.502** (packet-exact; 0.2° below the
  ordered window — the throat-notch class, r6-carried) + its fr twin
  87.8°/0.361 (sub-0.4, packet-scoped out honestly); frame-rear
  near-verticals 91.1°/0.503 fl + 88.2°/0.518 fr @ 2D frame rear ≈ the
  whip/tail corridor (rear-view whip pair confirms attribution; quarter
  midWorld is depth-assumed — law discovery (2) applied). The notch itself
  is render-plugged by `throatNotchShadow` (§C lawful: *Shadow*-named,
  mask-excluded, LIT-class 0x424535/env 0.24, turretG-parented — verified
  in source AND on my yaw pair); the throat reads FILLED at 1× on my
  frontleft/hero-fl pairs. Priced carry, same class r6 carried.
- **S3 side grading ✓**: the bustle side wall shades graded vertically and
  toward the tail on left/right/quarters/heros (my pairs + builder A/B
  crops concur); landed inside a bit-identical gate ×2 with turret +0.2.
- **S4 batten mute ✓**: no pale rails on the rear face — the three straps
  read cloth; the tarp'd shell is one soft mass (rear + rearleft/rearright
  at 1× and 3×).
- **T1 curtain continuity ✓ (order intent met; window caveat verified)**:
  the r6 signature (3-4 discrete gray verticals with LIT gaps) is DEAD —
  my curtain-core tracking profile (darkest 12-row run, y 328..392) is
  uniform over the run core (rearleft x 210..450 within ~4L; no periodic
  alternation on either quarter; end bumps are idler/sprocket content).
  Fixed-row windows overread exactly as the packet documents (run-cover
  TOP faces are lit N3-ordered content — 76L class). No sky/wall reads
  between curtain columns anywhere.
- **T2 carrier-ring mute ✓ EXACT + N5 held**: drum window [55..110]×
  [355..410] p95 **83.6** (bar ≤ ref+6 = 84.5; ref **78.5**; r6 93.0) — the
  drawn circles are subdued rim light at 1×. N5 compensation verified:
  hero-rr gear sd **11.56** (bar ≥11; ref 13.15), p95 **79.4** vs ref 86.4
  (proc QUIETER — no overshoot; ≤ ref+4 bar 90.4). A1 HELD EXACT: sub-30
  **0**, p5 **54.1**, med 66.6, p75 71.6. A2 HELD: wheel band p75 **72.2**
  (bar ≥66). N1 HELD: gear r/g **1.004** (bar ≤1.01), own-hull 0.971 —
  residual split 0.033 = the r6 watch, unmoved, sub-driver. N4 HELD:
  largest sub-25 blob **0 px** in rearleft/rearright/hero-rr. WATCH (new,
  in-class): the rimBoost double-rings read slightly more regular than the
  ref's rim lights at quarter zoom — measured inside every ordered bar.
- **ALBEDO LANE (knob-on, 09eeafe) ✓ ref-parity print class**: top census
  [260..380]×[330..490] sub-50 proc **1620** vs ref **1160** EXACT (r6
  2334). Zoom adjudication of the window: the proc darks are fittings
  shadow slots (T-bar, posts, spare links, pioneer tools) — CONTENT, not
  the r6 bare-plate blotch mass; the deck now carries the ref-class
  bakeDirt mottle. The r6 top-view driver is dead; the ~460 residual is
  a distribution hair at 1× (proc accents crisper, ref blotches softer).
- **C4 re-derived**: hero-rr tip window mask-air **28.3% vs ref 27.5%**
  (r6 28.6 — the egg-end filled ~0.3pp of the sliver; improvement,
  anchor-fenced carry).
- **C1 hold ✓ machine-zero**: blue-dominant census 0 px, front AND
  close-front, both halves.

## Standing checks (§B + §H.4)

- **FRONT SLOPES §B1: PASS** — front 27 matched, ONE flag Δ-5.4° @ the
  certified M2 crown band (identical to r6 to the decimal); glacis rake,
  cheek slopes, dive line inside noise; p95 Δtop 0.138 / Δbot 0.099
  (r6-identical). The T26 casting renders ROUND (needle nose, dome —
  own-class). Quarter worst flags -10.1/-8.5/-12.7/-14.4 = the r6-priced
  ±11-14 corner-bias/dive family, unchanged carriers.
- **FLOOD BLUE-SIGNATURE / NO EMPTY AREAS §B2: PASS** — **0 blue-dominant
  px across all 28 halves** (14 views × 2, full-frame census). Machine
  top-down contig 0 ✓. Void inventory = r6 classes to the m²: hero-rr
  0.055 under-belly + 0.051 fender-toe (+4 sub-0.02 minors), toptilt
  4.673 projection-air family (same class), close-roof 0.406 = the
  MG-PHYSICS sky window (must stay open) + 0.039 barrel-bow air. No new
  voids from the bustle re-emit.
- **DECORATION / MG PHYSICS §B3: PASS** — mg1+1d censused; sky-backed M2
  pale (crown corridor block-luma proc 60.6 vs ref 59.5 on my remapped
  scan); receiver stepped + dappled on my close-roof; whip D1 lawful.
  NOTE (measurement mapping): the r4-era rod window [215..370]×[200..240]
  no longer intersects the mast on the current close-roof framing (all-bg
  on BOTH my render and the r8 baseline) — no rod claim was made this
  round; scan remapped to the actual corridor, parity confirmed.
- **TRACK CONTAINMENT §B4: PASS** — exact audit 0/0; wraps clear at 3-4×.
- **TURRET PARENTING §B5: PASS, LIVE-VERIFIED** — machine 0/0/0 AND a
  fresh yaw-90 pair with the r8 content: the whole turret group (smooth
  bustle, tarp, notch curtain, mast M2, whip, '123') sweeps together; hull
  furniture stays. Floaters 100 ×2 ✓.
- **TRACK RUN §B6: PASS** — floor-line trace: flat contact 249 px (proc)
  vs 247 (ref) both sides, ground line Δ2 px, BOTH ends lifted 74-81 px —
  the \\________/ trapezoid, matched ends.
- **ROUND-CASTING CLASS (§B1 companion)**: the S1 shell is chord-limited
  smooth-normal work — no polygonal read at 1×; the paired arc carries
  `aperiodic waviness`, not facet steps (fitter note), matching the ref's
  own casting read.
- **VARIANT-DISTINCTNESS §H.4: PASS (stated for BOTH m46 states)** — vs
  m46 at committed 99a3b0b4 (r6/r7 record + pre-drift hash) AND vs the
  r9-in-progress tree my 17:48 pairs actually show: m47's tells are
  structural — long squared tarp'd bustle + '123' vs short rounded bustle;
  mast-mounted M2 + pyramid truss + whip vs low forward pintle; needle-
  nose blister muzzle vs plain tube; deep continuous curtain + skirt tabs
  + louvre trays + pioneer/rack dressing vs plainer deck; double rim-ring
  camo'd wheels vs plain olive gear. Both marks now carry toned gear (the
  r6-era black-gear tell is retired as briefed) and remain unmistakable at
  a garage glance. The r9 work pushes m46 TOWARD m47's shell grammar and
  the tells still hold — a fortiori vs committed. No re-badge read vs
  m60a1/m60a3 (different generation; hashes frozen-exact).

## Per-view justifications (bar ≥9.0 "same vehicle, same tier", 1×; certified/banked residuals named and drive no score per program precedent — pt91m r28)

- **view-front 9.0** — r6-identical machine profile (27 matched, 1
  certified flag, p95 exact), C1 zero; residuals: certified M2-band mass,
  texture-hint depth (r6-priced class).
- **view-frontleft 9.0** — S3 wall grades; T1 band continuous (core
  profile uniform); S2 window clean; residuals: 87.8°/0.50 throat carry
  (priced, plugged), whip-corridor frame-rear verticals (D1), ±11-14
  corner-bias family (priced).
- **view-left 9.0** — A1/A2 exact, T2 ring mute delivered (the r6 loudest
  accent); horn rhythm + subtle posts remain (r6-priced hairs); profile
  p95 0.094 class unchanged.
- **view-rearleft 9.0** — wall grades, band continuous (11.7-12L core
  class verified), rings muted; residuals: egg-end perspective fragments +
  whip corridor (certified/priced).
- **view-rear 9.0** — arc PAIRED at the ref's own cheek roll; B2 windows
  beat bars; corners grade on crops; battens cloth; procOnly-28
  adjudicated (decomposition above); residuals: cheek-lean inversion
  (sub-0.28 m polish note), rear furniture sparser than ref rack hardware
  (tarp own-grammar), certified M2/whip corridor.
- **view-rearright 9.0** — mirror of rearleft; '123' fine; Δ-12.7 family
  priced.
- **view-right 9.0** — as left mirrored; capsule deflector (B6 banked);
  Δ+11.9 dive mirror priced (r6-exact recurrence).
- **view-frontright 9.0** — the r6 B1 twin (91.9°/0.65) is DEAD; procOnly
  11 — cleaner than the ref's own refOnly 15; residuals: 87.8°/0.36
  sub-window twin, whip corridor, Δ-14.4 family (priced).
- **view-top 9.0 — THE r6 FLOOR, +0.2** — census 1620/1160 with the
  residual adjudicated as fittings content on the zoom; deck mottle
  ref-class; 14 matched / ONE flag Δ+1.8°; p95 0.191 both (registration
  class); residuals: certified notched-rail/fender-bump trade, crisper
  dark-accent distribution (hair), mid-deck furniture density (hair,
  largely compensated by rack bed + louvre banks + pioneer row).
- **hero-frontleft 9.0** — slab/posts/tangent all dead; arcs paired 1
  (ref 4 — ref's rounder hero grammar, priced hair); residuals: C4 sliver
  (28.3, banked), 95.6°/0.43 egg-end perspective fragment, fender-line
  horizontal (hull class).
- **hero-rearright 9.0 — the r4 floor, +0.5 over two rounds** — N1
  1.004 / sd 11.56 / p95 79.4 (quieter than ref) / N4 zeros / wedges 0;
  envelope-diagonal chains re-confirmed BILATERAL (ref 143.7°/5.82 +
  152.1°/5.30 vs proc 131.5°/4.68 + 142.3°/3.88 — the adjudicated rig
  artifact, third consecutive confirmation); voids r6-class; residuals:
  gear↔hull r/g split 0.033 (watch, in-bar), rimBoost regularity (watch,
  in-class), C4 (banked).
- **hero-toptilt 9.0** — albedo co-holder delivered; plan grammar strong;
  4.673 m² = the adjudicated projection-air family; residuals: cupola
  step (T3) and tarp strap split (T4) — BOTH documented optional declines
  (T3 abort-priced), read as furniture-depth hairs at 1×, not tier
  breaks (pt91m precedent: documented optional declines with a same-tier
  read do not hold the bar).
- **close-front 9.0** — C3/C5/C6 intact (muzzle mass, plate rhythm Δ5.1L/
  2.6R class, weld beads); T1 band visible-continuous at the wheel row;
  residuals ALL banked/certified: B6 capsule-vs-twin-drum (anchor-fenced,
  loudest here), M2 slew Δ+21.1 + mast verticals 92.1/90.8 @ y 3.29..3.76
  (certified corridor), casting-texture depth + simpler light clusters
  (the same texture-hair class view-front carried at 9.0 since r6).
- **close-roof 9.0** — receiver stepped + dappled; dome cast; MG sky
  window lawful (0.406); crown corridor luma parity 60.6/59.5; cover line
  9.2°/0.82 = the certified band (r6 7.2°/0.91 class); 127°/1.15
  lower-front = fender/glacis perspective corridor, byte-identical in the
  builder's committed-state eval (adjudicated non-defect); residuals: M2
  slab-stack grammar (certified mass band + priced hair), cupola step
  (T3 declined), rack-edge accents (delivered lane, in-class).

## Builder-claims audit (§D; r8 packet vs MY rig)

Every load-bearing number reproduced: gate line ×2 to the decimal, hash
70941de0 (109/103383), B2 triple (71.4 / 75.3 / 73.8) + cavity sub-45 0,
A1 quadruple (0 / 54.1 / 66.6 / 71.6), A2 72.2, T2 83.6 vs ref 78.5, N1
1.004 / 0.971, N5 11.56 / 79.4, N4 zeros ×3, top census 1620 / 1160, arcs
paired 1, S2 window zeros both quarters, 87.8°/0.502 carry exact, m46
99a3b0b4 (pre-drift) + graduates exact, clip 0/0, parent 0/0/0. C4 28.3 vs
claim 28.6 (0.3pp, egg-end sliver fill — improvement). The packet's
honest-residuals section is ACCURATE (procOnly 28 with its inventory —
independently decomposed and confirmed; the 87.8 carry; T3/T4 declines;
knob-on re-baseline). Both flagged-for-critic adjudications RESOLVED IN
THE BUILDER'S FAVOR on my own evidence. DISCIPLINE NOTES (minor, no
flag): (1) the §B5 yaw-90 pair was not re-run in the packet after a
geometry-moving round — my own run supplies it (PASS); write it next
time. (2) The packet's "sub-45 0" phrasing binds to the cavity window
(full-window is 19, improved from 23) — unambiguous in context, worth
exact wording. (3) S2's fr sub-0.4 twin (87.8°/0.361) went uncited —
outside the ordered window, honest scoping, logged here for the record.

## ORDERS

NONE — graduation. Post-graduation watch list (no score impact, for the
freeze record): cheek-lean inversion (rear, sub-0.28 m); rimBoost ring
regularity; gear↔hull r/g split 0.033; B6 twin-drum deflector
(anchor-fenced); C4 sliver 28.3 (anchor-fenced); B9 M2 slew (certified
band, optional); T3 cupola step / T4 strap split (declined-optional
polish); top-view dark-accent distribution. Any future edit takes the
§10 graduate-change flow (fix → gate hold → critic re-cert → re-freeze,
one commit).

## Verdict

**GRADUATION PASS** — floor 9.0, all 14 views at the bar, and no machine
gate broken anywhere: 90.5 PASS ×2 bit-identical on my watch (hull 90.5 /
whole 91.0 / turret 91.6 / stations 93.6 / dims 100 / floaters 100), clip
0/0, contig 0, parenting 0/0/0 with a live yaw-pair proof, trapezoid
exact, slopes clean, flood blue 0 across all 28 halves, §H.4 distinct in
both m46 states, hashes byte-stable through a SECOND live concurrent-
builder window (70941de0 before, after, and against the modified tree;
close-roof pixel-identical to the builder's committed evidence). The r6
structural driver died on proven machinery (arc paired at the ref's own
cheek roll), the albedo lane delivered the top-view floor, and every
remaining residual is named, certified, banked, declined-optional, or a
sub-visible polish hair. Same vehicle, same tier, every angle. DUAL GATE
MET — recommend §10 GRADUATION in this commit window: m47_patton, the
patton family's first graduate and the program's 15th. Hash-freeze from
70941de0 at 914bdb0 (+ 09eeafe knob state).
