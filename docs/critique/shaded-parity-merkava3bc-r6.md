# merkava3b + merkava3c shaded-parity r6 — independent critic (2026-08-02)

Gates hold (90.1/90.7, c89b26c). Verdict: BOTH FAIL — min 8.5 ALL NINE
VIEWS both tanks (fifth straight floor rise: 7.0→7.0→7.5/7.0→7.5/7.5→
8.0/8.0→8.5/8.5). Laws PASS (fill 0.803/0.807; rings round). Corner
flaps FIXED (med 63 vs 64; 3C tone 63 vs 70 trivia).

CANVAS: half-step — linework verified deleted, tone close (med 93 vs
97), but p75 94 vs 105 = no highlight modeling and NO FORM: straight
crown ledge, uniform-tone straight-edged "fold" polygons, near-straight
hems. Reads as paneled bulkhead with angular patches.
MG RODS: 4th claim-vs-render miss — 3B right shows one short/high rod
(w 21+16px at ytop~254 vs ref 268-275, w 22/34), 3B left/front/rear
ZERO, 3C ZERO everywhere.

## R7 work order

1. CANVAS FORM (not tone): UNDULATING crown edge (dips BELOW the
   certified cap line are free — undulate downward), rolled corner hem
   sweep, gradient shading across displaced fold geometry (p75 target
   ~105 = real lit crowns). The test: silhouette of the crown edge at
   dead-rear must wave, not rule.
2. MG RODS with MEASURED-RENDER PROOF: the DONE gate is a paste of
   measured rod spans from the rendered elevations (critic's method:
   contiguous above-roofline dark runs; ref = w 22-34px at ytop
   268-275). Both sides, both tanks, all four elevations where the ref
   shows them. Author rods long/low enough to survive 640px rendering;
   READ the render, don't assert geometry.
3. Roof/top contrast: near-black plates + white edge lines → ref's
   tone-on-tone (no <L35 on ref-fused surfaces — the abrams law).
Secondary: mantlet drape read (cloth-wrapped vs ring-stack), bustle box
ribs → cloth, track-bead scale, 3C flap tone 63→70.
