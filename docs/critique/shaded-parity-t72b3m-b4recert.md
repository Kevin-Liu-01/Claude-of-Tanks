# t72b3m §B4 graduate-change RE-CERT — independent critic verdict (2026-08-04)

Scope: re-cert on the changed views for the IN-TREE (uncommitted) russia.js
§B4 containment round (863/301 -> 0/0 --exact) + §B5 documented negative,
per BUILD-STANDARD §B4/§B5/§D and the graduate-change flow (GEOMETRY-GATE
§10 / rig standard §H.3). Graduate baseline: hash c19ec9f0, gate 91.8,
graduation critic r25 (9.0 all fourteen). NOT a fresh adjudication — the
r25 verdict stands for everything the diff did not touch; this verdict
certifies the touched zones against the graduation standard (>=9.0).

Rig: FRESH 14 pairs re-rendered by me (tools/tmp-tank-critic.mjs, zero
console errors) -> shots/critic-t72b3m/; visual-evaluator.mjs run per §D
(RIG PARITY OK, max yawProxy 1.8 deg @close-roof — the r25 top-view
degeneracy did not recur); graduation-era r25 renders preserved and used
as the before-image (scratchpad copy taken before overwrite). Measurement
tools: tmp-t72b3m-r25-critic.mjs (banked locks/orders/ring on the fresh
pairs) + tmp-t72b3m-b4recert-{skyline,diff,crops}.mjs (label-masked
skylines, independent before/after pixel diff, blue-signature enclosed-sky
census, zone crops). §B5 spot render: tmp-b5-shots.mjs --yaw=90 (degrees).

## Verdict: **RE-CERT PASS** — every changed view holds 9.0; §B4 reads
## correct; orchestrator may land russia.js + re-freeze 3d92bb98.

## Per-changed-view scores (graduation standard, >=9.0 bar)
| view | my diff vs grad-era (>8 lvl, proc half) | score | §B4-zone read |
|---|---|---|---|
| view-front | 11,648 px, box x73-566 y227-434 | **9.0** | glacis raft re-pitched inter-track (3 cols/side, seam grid re-pitched, hooks inboard) — the REF's own cassette-band class; window-A 168 px @x190-201 EXACT; masked skyline 4.31 == graduation per column |
| close-front | 2,593 px | **9.0** | bow at 2x: keeper plate + lifted prong read as a real fender box stepping over the idler; wrap slides clear beneath; no daylight, no float |
| close-roof | 1,270 px (bow corner in-frame) | **9.0** | bow-corner re-org clean; enclosed-air net −2 px (two pockets merged) |
| hero-frontleft | 1,231 px | **9.0** | bow re-org sub-visible at hero scale; zero enclosed sky |
| view-frontleft | 1,202 px (thin gear-band strips) | **9.0** | skirt/track interface identical at 3x; pockets unchanged |
| view-frontright | 1,201 px | **9.0** | mirror of frontleft, same read |
| view-left | 72 px | **9.0** | flap re-hung forward of the idler wrap, seated INTO the prong (mounted, floater law); trapezoid intact; stations 26.8 x5, discs separated, skyline 1.59 == bank |
| view-right | 72 px | **9.0** | mirror read; skyline 1.92 == bank; stations 26.8 x5 |
| view-rearleft | 115 px (sponson-corner sliver) | **9.0** | stern ramp/mudguard identical at 3x |
| view-rearright | 105 px | **9.0** | same |
| view-rear | 11 px | **9.0** | all rear locks byte-level: rooms 293/84+48/46/26 at exact r25 stations, band tone 84.3=84.3, sky rects 875/875 EMPTY x2 |
| view-top | 200 px | **9.0** | plan census 335262 vs ref 335296 (−0.010%; graduation +0.004% — the documented 48-px raft-re-pitch delta); all 12 enclosed comps pre-existing, two rear-crown pockets +2 px each (AA) |
| hero-rearright | 225 px | **9.0** | sky rects 2109/2109 EMPTY x2 offsets; pockets unchanged |
| hero-toptilt | 319 px | **9.0** | polar ring lock EXACT to r25 decimals: N/NE lids −77.5:83 / −65:83 / −62.5:81, S-arc minima 49-55, banked −55..−25 alternation intact |

## §B4 before/after read (the one sentence)
Before, the bow band visibly buried the mudflap plane and the outer raft
cassettes inside the idler-wrap annulus (the 863/301 interpenetration
class); after, the flap hangs at the fender front seated into the lifted
prong, the ramp rises clean to the raised idler beneath it, and the raft
sits inter-track exactly where the reference carries its own cassette
band — contained at zero voxels with no new daylight anywhere.

## §B4 containment evidence
- track-clip-audit --exact on current bytes: **front 0 / rear 0** (my run).
- Independent 14-view before/after diff (my fresh renders vs the r25
  renders I archived): every >8-level delta box sits in the bow/stern
  gear zones (table above) — zone claims in the packet REPRODUCED; the
  remainder is the disclosed ±1-2-level coplanar re-sort (invisible; my
  >8 counts run BELOW the builder's own).
- NO NEW AIR GAPS (the re-hang risk): blue-signature enclosed-sky census
  (bg maxch<=13 AND B−R>=+8, §D) per view, fresh vs graduation-era —
  every enclosed component pre-exists at the same coordinates; the only
  deltas anywhere: two rear wrap-crown plan pockets +2 px each (64->66,
  58->60, symmetric AA), hero-toptilt pocket 150->138 (shrunk),
  close-roof 29+7 -> 34 (merged, net −2). Nothing new opened where the
  interpenetration was.
- Mudflaps read MOUNTED both sides (seat into the prong body; no gap at
  4x; connectivity was the builder's floater-law fix and the renders
  agree).
- §B6 trapezoid intact both sides: short ground base, cleated ramps
  rising to a raised idler AND raised sprocket (L/R full-run crops).
- §B1 front slopes untouched: masked skylines fresh == graduation PER
  COLUMN (4.31 / 5.61 / 1.59 / 1.92 — the r25 decimals), incl. the known
  x128-130 tower-depth residual, unchanged.

## §B2 sky adjudication (one new instrument finding, cleared)
visual-evaluator flagged one proc top-view enclosed void 0.023 m^2 @world
(1.35, 1.09, 2.03). Adjudication: that is the plan slot between the
re-hung flap (z 1.80, top 1.11) and the fender/prong content above it —
BEYOND the hull front (zMax 1.749), i.e. a between-gear-elements pocket,
not sky through hull or deck. It does not read at 1x or 4x in the
official pairs; my blue-signature census on the critic pairs finds NO
enclosed component there in either era; standard-check's §B2 top-down
hole scan is byte-identical to the graduation baseline (same 3 r22
rail-channel clusters 21c/21c/17c, contig 167). Same instrument-artifact
tier as the r25 evaluator voids. Watch item, non-blocking.

## §B5 documented-negative spot-check (PASS)
Fresh yaw-90 render (official tmp-b5-shots harness — NOTE it takes
DEGREES; --yaw=90): the turret assembly (dome, Relikt ring+lids, tower,
NSVT, bustle boxes, wings, bins) rotates as ONE unit; the deck pile, rear
slat cage and engine deck stay hull-aligned; nothing strands, nothing
sweeps mid-air. That is CORRECT per the packet's adjudication (deck cargo
is hull-parented on the real vehicle; ORACLE-REGISTRATION-PINNED, m1a2
precedent). My yaw-90 renders match the builder's yaw90-after proofs at
**0 px >8** across sampled views — the shots/russia-b5-t72b3m/ proof set
is authentic on current bytes. turret-parent-audit 0/0/0 (my run).

## Rig integrity (all my runs, current uncommitted bytes)
- geometry-gate x2: **min 91.8 PASS both**, components
  92.3/92.1/91.8/94.5/100/100 — IDENTICAL to the graduation ledger row to
  the decimal, twice; floaters 100 x2.
- Hash **3d92bb98 (122 meshes / 205516 verts)** measured before AND after
  my full render battery — stable. pt91m **e6994e54** (52/100624) and t84
  **531fe4f0** (47/84292) spot-verified intact in the same runs.
- standard-check: clip 0/0 PASS; holes/contig byte-identical to baseline;
  mg0+0d = the standing hand-authored-NSVT packet justification.
- Banked-lock sweep (r25 rig on fresh pairs): top census, khaki 0, four
  barrel-sky rects EMPTY, ten stations min 26.8 TO THE DECIMAL, disc
  separations at 803/840/888/932/977, rear rooms, window-A/B, band tone
  84.3/84.3 rowStd 2.22/0.53, MG receiver row 198 max 73 / body p10 20.6
  — every number reproduces r25.

## Rig notes for the file (not model defects)
- tmp-t72b3m-r25-critic.mjs's LOCAL skyline()/skyDiffBand() do NOT mask
  the y<=24 corner labels — its raw whole-view skyline and fender prints
  are label-contaminated (meanAbs ~10, fake −180..−192 cols at x98-119).
  The r25 verdict numbers are masked; reproduce them with a y>=25 scan
  start (tmp-t72b3m-b4recert-skyline.mjs) — masked, fresh == graduation
  per column.
- tmp-b5-shots.mjs --yaw is DEGREES (--yaw=1.5708 is a ~rest pose, and
  its 14-view set diffs ~35k px against a true yaw-90 — do not misread
  that as a proof mismatch).

## Residual ledger delta vs graduation
Carried forward UNCHANGED (measured, not assumed): all eight r25 ledger
items (ring-crown hero read, window-B cap, x128-130 tower depth, x241-262
crate-step, x181 notch, rear rowStd ripple, front-air 0.90x composition,
upper-glacis V-tier). NEW watch items: (a) evaluator top-view flap-slot
void (adjudicated above); (b) rear wrap-crown plan pockets +2 px/side.
Nothing floor-holding.

**RE-CERT PASS — the §B4 graduate-change is certified: land russia.js and
re-freeze t72b3m at 3d92bb98 (122/205516) in the same commit.**
