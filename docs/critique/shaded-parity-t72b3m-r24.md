# t72b3m shaded-parity r24 — independent critic verdict (2026-08-03)

Rig: FRESH pairs re-rendered from the 9cb6614 tree at 10:28 (zero console
errors); scored only shots/critic-t72b3m/*.png per CRITIC-PAIRS LAW.
ITU-601; bg |px-0x151b20| maxch<=13; enclosed-air flood census; polar
tangential ring scans; skyline diff profiles. Both packet sections exist
(r23 reconstruction + r24) — the r12 process order is satisfied.

## Verdict: FAIL — min 8.5; **FLOOR UP 8.0 -> 8.5 (eighth rise, biggest
floor jump of the grind); avg 8.50 -> 8.61. THREE views now at 9.0:
view-top HOLDS its bank, VIEW-LEFT and VIEW-RIGHT cross and BANK.**

Per-view: front 8.5 · frontleft 8.5 · **left 9.0 (BANK)** · rearleft 8.5
· rear 8.5 · rearright 8.5 · **right 9.0 (BANK)** · frontright 8.5 ·
**top 9.0 (bank holds)** · hero-fl 8.5 · hero-rr 8.5 · hero-toptilt 8.5
· close-front 8.5 · close-roof 8.5.

## CLAIMS AUDIT — cleanest round of the program: 10 exact, 2 partial
(probe-definition class, same-direction), 0 flipped, 0 frame-shopped.
Every number below re-measured by me on the fresh pairs:
- view-top bg census proc 335310 / ref 335296 (Δ0.004%) — EXACT to the
  packet's own pixels; banked view undisturbed; fin adds 0 top px.
- window-A **168 px @ x190-201 EXACT** (ref 164 @ x190-201); front air
  total 790 vs ref 806 = 0.98x — the r11 fill order is fully landed.
- BOTH barrel-sky rects 100% bg incl +11-row shifts (view-rear 875/875
  x2, hero-rr 2109/2109 x2) — exact.
- Ten wheel stations min-luma **26.8 TO THE DECIMAL** (L x802/841/888/
  932/979, R x941/983/1030/1072/1117 pair coords; 60-74 px of the class
  per station window). Left disc row **six SEPARATED runs** 36/31/39/37/
  38/34 px @ th55 y371 — the r22 merged pair is GONE; stations match the
  ref's true-bg gaps (ref y362: x160/205/249/298/342) within ~5 px.
- Rear band rows 264-296: proc ONE wall mean 73.9 / rowStd 1.88 vs ref
  84.3 / 0.53 (my rect x850-1150; packet 72.4/2.48 vs 81.7/0.48 — same
  class). The r23 double-wall 56/58 dips (std ~8) are dead. Tone Δ10
  residual as disclosed; the row-284 seam does not survive 300-col row
  means (narrower than disclosed).
- khaki zero: 0 warm px (R>G) of 16328 in the top turret zone. Plateau
  sub-45 census 4 px vs ref 1 (claimed ~0) — patch contrast at ref class.
- Petal ring, 16-pt E-arc tangential (my polar rig, proc c1013,358
  sy0.80 r40-72): lids 84-104 vs notches ~62-70 on image -55..-25 — the
  claimed alternation amplitude class IS there. PARTIAL: image -75..-59
  (vehicle N/NE arc) max 62-81 vs ref 87-100 — no lit lids on that third
  of the arc (ref keeps petals; proc reads fused there).
- Rear rooms: window-A **84 px at the ref's exact x station** (x438-449;
  ref 164) — exact as claimed; window-B measured 46 px @ x362-366 vs
  claimed 70 and vs ref 217 @ x378-391 (14 cols west of ref station);
  corner room 296 px @ x418-450 (claimed 295). Rail-slot 26.8-class at
  tilt: 104 px located (left run + right corner) vs the packet's 181 —
  probe-def difference; the line READ exists, the ref's wide black rooms
  remain ref-only (disclosed residual d).
- Skylines: view-left mean |Δ| 1.6 px (flags only in the disclosed
  x261-307 dash/ghost rhythm band, 1-3-col spikes); view-right mean 1.9
  (crest rod rhythm x333-377, aft-step 3 cols @ x500 Δ19 — the disclosed
  floor); view-front mean 5.7; view-rear mean 7.2.

## LAWS: NO-EMPTY-AREAS PASS (top filled, turret contiguous, mosaic
present) · CIRCULARITY PASS (six discrete discs both rows, stations
exact) · FILL PASS-BY-TRADE (front 0.98x; rear rooms at ref stations but
0.33x at the window pair — front-window lock documented as the bound) ·
DECORATION/MG PASS-WITH-FLAG: the NSVT reads by receiver mass + sky
silhouette + angled barrel (view-rear pair x1024-1042 y197-242), but the
receiver renders as the darkest slab on the rear silhouette — pale
top-lit edges are the missing third of MG PHYSICS (system untouched
since it passed r9; flag, not a floor-holder).

## Why the 8.5 wall (what separates every 8.5 from 9.0)
ONE structural family + three local items:
1. RING RELIEF DEPTH (holds hero-rr, hero-toptilt, close-roof, and
   echoes into front/diagonals as the smooth pale crown sweep): the
   alternation class landed, but (a) the N/NE arc has no lit lids
   (numbers above), (b) notch shadows on the S arc are one tone too
   shallow, (c) the ref crown stands prouder (disclosed, budget-bound).
2. view-rear: window-B room off-station and half the claimed size; band
   tone Δ10; left shoulder line rides +13-15 px for ~80 cols (x181-260,
   spike -42 @ x181).
3. view-front: left fender stack +19-22 px for 25 cols (x101-125) then
   -20-22 (x128-130); glacis texture plainer than ref at close scale.
4. Rear-quarter boxiness: rack slab faces (hero-rr x1150-1250 y330-420)
   and front fender corner slabs (both front diagonals).

## R25 order
[STRUCTURE — one course, four views]
1. N/NE RING LIDS + S-ARC NOTCH TONE: extend the petal course so polar
   image-angles -90..-59 (proc c1013,358) gain 2-3 lids >=80 luma with
   <=70 notches; deepen S-arc (+35..+90) dark flanks one class. Height
   is budget-locked — deliver by CONTRAST, the -55..-25 recipe already
   proves it.
[LOCAL]
2. view-rear: slide window-B room to x375-390 (ref station) and toward
   70 px if the hood permits; +8-10 luma bucket on the band-pile rear
   faces; shave the x181-260 shoulder run ~10 cm.
3. view-front: align the left fender stack top to ref ±6 px at x101-130.
4. MG PHYSICS third clause: receiver TOP face + barrel top edge to the
   70-85 lit class (material bucket, zero geometry).
5. Rack slab seam+tone break (hero-rr); front fender corner soften
   (frontleft/frontright); one more V-course tier on the upper glacis if
   any certified row allows (close-front).
BANKED — do not relitigate, verify only: view-top census parity, ten
26.8 stations, disc separation both rows, window-A 168, sky rects,
khaki zero, view-left/view-right skylines (means 1.6/1.9).
