# m1a2_sepv2 — INDEPENDENT GRADUATION ADJUDICATION (2026-08-05)

Graduation critic verdict for the program's 25th candidate: the r10
family-rig rebuild (buildM1a2 param delta, worksHull registration truth)
plus the §B3 box-cleanup round (owner directive: "random boxes that are
not ERAs around armor and especially guns"). State certified: the
UNCOMMITTED abrams.js working tree at proc hash **b489ba14**.

## HEADLINE: **GRADUATION PASS (dual gate met at b489ba14 → orchestrator
## runs §10; the program's 25th)** — gate min 91.3 PASS ×2 identical,
## verified by this critic; all FOURTEEN views ≥ 9.0 (floor 9.0, mean
## 9.11) as the same M1A2 SEPv2, same tier. The owner's §B3 class is
## dead at 1x-5x: the gun root reads an armored sleeve housing with
## rounded crowns, clamp collars and an exit boot — no bare cuboid
## anywhere near the gun, the mantlet, or the armor faces. §H.4 tells
## (cable / CIPs / crate / twin fifties) all verified distinct against
## m1a2/m1a1/m1a1ha. §B5 stranded-2 = exactly the two certified
## oracle-registration-pinned classes, nothing more, and my own
## rest/yaw-90 pair proves the rotating set rotates while the works
## field honestly mirrors the print's own rig.

## Provenance (§D discipline — everything below measured by this critic)

- Source audit: the `sep`-gated §B3 hunks read in the working diff
  (crown-pair recipe on D1/D1-step/D2 + D-left-corner + E band ×2 edges,
  clamp collars at z 2.49/3.06 with dark tension-bolt segments, gunDark
  exit boot r 0.10 @ world z 3.878..3.928, wind sensor head/lens/mast/
  collar/base swap, bow shoe stacks with pad plates + horn dots to
  x 1.4185, rail-bin lid seams/latches/hinges 6 mm x-proud). m1a2 keeps
  the original authors byte-identical (frozen f3c34424, hash-verified).
- Hash bracket (tools/tmp-critic-hashgeo.mjs, before AND after all
  renders): **b489ba14 (46 meshes / 116696 verts)** both times. Frozen
  siblings byte-exact both checks: m1a2 f3c34424, m1a1 97c10194,
  m1a1ha f5c556dc, m1a2_tejas 3fcae440, m1a2_tusk f7ecade4.
- GATE ×2 (runs consecutive, same command as the m1a1ha lane): **min
  91.3 | hull 93.1 whole 92.4 turret 91.3 stations 93.4 dims 100
  floaters 100 PASS** — both runs identical, = the builder's claim to
  the decimal. Every component ≥ 90; every component ≥ the m1a2's own
  91.0 graduation-family row.
- Pairs rendered FRESH by this critic (tmp-b1b3-critic-batch.mjs, one
  FIFO ticket, official path): 14/14, zero console errors; scored ONLY
  shots/critic-m1a2_sepv2/*.png. My set is **byte-identical 14/14** to
  the builder's evidence — deterministic pipeline, honest builder shots.
- `node tools/visual-evaluator.mjs --id=m1a2_sepv2` (camoSeed 4242):
  **RIG PARITY OK** — max yawProxy 1.28° @front, max |dCentroid|
  0.066 m, no flips (the old 0.57 m misregistration class confirmed
  dead). Flagged profile rows are the documented carry classes
  (corner-handover Δ at vertical edges + short re-segmented contours).
  Report + overlays: shots/visual-eval-m1a2_sepv2/ (this run; builder's
  17:08 run archived to critic scratchpad first).
- `track-clip-audit --exact`: **front 0 / rear 0**. `standard-check`:
  gateMin 91.3, clip 0/0 ✓, contig 0 ✓, decor mg1+1d ✓.
- `turret-parent-audit`: **stranded 2 / abutting 0 / dangling 0** — the
  two hits are (unnamed)(45%) works-cloth bucket + (unnamed)(26%)
  hullDetail: byte-same percentages as the r10 certification of the
  ORACLE-REGISTRATION-PINNED class (BUILD-STANDARD §B5 names the m1a2
  works field as canonical; userdrops5 keeps all ten works nodes
  hull-side, so proc re-parents would break parity). NOTHING beyond the
  two certified classes. Normalize plan stays with the orchestrator
  (coupled followers-extension + worksHull:false flip, one landing).
- §B5 semantics verified independently at the CURRENT hash: my own
  rest/yaw-90 14-view pairs (tools/tmp-b5-shots.mjs →
  shots/critic-sepv2-gradyaw/{rest,yaw90}) — at yaw 90 the shell, bustle
  rack, crate, CROWS, CIP panels rotate with the ring; the works field
  stays hull-side like the print's own rig; nothing sweeps mid-air,
  nothing drags the deck. (The builder's r10 yaw evidence at
  shots/abrams-sepv2-r10/ predates the §B3 round — hash d98bf39a — so
  the current-state §B5 doc pair is MINE; floaters 100 ×2 covers pose
  separation.)

## §B3 — the point of the round (owner: "random boxes ... especially
## around guns"), verified at 2-5x on the official pairs

- GUN-ROOT D/E BAND: reads as the M256's armored sleeve housing — top
  edges roll over rounded crowns (highlight arcs visible at 4x, no razor
  box corners), the two clamp-collar stations read as flank plates with
  the dark tension-bolt segment, and the tube EXITS THROUGH A BOOT
  COLLAR (dead-ahead view: concentric dark ring around the tube base —
  a collar, not a wall). No bare stacked rectangles from any of the 14
  angles.
- WIND POST: reads as the driver's wind sensor — head plate with dark
  lens slot on a slim dark mast with base bracket (view-front 5x +
  view-top: head + bracket visible from above next to the periscope
  ring).
- BOW SHOE STACKS: read as stowed track shoes — pad-plate segmentation +
  pale guide-horn dots on the outer faces (view-left 4x, both bow
  corners in the heroes).
- RAIL BIN LIDS: sponson boxes carry lid seam lines + latch blocks +
  hinge dots (view-left/right 4x; the §B3 bin grammar).
- ADJUDICATED KEEP confirmed: the close-front dark pocket between the
  cheek tips and the D1 start is the ref-endorsed root shadow pocket
  (present in the ref half at the same station) — shadow, not a box.

## §B2 flood (maxch ≤13 AND B−R ≥ +8; label letter-holes excluded)

PROC true enclosed-sky is **0 px** in view-front, frontleft, rearleft,
rear, rearright, frontright, hero-toptilt, close-front, close-roof;
~12-18 px in the heroes (certified rack-gap class; ref's own heroes
carry 245/274 px); 13 px in view-top (ref 375). The only substantive
proc regions are the certified side-view classes: view-left 187 px
@x323-343 y258-267 (works-rail lane pocket) + 117 px @x394-414 y303-311
(under-sponson slit), view-right 277 + 114 px mirrored — same stations
as the graduated m1a2's certified 315-652 px reads, and the ref carries
the same classes at its own stations. NO enclosed-air violation.

## Per-view scores (all fourteen, graduation bar ≥9.0 every view,
## chieftain5-anchor calibration)

| view | score | named reads |
|---|---|---|
| view-front | **9.2** | Exit boot + rounded band crowns dead-ahead; wind sensor reads instrument; skirt-front hanger trusses vs ref's slotted plates = carried family class; flood 0. |
| view-frontleft | **9.1** | §B1 cheek rake; CIP panel tell on the flank; bow shoes; certified corner-handover evaluator rows at z −2.15..−2.17 carry. |
| view-left | **9.1** | Rail-bin lid grammar; works field massing; §B6 trapezoid (raised idler + raised toothed sprocket); certified lane-pocket/slit stations only. |
| view-rearleft | **9.1** | Corner contained; louver grille parity; BIN-EXTENT rear-flank dressing void carries (priced residual). |
| view-rear | **9.1** | Louvered doors + works crates + skirt tails read ref-true; flood 0. |
| view-rearright | **9.1** | Mirror; sprocket wrap honest; contained. |
| view-right | **9.1** | Bins mirrored; certified pocket stations only; trapezoid holds. |
| view-frontright | **9.0** | Sun-quarter disc-pop residual (m1a2-family certified polish class) carries; §B3 items clean. |
| view-top | **9.2** | Cable reads as a draped clamped arc; crate lid slats + strap (box-vs-round through the rail windows); works lids; plan instrument clean; flood 13 px. |
| hero-frontleft | **9.1** | Slope motivates the mass (glacis drives the bow volume); CIPs + shoes read; 18 px = certified rack-gap class. |
| hero-rearright | **9.1** | Works field reads dense crated stowage vs the ref's soft cloth pile (certified roundness residual); corners contained; 12 px. |
| hero-toptilt | **9.1** | Filled decks; CDR/loader drums + hatch rings circular; crate/rail band attached; no voids under perspective. |
| close-front | **9.2** | The §B3 money view: sleeve band + collars + boot at 4x; shoes with horn dots; glacis one-line; zero bare cuboids. |
| close-roof | **9.0** | Twin fifties (loader M2 with spade grips + AA ring; CROWS M2 forward), crate, rail band; rail-window strip reads stark at this one angle but as rack rail + posts (certified); drums circular. |

Floor **9.0**, mean **9.11**. Every view same-vehicle, same-tier.

## §H.4 variant distinctness (four-up, fresh proc pixels)

m1a1: clean-green scheme, bare-roof M2, hull tow cable line, open rack.
m1a1ha: red-brown two-tone, stowed shielded M2 + spare-links strip.
m1a2: green-dominant, CROWS post, works field + dressed sponsons, wavy
deep skirts. m1a2_sepv2: heavy red-brown streak scheme + CIP panels on
the forward cheeks + rigid slatted crate in the bustle + draped tow
cable on the forward deck + twin fifties. No 'same tank re-badged' read
at a garage glance; every tell verified in the pairs (cable/crate from
top, CIPs from sides/heroes, twin M2 from close-roof).

## Honest residuals (carried, priced, none blocking)

- The m1a2 certified carry classes transfer verbatim: works-band
  side_whole columns (glsaa_8/CDR class), corner-handover evaluator
  rows, BIN-EXTENT rear-flank void, plan tail-notch bins, frontright
  disc pop, works-field boxy-vs-cloth roundness at heroes.
- §B5 stranded-2 stands BY CERTIFICATION until the orchestrator lands
  the coupled registration+flag round (followers extension + one-flag
  worksHull flip, re-gate + re-cert in that landing).
- CITV/GPS hoods remain law-blocked (r3 roof-recess 2.4275 lid ceiling)
  — documented, not §B3 misses.
- The graduated m1a2 shows the same §B3 1x classes sep just fixed (bare
  D/E band, square wind post, bare shoe stacks, bare rail boxes) —
  REPORTED for a scheduled m1a2 graduate round per the report-don't-force
  clause; evidence at shots/abrams-b3/ tint set.

## Verdict

**GRADUATION PASS (dual gate met at b489ba14 → orchestrator runs §10;
the program's 25th).** Land abrams.js + the m1a2_sepv2.md packet in ONE
commit; §10 retires the registration into the THREE override maps
(procedural-fidelity LOCAL_REFERENCE_OVERRIDES + tmp-tank-critic
CRITIC_REFERENCE_OVERRIDES (local tmp) + visual-evaluator-page
CRITIC_REFERENCE_OVERRIDES) and FREEZES m1a2_sepv2 at **b489ba14**
(46 meshes / 116696 verts). Any geometry edit before landing
invalidates this verdict. Independent critic: abrams-wave critic,
2026-08-05.
