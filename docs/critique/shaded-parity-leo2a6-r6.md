# leo2a6 shaded-parity r6 — independent critic (2026-08-02)

Gate 91.0 gatePassed (a3c2844). Verdict: FAIL — min 8.5, with EIGHT
views at 9.0 (front UNBLOCKED: wrap inversion dead, residual
sub-spottable at native exposure). view-rear alone at 8.5 — purely
STRUCTURAL now (all tone work landed: separator ratio ~0.85 of ref
family, cells bin-green, band strip one material family, p10 floors
matched exactly).

Laws hold: 0-3px enclosed bg on scored views/heroes; mirrorFix
confirmed; fan circles clean. New closeup-only class: behind-wheel bg
wedges (close-roof 324px, hero-rearright 51px) — sponson plane too
short behind wheels 2-4; sky-through-hull at near-ground cameras.

SAMPLING LESSON (r5's, recurring): sample the BANK, not the slat — the
separator spec was met on-element while the ref's grille is TWICE the
band extent.

## R7 work order — two rear geometry moves + one hero patch

1. GRILLE BANK EXTENT: extend the louver bank to fill the deck face
   (~13-14 rows at the current 6px pitch + landed separator contrast);
   kill the blank apron below; replace faint dotted fan rings with bold
   circular fan grilles; delete the invented central X-cross bracket.
2. TURRET REAR WALL: replace the full-width cell-lattice read with a
   mostly-SOLID wall carrying two dark stowage panels (tone family
   already correct; ref = solid wall + 2 panels).
3. HERO PATCH: extend the sponson/hull-side plane down behind wheels
   2-4 (kills the behind-wheel sky wedges; interior geometry).
All three are geometry — RE-GATE after each (91.0 must hold
gatePassed); the rear-face additions live interior to the rear
silhouette but prove it, don't assume.
Trivia (optional): under-fender shadow toward 0.92x, tan X-marks at
wrap joint, wheel-face hue.
