# m26_pershing — INDEPENDENT GRADUATION ADJUDICATION r2 (2026-08-05)

Second-sitting graduation critic verdict for the program's 26th candidate:
the r4 blocker retune (loft.smooth + deck-slat crowns, both r1 orders +
the optional polish lane) at candidate hash **2f579de8**, commit c28096f.
Fresh adversarial adjudication per §J critic-lane laws; chieftain5-anchor
severity; baseline = the r1 FAIL verdict
(docs/critique/shaded-parity-m26-graduation.md, floor 8.8 at f348ecd5);
m46 r10 / m47 r8 are the family calibration set (same slab→smooth lineage,
both graduated 9.0 ×14 after the same class of fix).

## HEADLINE: **GRADUATION PASS (dual gate met at 2f579de8 → orchestrator
## runs §10; the program's 26th)** — floor **9.0**, all fourteen views.
## Both r1 orders are delivered and verified on the official rigs: the
## casting shades as ONE cast roll in every casting-dominant view (the
## slab-loft facet patchwork is dead — evaluator corroboration: view-front
## 0 flagged edges vs r1's Δ−14.8° shield-crown signature), and the deck
## carries the ref's own louvre grammar row-for-row at its own stations
## (my per-row scan: proc Bank B crests at the ref's exact y rows, p75
## 66.0–66.8 vs ref 65.7–67.4). Zero new sky, zero silhouette motion,
## every certified residual re-measured EXACT.

## Provenance (§D discipline — everything below measured by this critic)

- State: HEAD c28096f; src/vehicles/profiles/patton.js byte-clean vs HEAD
  (git diff empty). Foreign WIP confined to the leopard lane (leopard.js,
  leo2_revolution.json, ledger churn — another agent's live round, never
  touched, never gated by me); the hash bracket proves it cannot reach
  m26 or the sibs.
- Hash bracket (`tmp-hashgeo`, BEFORE and AFTER all renders):
  **m26_pershing 2f579de8 (46 meshes / 72532 verts)** both runs = the
  packet's candidate hash exact. Frozen sibs byte-exact both runs at the
  9bf2a6d registry: m45 31914c3f, m46 90ebf864, m47 53b6123a, m60a1
  fbf9f4cc, m60a3 051c454c (tool prints 51c454c unpadded).
- GATE ×2 (official geometry-gate.mjs, consecutive): **min 90.5 | hull
  93.1 whole 90.5 turret 94.2 stations 90.6 dims 100 floaters 100 PASS —
  both lines EXACT** = the packet claim. I saw the 90.5 side of the
  banked 90.4↔90.5 whole-row flicker twin on both runs. Reproducibility
  proof: my runs re-wrote docs/geometry-gate/m26_pershing.json and git
  reports it byte-identical to the committed r4 landing.
- Dims rows measured: heightM 3.09/3.08 (+0.36%), hullLengthM 6.34
  (+0.19%), overallLengthM 8.66 (+0.12%), widthM 3.51 (+0.05%).
- `track-clip-audit --exact`: **band 0/0 AND shoe 0/0**, no blind spot.
- `turret-parent-audit`: **stranded 0 / abutting 0 / dangling 0.**
- `tank-standard-check`: gateMin 90.5, clip 0/0 ✓, **contig 0 ✓, decor
  mg1+0d ✓** (stowed FITTINGS 'mag' census; measured m2Station stays the
  gate-driven roof gun — the §I packet justification carries from r1).
- `visual-evaluator` (camoSeed 4242): **RIG PARITY OK — max yawProxy
  0.9° @frontright, max |dCentroid| 0.039 m, no flips** → scoring valid.
  shots/visual-eval-m26_pershing/ (my run).
- Pairs rendered FRESH by this critic on the official path
  (tmp-tank-critic.mjs, self-ticketed FIFO): 14/14 pairs, zero console
  errors. Scored ONLY shots/critic-m26_pershing/*.png (mine). The
  builder's r4 pairs were archived to critic scratchpad before
  overwrite; the r1 before-pairs (f348ecd5) used for A/B live at the r4
  builder archive (scratchpad m26r4/before-pairs — sha-stamped in my
  session log).
- §J yaw-pair re-rendered at the verdict hash (tmp-b5-shots →
  shots/critic-m26-gradyaw/{rest,yaw90}, r1 sets archived): at yaw 90
  the casting, M2 + cradle + cans, bustle rack, cupola + loader pods,
  stow roll, antenna pot AND the new brake window bars rotate with the
  ring; dash arrays, Banks A+B, pintle kit, hoods, caps, grille, lights,
  shackles, flaps stay hull-side; nothing sweeps mid-air, nothing drags
  the deck. Floaters 100 ×2 in-gate covers pose separation.

## Order delivery verification (the r1 blockers, measured)

1. **loft.smooth (SMOOTHLOFT-hwL PARITY fix)** — VERIFIED DELIVERED.
   A/B at identical framing vs the r1 before-pairs: the flat-quad
   patchwork (front face / 2–3 cheek planes / shoulder step / crown +
   the bustle-flank vertical banding) shades as ONE continuous cast
   roll at close-roof, both quarters, both heroes, head-on and both
   rear quarters. §D corroboration: view-front now reads **0 flagged
   edges (worst Δ0.0°)** where the r1 run carried the shield-crown
   Δ−14.8° facet signature; close-roof's 3 flags are Δ≤1.9°
   (sub-2° hairs). Silhouette identity proven twice over: gate lines
   EXACT ×2 and the gate JSON byte-reproduces the committed landing —
   the smooth re-emit moved shading only. The left cupola-flank bulge
   (the hwL lesson) is present and shades inside the roll.
2. **Deck-slat crowns at the ref's own array stations** — VERIFIED
   DELIVERED, my own §D numbers (view-top pair, ITU-601 per-row p75
   peaks, full-pair coords): REF Bank B crest rows at y 99/104/109/114/
   119 (5 px pitch = 0.0795 m), p75 65.7–67.4 over field ~53.7; PROC
   crest rows at THE SAME y stations, p75 66.0–66.8 over field ~50.1 —
   row-for-row station match, tone IN the ref band. Engine-bay dash
   arrays: REF crest rows p75 66.7–73.7 over field 56.0; PROC 62.6–67.2
   over field 53.8 — in-band, a hair dim of the ref's brightest rows
   (named residual below). Grammar verified on the renders: mirrored
   five-band dash blocks, Bank B full-width rows, Bank A centre rows,
   the ref's plain zones honestly bare; rows read at top, toptilt, and
   both rear quarters (the dead-astern grazing loss is the packet's
   named residual).
3. Optional polish (both landed, verified): (a) pintle-bracket tell on
   the rear plate (backing plate + latch + posts + chain-eye dots vs
   the before's bare cylinder patch — legible at 3×, mask-neutral);
   (b) brake window-contrast bars (6× A/B: dark transverse window
   reads between the two proud rings exactly where the ref's notch
   cuts read; drum/ring silhouette unchanged; muzzle-collar residual
   0.063 ×1 col re-measured EXACT).

## §B2 flood (maxch ≤13 AND B−R ≥ +8; §J label band excluded;
## tools/tmp-m26grad-flood.py on MY fresh pairs)

**14/14 views byte-match the r1 adjudicated-legal table** — front 536 /
fl 97 / fr 89 / left 119 / right 142 / rl 41 / rear 213 / rr 45 / top 8
/ hero-fl 140 / hero-rr 155 / toptilt 36 / close-front 58 / close-roof
1334; REF halves 0 except their own wheel-pocket classes (left 427,
right 435, close-roof 277, close-front 3). The retune added ZERO new
sky; every class carries its r1 adjudication (under-sponson corridors,
M2-underside daylight, empty-rack window, wheel pockets, tail notch,
idler-wrap containment air). No through-hull, through-turret, or
enclosed-interior sky in any view.

## Per-view scores (all fourteen, graduation bar ≥9.0 every view,
## chieftain5-anchor severity, deltas named vs the r1 table)

| view | r1 | r2 | named reads |
|---|---|---|---|
| view-front | 8.9 | **9.0** | The r1 holder (welded-wedge cheek creases) is DEAD — cheeks roll into the crown continuously; evaluator 0 flagged edges (r1: shield-crown Δ−14.8°). Remaining reads are certified classes: recessed-shield config (rotor-lane trade, turret_plan @0.83 = 0.119 re-measured), M2 crown strip +0.239 ×1 col, legal floods. Bow plate + MG square + eyes + guards read. |
| view-frontleft | 8.9 | **9.0** | Quarter-pitch facet mosaic fused into one roll (A/B verified). Fender-line flatness Δ+8.6° ×0.81 m persists = the r1 certified silhouette class priced inside hull 93.1, unchanged bytes. Casting-skirt/M2 edge flags (Δ−13.9°/−13.4°) decode to certified contour classes on a silhouette proven byte-identical. |
| view-left | 9.0 | **9.0** | HELD. §B6 trapezoid + tension idler + 5 rollers + duckbills + M2 two-band split + honest rack window; NEW brake window bar reads as the ref's dark notch (6× A/B); certified turret_side residuals at their stations re-measured exact. |
| view-rearleft | 8.9 | **9.0** | Bustle-flank facets fused; Bank rows READ on the ramp at quarter pitch (crop-verified); tail tiers + duckbills + contained corner; rack rails attached. |
| view-rear | 8.9 | **9.0** | Bustle rolls as one casting; pintle-bracket tell delivered on the plate (r1's named lack); bolt rows + grille + shelf + tail stack honest; floods legal. Residuals: X-braces (deferred — architecture residual, below), dead-astern grazing hides the Bank tone rows (~4.6° class), hinge-clutter delta. Evaluator's Δ−14.5° ×0.36 m @x0.8..1.1 y2.39 = the dome-shoulder contour class (certified print-class panel count, shading now continuous). |
| view-rearright | 8.9 | **9.0** | Mirror of rearleft; flap = darkGearFit family bucket; sprocket wrap honest. |
| view-right | 9.0 | **9.0** | HELD. As left mirrored; shelf pods flush casting steps; antenna pot at dome-rear right; brake bars ✓. |
| view-frontright | 8.9 | **9.0** | Mirror of frontleft; facet family dead; Δtop +0.724 @z −1.14 re-prints as the labeled M2-cliff offset class (not a height error); fender-line mirror class certified. |
| view-top | 8.9 | **9.0** | The r1 holder (deck-slat crown gap) is closed with the ref's own grammar AT its stations: Bank A centre rows + Bank B full-width band + mirrored engine dash blocks + honest plain zones (my per-row scan above); plan registration 0.1°; tail ladder + taper + brake read; 8 px flood. Residuals: fender cleat runs (ref's L/C marks) absent over the certified notched-rail fender architecture; Bank A a hair hot vs its ref rows; X-braces below. m46-r10 calibration: grammar-at-stations with named density hairs graduated at 9.0. |
| hero-frontleft | 8.9 | **9.0** | Facet family dead at hero pitch — casting one roll over the furniture-rich glacis; slope-motivates-mass reads hold; floods legal. |
| hero-rearright | 8.9 | **9.0** | Dead facets + the full deck program at hero pitch (dash arrays + both Banks read); rack window honest; tail contained; dome-shoulder arc r0.213 remains a certified CONTOUR class — its shading now rolls, the panel count is the print-class residual. |
| hero-toptilt | 8.8 | **9.0** | JOINT r1 FLOOR — BOTH holders delivered: the deck density gap is closed (before/after A/B: bare ramp bands → the ref's louvre program at its stations) and the roof reads one cast roll (bustle facet strips fused). Decks FILLED, cupola/hatch circles read, no voids under perspective. |
| close-front | 9.0 | **9.0** | HELD and improved: one-rake glacis + platforms + pinned eyes + clevis + flap + guards + light cluster; brake drum + rings + window band; M2 mass with attached cradle; the r1 note "shield flat face betrays the loft at crop rear" is now a rolled casting behind the shield recess. §B1 bow cert stands — no staircase, real furniture carries the silhouette; true-46° correctly NOT ordered (certified print-class cost +0.37 ×12 + 4 cols). |
| close-roof | 8.8 | **9.0** | JOINT r1 FLOOR — the definitive A/B: the flat-quad patchwork (front face / cheek planes / shoulder step / crown, bustle vertical banding) shades as ONE cast roll with bolt-dot rows riding the contour and a faint legible parting-line arc; deck dash arrays at the ref's grouser-grid density at the crop edge (ref's own 277 px pocket class unchanged); M2 cluster + cupola + pods individually legible; floods legal (1334 = the adjudicated wheel-gap + M2-underside classes, byte-match). |

Floor **9.0**, mean **9.0**, every view at the bar. Identity was never
in doubt at r1 and is untouched here: same M26, same stations, every
mark tell present in every view.

## §H.4 distinctness (fresh four-up, proc pixels, sibs hash-frozen)

m26 = plain-olive + double-baffle drum + bustle-rear M2 barrel-forward
+ bow fender platforms/eyes/clevis + 5 return rollers + tension idler +
duckbill tail + NO fender mufflers. m45 = stub 90 mm, front-left roof
M2, banded turret scheme. m46 = dapple camo, fat evacuator +
single-baffle drum, fender mufflers, forward M2. m47 = needle-nose +
m36 deflector, long bustle, AA pedestal cluster, red-band wheels. No
're-badge' read at a garage glance.

## Honest residuals (carried, named, none blocking — verified this round)

- **X-brace architecture residual** (r4-documented): the ref's rear
  mud-guard X-stays span an OPEN track zone on the proc's certified
  narrow-fender architecture — floating X-strips would violate §B2
  attachment. Reads at view-top corners + view-rear guards. Needs an
  orchestrator ruling on the rear-fender architecture if ever taken;
  correctly NOT delivered this round.
- **Ramp-band rear-grazing**: Bank tone rows vanish at the rear
  camera's ~4.6° grazing (ref's 3D louvres still read there);
  top/tilt/quarters carry the density. Tone-pure heights are the
  TRACE-LINE DOUBLE-COUNT law's price — 3D louvres there would re-break
  stations i1 (measured 1.81→2.40 in r4).
- **Engine-dash crest tone**: proc rows p75 62.6–67.2 vs ref 63–74 —
  in-band, a hair dim of the ref's brightest rows; Bank A +4L hot vs
  its own ref rows. m46-graduation tone-window class.
- **Fender cleat runs**: ref's L/C cleat marks along the fender tops
  absent; proc carries hanger-bump rhythm + bolt lines over the
  certified notched-rail fender trade.
- **Certified gate residuals re-measured EXACT** (my run):
  side_whole @4.34 = 0.142 (tier3 law-1 price), @−2.0 = 0.085 (clevis
  tab counterweight), @−4.34 = 0.063 (muzzle-collar); turret_side
  @−1.02 = 0.063 (basket/casting); turret_plan @0.83 = 0.119
  (rotor-lane trade); front_whole @−0.06 = +0.239 (M2 crown strip ×1
  col); stations i1 topPct 1.93 (= 1.81 certified rear-ramp window +
  0.12 tone-row quantization, per the r4 packet). Registration dAlong
  −0.002 / dy 0.
- **§B1 bow**: certified print-class near-vertical glacis carries (the
  pre-warp extract proof stands); one rake + real furniture, no
  staircase. NOT an order (owner-law oracle-matching, measured
  true-rake cost certified).
- **Smooth-loft surface observations** (no order): faint cast
  parting-line arc over the crown at close-roof (legible as a casting
  seam, not a facet) and mild vertical mach-banding on the cheek rolls
  at ≥3× (reads as cast texture at 1×). Named for the record.
- Ref M2 canted vs proc level barrel — sub-column, unchanged.

## Standing-check table (§F.4)

| check | result |
|---|---|
| gate ×2 (official) | 90.5 PASS ×2, lines EXACT (93.1/90.5/94.2/90.6/100/100); the 90.5 side of the banked flicker twin both runs; gate JSON byte-reproduces the committed landing |
| tmp-hashgeo ×2 (bracketing all renders) | m26 2f579de8 (46/72532) stable; m45 31914c3f / m46 90ebf864 / m47 53b6123a / m60a1 fbf9f4cc / m60a3 051c454c byte-exact both ends |
| track-clip --exact | band front 0 / rear 0 + shoe front 0 / rear 0, no blind spot |
| turret-parent | stranded 0 / abutting 0 / dangling 0 |
| standard-check | contig 0 ✓, decor mg1+0d ✓ (§I justification carries), clip 0/0 ✓ |
| §B2 floods | 14/14 byte-match the r1 adjudicated table; zero new sky |
| evaluator rig parity | OK — max yawProxy 0.9° @frontright, max dCentroid 0.039 m, no flips |
| §J yaw-pair @2f579de8 | fresh rest/yaw90 ×14; turret kit rotates, hull kit static, nothing sweeps/drags |
| §H.4 four-up | four distinct marks, tells hold |

## Law-bank notes (orchestrator lane)

1. **SMOOTH-RE-EMIT GATE-BYTE PROOF** (§D corollary, critic-side
   confirmation of the r4 SMOOTHLOFT-hwL law): a correctly-parameterized
   smooth re-emit is not merely "gate-line equal" — the official gate
   JSON byte-reproduces the slab-era committed rows on an hwL rig. A
   smooth flip whose gate JSON diffs in ANY row shed grammar somewhere
   (the hwL class); demand byte-reproduction, not just equal headlines.
2. **FLICKER-TWIN SINGLE-RUN READS**: both my runs read the 90.5 side
   of the banked 90.4↔90.5 whole-row twin — run-phase, not geometry
   (AA-teeter class). A future single run printing 90.4 at this hash is
   NOT a regression; re-run before pricing.
3. **CLOSE-VIEW EVALUATOR WORLD-DECODE artifact reproduced at r2**
   (r1 law-bank note #2 confirmed): close-front prints Δ−17.3° @world
   y 3.00..3.57 on a 3.105-top build — adjudicated on overlay pixels
   (no geometry exists there in either model); the class is stable
   across sittings, candidates for the tool lane.
4. Measurement note: per-row p75 luma profiles identify deck crest
   stations on BOTH pair halves at identical y rows (plan-registered
   ortho); zone-average luma dilutes rows into fields and under-reads
   crest tone by ~10L — row-profile first for any slat/louvre claim.

## Verdict

**GRADUATION PASS (dual gate met at 2f579de8 → orchestrator runs §10;
the program's 26th)** — geometry 90.5 PASS ×2 every component ≥90, and
the independent visual gate reads **9.0 on every one of the fourteen
views** at graduation severity. Both r1 orders are delivered exactly as
specified and verified on the official rigs with this critic's own
numbers; the optional polish landed; zero new sky; zero silhouette
motion; every certified residual honest at its priced station; the
carry-list (X-brace architecture, grazing ramp band, dash-tone hair,
cleat runs) is named and none of it is a tier read at 1×. The casting
now shades like a casting. Freeze 2f579de8, graduate per
GEOMETRY-GATE.md §10 in the same commit. Independent critic: m26
graduation lane r2, 2026-08-05.
