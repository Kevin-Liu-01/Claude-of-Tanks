# t72b3m shaded-parity r3 — independent critic (2026-08-02)

Gate 92.2 gatePassed (f9ceae7). Verdict: FAIL — min 5.5 (from 5).
view-top 8 (rear-180 arc FIXED in plan; fill 100%). But the systemic
off-axis item repeats a 2nd builder-read-vs-critic split: buckets/tone
changed, the critic sees FLAT DECK + crate skyline from all six
side/quarter views ("no dome curvature anywhere off-axis").

## THE RECONCILIATION (r4 work order)

The gate turret is 92.2 — the ref's side-silhouette tops ≈ the
build's. The ref reads DOME because its surface is a smooth CURVED
shell catching gradient light; the build is flat plates at the same
heights. Also: the r14 "rear standing plates impossible" proof covers
ORTHO masks only — the ref's toptilt shows a staircase collar wrapping
the full rear, which means wedges SHORTER than the rack envelope are
ortho-mask-invisible yet perspective-visible.

1. CURVED DOME SHELL: replace the flat roof plates with an actual
   lathe/spherical cap whose profile traces the SAME certified column
   tops — silhouette unchanged, curved normals → gradient → dome read.
   DONE-GATE: paste vertical luminance gradients across the turret top
   in view-left for BOTH halves (ref = smooth gradient; flat plateau =
   not done).
2. REAR COLLAR WEDGES under the rack-envelope line (ortho-neutral,
   perspective-visible): staircase collar wrapping the rear per the
   ref's toptilt.
3. FLANK BAND GEOMETRY (not tone): arched scalloped hem over the wheel
   arches (station-safe valleys), bag mound geometry, taper the band
   end (currently a hard floating rectangle over the last wheel).
4. Kill the pale track-bottom strip (86,95,63 vs ref 56,60,41) + pale
   stepped track ends; skirt line read lower.
5. NSVT gun read with the merkava measured-rod method (paste spans).
6. Chamfer the turret roof-plate 90° corners within columns (rect
   footprint read); turret/hull boundary shadow from quarters.
7. Near-white prisms/panels → scheme tones (ring-contrast law applies
   both directions); soften the bright white rear cage slots.
