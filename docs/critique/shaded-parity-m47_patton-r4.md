# m47_patton shaded-parity r4 — SECOND ADJUDICATION (2026-08-04, post-d149cc9)

Rig: fresh 14 pairs `node tools/tmp-tank-critic.mjs --id=m47_patton` →
shots/critic-m47_patton/ (zero console errors). BYTE-STABILITY BRACKET
(the concurrent-r5-builder hazard): patton.js working tree byte-identical
to HEAD (last commit d149cc9) at round start AND at round end; m47 geometry
hash **fbf23bfe** (61 meshes / 94 872 verts) via tmp-hashgeo BEFORE renders
and AFTER all evidence capture — the round scored a stationary d149cc9
build. src/ working-tree edits exist only in leopard.js + russia.js
(profile-local FITTINGS/param hunks; not in m47's import graph:
patton.js → kit.js + materials.js only — and the hash proves no effect).
Official gate re-run on my watch: **90.4 PASS ×2 bit-identical** (hull 90.4
/ whole 91.1 / turret 91.6 / stations 93.4 / dims 100 / floaters 100) —
reproduces the r4 packet line to the decimal. hull 0.4 headroom is the
razor; turret 1.6. `tank-standard-check`: clip **0/0 ✓**, contig **0 ✓**,
decor **mg1+1d ✓**. `visual-evaluator --id=m47_patton`: exit 0, **RIG
PARITY OK** (max yawProxy 1.3° @close-roof, matches the packet), camoSeed
4242, evidence at shots/visual-eval-m47_patton/. NEW OWNER LAWS (5aafdb9,
post-dated the build): **§B5 turret-parent audit** run — see standing
checks. **§B6 track-run silhouette** — checked. Graduates m60a1
**81e69e34** / m60a3 **efcde5c4** hash-verified packet-exact (do-not-gate
respected). Sibling m46_patton re-rendered read-only for §H.4
(shots/critic-m46_patton/, zero errors — fresh render required because
d149cc9's C1 lens fix is family-wide). Measurements: banked ITU-601/
mask-method scanners (tools/tmp-r7-merkava.py) on MY fresh pairs; zoom
crops diagnosis-only (tools/tmp-m47r4crit-crop.py). All r3/r4 numbers
re-derived this round.

## HEADLINE: FAIL — floor 8.5 (hero-rearright), mean 8.69, ceiling 8.9 (front); every TAKEN r3 order verified delivered ON ITS WINDOW with zero regressions; the 9.0 gap has collapsed from four drivers to ~1.5: GEAR GRAMMAR/HUE unification (new, measured) + the undelivered B-group cast rounding

front 8.9 · frontleft 8.7 · left 8.8 · rearleft 8.6 · rear 8.6 ·
rearright 8.6 · right 8.8 · frontright 8.7 · top 8.7 · hero-fl 8.6 ·
hero-rr 8.5 · toptilt 8.7 · close-front 8.7 · close-roof 8.7

Every view up 0.2-0.3 on r3, no view down. IDENTITY unchanged and never
in doubt (needle nose, blister pods, long bustle, bow ball, six dished
wheels, capsule-deflector M36). The r3 "mechanical-diagram" verdict is
dead: the loudest r3 number (sub-30 census 5470 vs 0) measures **0 vs 0**
today. What now stands between 8.5-8.9 and 9.0 is (N) a NEWLY-MEASURED
residual family — the retoned gear reads as its own brown/tan material
against the olive hull where the ref is one paint job — plus the r3
B-group items the builder honestly did not take (B1/B4/B2-rounding), and
M2 mass grammar (B5's tone landed; its silhouette is a monotone slab bar).

## r4 delivery verification (every claim re-measured on MY pairs)

- **A1 gear shade ✓ EXACT**: view-left [60..580]×[365..432] proc sub-30
  **0** (claim 0, bar ≤300, ref 0); p5 **53.8** (claim 53.8, bar ≥35, ref
  51.6); med/p75/sd **64.7/70.5/7.73** vs ref 64.0/69.6/7.93 — in-class,
  no overshoot (ordered-class law respected).
- **A2 wheel drums ✓ EXACT**: wheel band [170..380]×[386..416] p75
  **70.1** (claim 70.1, bar ≥66, ref 69.5); blotches read on all six
  drums/side in my left-gear crops; hub rings + bolts kept. Note proc
  p95 84.9 vs ref 73.2 (a small bright-rim tail — watch, no order).
- **A3 pale posts ✓ (done-gate as written)**: no pale verticals against
  track or sky in any quarter/hero — verified all 8 views. See N-driver:
  the posts are now DARK verticals against a LIT track band (grammar
  persists, contrast inverted; ref hides them in under-fender shadow).
- **A4 wrap faces ✓ (delivered implicitly by the A1 retone)**:
  close-front wrap window [55..175]×[400..520] proc med 69.8 / p75 74.9 /
  sd 10.56 vs ref 67.0 / 75.3 / 9.90 — the r3 "solid-black wrap wall"
  (which this window was ordered against) is in-class. Residual is
  texture RHYTHM (prominent chevron teeth vs the ref's subtle pitch), not
  tone — recorded under N-driver, no separate order.
- **B2 rear band — tone HALF ✓ / rounding HALF ✗**: view-rear
  [175..465]×[313..352] med **69.5** (claim 69.5, bar ≥68, ref 73.2),
  sub-45 **22** (claim 22, ref 3); ≥6 through-shadow slat lines read in
  rear AND top ✓; pale chin ✓; tail louvre tray ✓. The SAME ORDER's
  "round the rear shell corners (kill the x -1.11 vertical)" was NOT
  done: evaluator re-cites proc-only 90°/89.9° verticals @ x -1.16/-1.11
  (len 0.47/0.46) and the flat wall + inset picture-frame panel still
  defines the rear read vs the ref's rounded tarp'd shell. Carried as
  B2b.
- **B3 rack pit + top census — PARTIAL, exactly as documented ✓**: top
  [260..380]×[330..490] sub-50 **2024** (claim 2024, bar ≤1400, ref
  1160). The rack pit IS filled (tarp bed + roll + duffel + straps read
  in top/toptilt/rear at rim-safe heights); the ~600-px residual is the
  fleet camo's near-black blotch class on bare deck plates — I verified
  the blotches sit on LIT plates (albedo, not shadow; the critic rig has
  no shadow map). BANKED to the materials-owner lane as the packet
  states; not reachable from patton.js without repainting the shared
  generator. No order on the builder.
- **B5 M2 tone + truss ✓ / mass grammar residual**: view-left rod
  [215..370]×[200..240] block-luma med **76.8** (claim 76.8, bar ≥70,
  ref 78.6), ytop-med 217 vs ref 215 ✓; pyramid truss + tie beam read
  inside the pedestal gap from front/rear/close-roof — the r3 "floating
  H-frame" is dead; the 0.177 m² sky window stays open per MG PHYSICS ✓;
  collar END at the anchor ✓. RESIDUAL (new order B7): the receiver+ammo
  run reads as ONE monotone pale slab bar — evaluator: proc-only 7.2°
  len **0.91 m** top edge @ z -2.00..-1.13 y 3.20..3.30 (close-roof) —
  vs the ref's articulated, camo-dappled receiver/cradle masses; and the
  certified-band front flag moved Δ-1.7°(r3) → **Δ-6.1°** ±0.5 len 0.30
  @ (0.35, 3.25) (crown-strip edge read; certified class, rides B7).
- **B6 deflector end — NOT TAKEN, honestly banked ✓**: close-front crop
  confirms ref's blunt twin-drum cap (r0.15 span 168.7°, bore visible)
  vs proc's sleek capsule nose; rear/right arcs ref 2 proc 0. Anchor-
  fenced, priced — carry.
- **C1 blue lenses ✓ machine-zero**: blue-dominant census
  (b-max(r,g)>8): **0 px** in front AND close-front, both halves.
- **C2 dive band ✓**: bow reads unified camo in front/close-front — no
  primer stripe, band chroma blue-free, silhouette untouched (anchors).
- **C3 bow stub — NOT taken**: stub unchanged at close-front (thin .30
  muzzle, no collar). Minor; carried.
- **C4 fender-tip sliver — DEFERRED as licensed**: hero-rr tip window
  [100..300]×[330..470] mask-method air 28.6% vs ref 27.5% — the sliver
  class persists at ~1% window air; the r3 abort-price (tailStack
  anchors, zero new silhouette columns) still applies. Carried banked.
- **D1 whip ✓**: at (-0.60, 2.72, -0.88) tip ~3.50, reads in
  front/left/rear/close-roof aligned with the ref's own spike; the
  front-view proc-only 94.2°/93.0° verticals len 0.54/0.50 @ x -0.61 are
  its pale-refund 2-px shaft vs the ref's 1-px whip — lawful (D1's own
  pricing), no order. dims 100 ×2 holds the heightM budget.
- **D2 deck/tube relief ✓**: top tube rect [215..425]×[470..524] row-SD
  **3.49 vs ref 2.22** by my pstdev scanner (builder's variant read
  2.92 vs 2.98 — method delta noted; bar ≥2.2 met on both readings);
  three collar-seam rings read at close-front clear of the evac anchor;
  periscope faces read on both hood fronts.
- **D3 era variety ✓**: see §H.4 below.

## Standing checks (§B + §H.4)

- **FRONT SLOPES: PASS** — glacis rake, cheek slopes, dive line all
  inside noise; the only noise-exceeding front flag is Δ-6.1° ±0.5 at
  the M2 certified band (B7 class, not a slope violation). p95 Δtop
  0.134 m / Δbot 0.099 m.
- **CONTIGUITY / NO EMPTY AREAS: PASS** — §B2 machine scan 0 enclosed
  cells ✓; top + toptilt decks render filled. Evaluator void inventory
  (both in hero-rr, adjudicated legitimate air): 0.055 m² @ (-0.46,
  -0.54, -1.43) = under-belly class (r3 carried 0.054 same class);
  0.051 m² @ (1.52, 1.60, 1.53) = air under the real front-right fender
  overhang above the wrap (toe-undercut family — NEW coordinates, same
  class as the r3 0.041 watch item; watch, no order). r3's toptilt
  4.751 m² projection-bay and hero-rr 0.742 m² overhang-air entries did
  not re-flag this round.
- **DECORATION / MG PHYSICS: PASS** — sky-backed M2 reads pale top-lit
  (rod med 76.8, ≥2 px crowns verified at close-roof), receiver is MASS
  not stick, mounted on a visible truss; mg1+1d censused (hand-authored
  M2 station carries its standing §I packet justification; the +1d is
  the D1 whip). Bow ball + stub read at (+0.55, 1.31, 1.63) ✓.
- **TRACK CONTAINMENT: PASS** — clip 0/0 ✓; wraps visually clear of
  dive fenders and tail band at 3-4× in all 14 views.
- **§B5 TURRET-FURNITURE PARENTING (new law, first audit): PASS
  (adjudicated)** — `turret-parent-audit` prints stranded 2 / abutting 0
  / dangling 0. Both "stranded" entries are AABB-coarse FALSE FLAGS the
  law itself anticipates: "(unnamed)" = the hull loft mesh (box x ±1.29,
  y 0.31..1.73, z -3.99..+1.97 — the entire hull body, wholly below
  ringY 1.676) and "rig_hull" = the hull GROUP node itself, both merely
  overlapping a castingBox that descends to y 0.84 (the turret basket
  drags the casting AABB deep below the ring on this long-bustle rig).
  No NAMED furniture is stranded. Code audit confirms every visual
  turret tenant lives under rig_turret: m2Station meshes (patton.js:401),
  aaPedestal (:487), D1 whip (:733), B3 rackFill via 'turretCloth'
  buckets (:712-720), bustle tarps (:459, :516). Dangling 0 ✓ (front-deck
  pioneer kit + tray are hull deck gear in hullG — correct side of the
  law). ORCHESTRATOR NOTE for the audit tool: name-skip `rig_hull`/hull
  loft nodes to kill this false-flag class fleet-wide.
- **§B6 TRACK RUN SILHOUETTE (new law): PASS** — side view is the
  trapezoid \\________/: ground-run first-content columns x 120..400
  IDENTICAL to ref (top y 420, h 14-16 px both halves); front ramp rises
  to the raised idler (close-front + left), rear ramp to the raised
  sprocket (y 0.60 center) ✓ both ends, matching the real M47's raised
  idler/sprocket pattern. Residual: the proc ramp GAP reads as a hard
  black triangle vs the ref's soft shadow (order A8, tone lane).
- **VARIANT-DISTINCTIVENESS §H.4 (vs m46 fresh render + m60 graduates):
  PASS, strengthened** — the r3 structural tells all hold (turret
  planform, muzzle device, M2 station, blisters, bow); the r3 residual
  "LOADOUT variety thin" is CLOSED by D3: m47 now reads as the dressed
  variant (whip, rack tarp/roll/duffel + straps, pioneer row, stowage
  tray, louvre trays, collar rings) vs m46's bare build. No re-badge
  read vs m60a1/m60a3 (different hull/turret generation entirely; m47's
  bow ball + blisters + needle nose are unique in the family). FAMILY
  NOTE (no order): m46 still renders the r3-class BLACK gear — when m46
  takes its tone round it should adopt m47's cfg.gearTone recipe, minus
  the hue-split lesson below.

## THE NEW MEASURED DRIVER (N): gear reads as a second material family

The r4 retone lifted the gear out of the black-diagram class (A1 ✓) but
landed it in its OWN brown/tan family while the ref paints ONE olive
system over hull and gear alike. Measured, hero-rearright:
- **HUE SPLIT**: proc gear window [180..560]×[430..540] mean-RGB r/g
  ratio **1.07** vs proc's own hull window 0.97 (Δ0.10); ref gear 0.98
  vs ref hull 0.97 (Δ0.01). The proc is two-material; the ref is one.
- **FLAT READ**: same windows — proc sd 9.29 / p95 74.8 vs ref sd 13.15
  / p95 86.4: the ref gear carries rim glints and deep pockets; the proc
  is matte-flat.
- **UNPAINTED DRUMS**: sprocket + idler FACES are flat tan discs (A2
  painted only the road wheels; the ref camo-paints the whole gear) —
  the pale drum is the single loudest object in every rear quarter/hero.
- **FAR-SIDE SERRATION**: the far track's horn comb + top run reads as a
  pale serrated band floating over the deck line at hero angles —
  evaluator: proc-only edges **131.5° len 4.68 m** and 142.3° len 3.88 m
  @ (-1.9..-2.0, 1.7-2.2, -1.8) hero-rr (plus 133.8° len 1.33 m
  toptilt); the ref's fender shadow swallows that entire zone (its band
  med there 62.3 vs proc 56.5 — the delta is structure, not luma class).
- **POST GRAMMAR**: the A3-darkened roller brackets/muffler legs now
  stand as dark fence posts against the LIT track behind them
  (frontleft/rearleft/heros); the ref's live in the under-fender shadow.
- **RAMP TRIANGLES**: hard near-black wedges between last wheel and
  sprocket wrap vs the ref's graded shadow (rearleft/rearright/heros).
All zero-mask (tint/texture/AO lane). This driver holds every quarter,
hero, and the closes ~0.2-0.3 below the bar on its own.

## Per-view justifications (bar ≥9.0 "same vehicle, same tier")

- **view-front 8.9** — best view, one step out. All four r3 holders
  cleared (tracks in-class, zero blue, dive band camo'd, M2 pale+truss
  +whip). Held by: the ref's casting-face texture (scallop row + X-brace
  weld grammar) vs the proc's plain camo face; front track faces read
  dark cross-hatch vs the ref's pale horizontal plate rhythm; Δ-6.1° M2
  crown flag (certified class); proc-only skirt-drop vertical 0.46 m @
  (1.75, 1.65).
- **view-frontleft 8.7** — identity instant. N-driver at its widest
  (posts + far-track tan under the bow + hue split), B1 cliff re-cited
  (proc-only 92-93° @ z -1.6 band), nose slab face.
- **view-left 8.8** — every ordered window on this view green (A1 A2 B5
  D1). Held by the pale sprocket drum + horn-comb density + hue split;
  dive-seam Δ-11.2° priced carrier; whip lawful.
- **view-rearleft 8.6** — B2 tone + slat ceiling + rack fill land; held
  by slab bustle side + B1 cliff + sprocket drum + ramp triangle +
  posts.
- **view-rear 8.6** — band tone in-class (69.5, sub-45 22), slats/chin/
  louvre tray/M2 mast/whip all read. Held by the UNCHANGED slab
  geometry: flat wall + inset frame + 90° corner verticals (x -1.11/
  -1.16) + square pods (B4: arcs ref 2 proc 0) vs the ref's rounded
  tarp'd shell.
- **view-rearright 8.6** — mirror of rearleft + roof-line Δ+9.0° carried
  (r3 +9.6° class); '123' decal fine.
- **view-right 8.8** — as left mirrored; capsule deflector reads clean
  though not twin-drum (B6 banked); Δ+11.7° dive mirror priced.
- **view-frontright 8.7** — as frontleft mirrored (Δ+10.1° bustle-floor
  class carried).
- **view-top 8.7** — plan registration excellent (yaw 0°); rack pit
  FILLED with readable straps/tarp/duffel; D2 relief + periscopes ✓;
  pioneer/tray dress the front deck. Held by: B3 albedo blotch census
  2024 vs 1400 (banked class), notched ±1.68 rails vs continuous ±1.75
  (priced carrier, evaluator re-cites 1.47 m proc-only rail edges),
  flat-paneled dome seams, furniture fields still sparser than ref.
- **hero-frontleft 8.6** — N-driver loudest with B1 cliff + nose slab
  face + dome panels; M2/whip skyline now correct.
- **hero-rearright 8.5 — THE FLOOR** — the r3 floor view, +0.2: rack
  filled, band lit, M2 mounted, drums camo'd, no pale posts. Still
  concentrates: pale sprocket drum + dark posts + far-side serrated band
  (4.68 m edge) + hue split + ramp triangle + slab bustle + C4 sliver
  air (28.6% vs 27.5%).
- **hero-toptilt 8.7** — decks filled, plan grammar good, rack content
  reads; held by dome seams + sparse fields + far-side serration +
  albedo blotches.
- **close-front 8.7** — wrap walls FIXED in-class (med 69.8 vs 67.0),
  lenses smoked, dive camo'd, collar rings read. Held by: deflector-end
  grammar (banked B6), M2 barrel slew vs ref (Δ+21° n0.2 len 1.04 —
  the ref's M2 slews left-forward, proc's is dead-ahead; certified-band
  pose nuance, optional B9), plain casting face, thin C3 stub, simpler
  light-cluster read than the ref's twin-round-lights + guards.
- **close-roof 8.7** — M2 masses + truss + whip + pods + '123' + rivet
  rows read; pale-crown law verified at 2 px. Held by: monotone slab
  receiver bar (0.91 m top line), dome panel seam rectangles vs cast
  roll, rack-edge albedo blotches.

## Builder-claims audit (§D; r4 packet vs MY rig)

Every number in the r4 packet section reproduced EXACTLY on my fresh
pairs (A1 quadruple, A2, B2 med + sub-45, B3 census, B5 rod med + ytop,
C1 zero, gate line ×2, yawProxy 1.3, graduate hashes) — the single
discrepancy is D2 row-SD 2.92-vs-my-3.49 (scanner variant, bar met both
ways). Residual honesty verified: B3's 2024, the albedo mechanism (I
confirmed lit-plate blotches, not shadow), B6/C4 fences, stations
95.4→93.4 pricing, worst side_hull columns unchanged. ONE DISCIPLINE
NOTE: the d149cc9 commit headline "all A-D orders delivered" oversells —
A4 was absorbed by A1 (verified fine), but B1, B4, B2-rounding, C3, C4
were not taken (the packet body is honest about every one; the headline
is not the record, the packet is). No hidden regression found anywhere.

## ORDERS (r5; grouped by driver; tone/texture first. Razor: hull 90.4
(0.4), whole 91.1, turret 91.6 (1.6), stations 93.4. Gate ×2 after ANY
mask-touching edit; §C shadow-proxy law: verify mask-exclusion
per-harness before relying on it)

**GROUP N — gear unification (zero mask, the floor-mover):**
- N1. HUE-UNIFY the gear buckets into the hull olive family: hero-rr
  gear window [180..560]×[430..540] r/g **1.07 → ≤1.01** (own-hull 0.97,
  ref split ≤0.01); same check in hero-fl. Tint shift on the gearTone
  hexes — keep the A1 luma class (re-verify sub-30 = 0, p5 ≥35).
- N2. CAMO the sprocket + idler DRUM FACES (extend the A2 wheel
  treatment; the ref paints all gear): done-gate — no flat single-tone
  disc ≥15 px radius in any quarter/hero; drum-face p75 within the A2
  wheel-band class (≥66).
- N3. SWALLOW the far-side serration + posts in an under-fender shadow
  band (the ref's read): AO/shadow-proxy band under the fender line per
  §C (mask-excluded — VERIFY in this harness before landing) or darken
  horn/top-run buckets one step where they ride above the fender line.
  Done-gate: hero-rr far-side proc-only fender-line edges drop below
  2 m (evaluator; currently 4.68 + 3.88 m), posts no longer read as
  discrete verticals at 1× in frontleft/rearleft.
- N4. GRADE the ramp-gap triangles: soft shadow fill (dark backer INSIDE
  the existing silhouette) between last wheel and both wraps. Done-gate:
  no sub-25-luma wedge >40 px in rearleft/rearright/hero-rr.
- N5. (with N1) Restore a highlight tail: gear window sd toward ref
  (9.3 → ≥11) via rim-glint accents on horn crowns/wheel rims — stay
  inside the A1 class ceiling (no overshoot: p95 ≤ ref+4).

**GROUP B — the carried cast-grammar set (turret lane, 1.6 pts, priced,
gate ×2 each):**
- B1 (carried verbatim, re-cited): chamfer/roll ≤0.05 m on the bustle
  front 90° cliffs @ z -1.60..-1.64 y 2.0..2.68 both sides (evaluator:
  92.7°/90.5° len 0.51/0.52).
- B2b (the undone half of r3-B2): kill the rear-wall corner verticals
  @ x -1.11/-1.16 + the inset picture-frame read — cuts/chamfers INSIDE
  the current mask envelope only; rounded-shell read at 1×.
- B4 (carried): blister pod rear-profile rolls (ref arc r0.12 span
  109.8°; rear arcs ref 2 proc 0) — ≤2 turret rear columns, priced.
- B7 (NEW): M2 receiver mass grammar + tone variation: break the 0.91 m
  flat bar top (z -2.00..-1.13, y 3.20..3.30) into receiver hump + rear
  cap inside the certified band; dapple the pale clone (camo hint or
  two-tone modulation) so the assembly stops reading monotone — rod med
  stays ≥70; expect the front Δ-6.1° crown flag to settle ≤2°.
- B8 (NEW, tone-only): soften the dome panel-seam rectangles (seam
  contrast −50% or camo continuity across panels) — the ref rolls cast.
- B9 (OPTIONAL-IF-PRICED): slew the M2 barrel to the ref's pose
  (close-front Δ+21° len 1.04 — a rotation inside the certified band;
  prices side/front mask columns at the corridor tip — abort on any
  hull/stations wobble; dims 100 must hold ×2).

**GROUP C — small carried items (front lane, hull razor 0.4 — no
silhouette spend):**
- C3 (carried): bow .30 stub → real muzzle mass (collar + fatter stub,
  ≤0.03 proud, front-view interior).
- C5 (NEW, material only): front track FACES toward the ref's pale
  horizontal plate rhythm (front view [track columns] read dark
  cross-hatch); tone/texture on the wrap-face material — front-view
  track-face med within 5L of ref.
- C6 (NEW, polish, optional): casting-face texture hint on the needle
  nose (decal-lane scallop/weld grammar, flush, ≥15 mm off trace
  columns) — the last delta on the 8.9 front view.

**BANKED / NO ORDER:** B3 albedo census 2024 (fleet-camo generator —
materials-owner lane); B6 twin-drum deflector (anchor-fenced, priced);
C4 sliver (anchor-fenced, defer); notched fender rails (stations/width
carrier); dive-seam Δ±11-12° (anchor carriers); M2 certified-band cliff
flags (dims carrier); whip 2-px pale-refund (lawful D1); the two
adjudicated hero-rr air pockets (under-belly + fender-overhang classes,
watch); §B5 audit false-flag class (orchestrator: name-skip rig_hull/
hull-loft in turret-parent-audit).

## Verdict

FAIL — floor 8.5 (hero-rearright), mean 8.69, ceiling 8.9 (front); no
view at the 9.0 bar, and no machine gate broken anywhere (clip 0/0,
contig 0, §B5 stranded-real 0, §B6 trapezoid ✓, slopes clean, §H.4
strengthened to a clear pass). This is an honest, verified +0.2-0.3
across all 14 views on r3, with every taken order landing exactly on its
window and every skip declared. The remaining distance is dominated by
ONE zero-mask driver (Group N — hue/paint/shadow unification of the
gear) plus three priced turret-lane rounding items (B1/B2b/B4) and the
M2 grammar pass (B7/B8). Clear Group N and the quarters/heros go to
~8.8; add the B-group and m47 is at the bar. The geometry remains the
family's best (90.4 ×2, dims 100, floaters 100, byte-stable fbf23bfe
throughout this adjudication) — the last mile is paint physics and cast
truth, exactly where a 9.0 should be decided.
