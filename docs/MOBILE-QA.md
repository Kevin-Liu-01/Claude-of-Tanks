# MOBILE QA — sustained-performance round protocol + ledger

Owner directive (2026-08-07): iterate on mobile quality with autonomous
rounds until the budgets hold. First target: IN-BATTLE SUSTAINED
(look/drive/fire/fight). This file is the round contract — every loop
iteration follows it verbatim — and the ledger of what each round found,
changed, and measured.

## Why the iOS simulator is NOT the perf rig

The QA iPhone 16 simulator renders on SOFTWARE GL: absolute FPS there is
meaningless (lit+shadow probe FAILs by design, boots take ~2.5 min). What
transfers to real iPhones is MAIN-THREAD work: long tasks, GC, shader
compiles, camo canvas bakes, texture uploads. So:

- **Rig A (every round): headless Chrome, iPhone emulation** — runs the Lap
  (tools/mobilelap.mjs), produces the scorecard. This is the pass/fail rig.
- **Rig B (per landing): the QA simulator** — functional/visual mobile QA
  (touch HUD, safe areas, layout, Safari-specific breakage) via the QA
  server on :7777, panel attached so the owner can watch. Never FPS.
- **Real felt-FPS**: the owner's phone against the QA server. We report
  main-thread deltas; the phone is the final judge.

## The Lap (tools/mobilelap.mjs)

Deterministic scripted session, real-time (NEVER fastForward — long tasks
only exist in real time), one JSON scorecard per run. Stations:

| station | drive | budget (mobile tier) |
|---|---|---|
| garage_idle | 10 s idle in garage | long-task-free frames > 95% |
| tank_switch | 6 carousel switches | worst task < 250 ms |
| battle_load | real BATTLE click → controllable | < 8 s wall |
| look | 12 s aimpad touch drags | zero tasks > 100 ms / 10 s |
| drive | 15 s virtual-stick driving | zero tasks > 100 ms / 10 s |
| fire | 8 s Bradley mag dump (touch hold) | zero tasks > 100 ms / 10 s |
| fight | 20 s drive-at-enemy + fire; records tank:spotted | zero tasks > 100 ms / 10 s; reveal worst task < 50 ms |
| rematch | second battle start | < 8 s wall |

Per station: long tasks (count, worst, list), rAF gap p95/max, long-task-
free frame %, sim-time delta (battle stations — load throttling makes
wall-clock counts lie; see perf-probe hygiene), renderer.info deltas
(programs / textures / geometries / draw calls), JS heap delta.

## Round protocol (the gates are the contract)

0. **Sync**: `git fetch && git rebase origin/main` on the working branch —
   the parallel orchestrator lands perf work continuously; never measure a
   stale base. Read the last ledger row.
1. **Measure gate**: check foreign load (`ps aux | sort -rk3 | head`,
   `uptime`). If 1-min load per-core is saturated by foreign Chromes, WAIT
   (Monitor on load) rather than measure garbage. Run the Lap twice; a
   station only counts as failing if it fails BOTH runs.
2. **Research gate** (no fix without this): for the worst failing station,
   produce attribution WITH EVIDENCE before touching code — CDP CPU profile
   (Profiler, 0.2 ms sampling) sliced to the station window, long-task
   sources, program/texture/bake deltas. The gate artifact is one sentence
   with numbers ("fight worst task 340 ms: 61% resolveShellHit armor rays,
   22% fx spawn") written into the ledger row BEFORE the fix. A round that
   cannot attribute cleanly STOPS and records why — it does not guess.
3. **Fix**: smallest change that targets the attributed cost. One cause per
   round unless trivially co-located.
4. **Verify gate**: Lap ×2 again — target station improves, NO other
   station regresses past budget, `npm test` green, screenshot harness
   green on living views, afvprobe/reviveprobe/autofireprobe green when the
   touched code overlaps their contracts.
5. **Land**: commit (numbers in the message), cherry-pick → push to main
   (tight fetch→push window; expect one rejection race), fast-forward the
   live tree IF clean + no index.lock, restart the QA server (:7777, it
   never watches files), drop QA-sim Safari cache
   (`simctl terminate <udid> com.apple.mobilesafari`).
6. **Ledger**: append the round row below. Every ~3 rounds, run Rig B and
   attach station screenshots for the owner.

Round hygiene: budgets are the ratified bar (owner 2026-08-07); loosening
one requires an owner decision, never a round's own judgment. If two
consecutive rounds attribute the same cost to code the parallel
orchestrator owns (vehicle builders, world gen), file a chip instead of
fighting over the files.

## Budgets (ratified 2026-08-07)

tank_switch < 250 ms worst · battle_load/rematch < 8 s ·
look/drive/fire/fight zero >100 ms tasks per 10 s window AND frame-gap
p95 <= 20 ms (50+ fps floor — FEEL r12: stall budgets alone went green
while the game ran 40-50 fps and felt laggy; smoothness is now budgeted) ·
spot reveal < 50 ms · garage_idle > 95% clean frames

## Rig B pass 1 (2026-08-08, round 3 — QA iPhone 16 sim, portrait, :7777)

Functional: boot ✓ (tier=mobile dpr=3, lit+shadow=FAIL -> shadows-off
rescue, expected on software GL) · garage renders + carousel/tabs work ✓ ·
battle loads ✓ · touch HUD complete and thumb-reachable in portrait, above
Safari's bar (safe areas ✓) · FIRE hold works (ammo 24 -> 23, MBT cadence)
· swipe-to-aim works · audio live · battle sim runs (score/clock tick).
FINDINGS (visual/UX, need rounds): (1) enemy NAMEPLATES overlap into an
unreadable red/green smear at spawn sightlines in portrait FOV — needs
range-based declutter/fade or collision layout; (2) the COT DIAG overlay
persists after boot and covers the first garage carousel cards — should
auto-hide post-splash; (3) the battlefield picker is absent in portrait
garage (camo panel only) — confirm intended vs layout truncation.

## Ledger

Round 2 interim (2026-08-08, research gate OPEN — no fix landed): the
in-battle compile leak is NOT (a) spotting reveals or kills (revealprobe:
force-reveal all 13 enemies + wreck one = 0 births), NOT (b) camera/scope/
prop-break (action bisect: 0 births each), NOT (c) fx.resetAll disposal
(fxcycle probe: diedAtReset=0), NOT (d) missing warm coverage of the live
shell pipeline (a boot-time REAL warm shell — mid-flight + impact warm
frames, both caliber classes — changed nothing; reverted), NOT (e) caliber
class alone (m1a2 births like the Bradley). PROVEN SHAPE (fxcycle): every
shot/impact births programs with NEVER-REPEATING cacheKeys (shot1 +3,
shot2 +7, zero re-links) on fx-group MeshBasicMaterials + camo-kit Decor_*
mats — per-instance cacheKey variance, unwarmable by definition. RESOLVED (same
date): cacheKey nearest-neighbor diff — every born key is its warmed
neighbor MINUS the '|burn-r6' customProgramCacheKey suffix, i.e. unhooked
transient clones. Material.clone stack capture named the cloner verbatim:
**src/game/garageDressing.js buildBayA/buildBayB via pumpGarageDressing —
the garage workshop-dressing idle pump has NO battle gate**, so
requestIdleCallback keeps landing 50-170 ms repair-bay tank bakes (with
fresh unhooked material clones + program links) in battle frame gaps.
Combat correlation was coincidence; the same pump explains part of the
garage_idle churn. FIX: pumpGarageDressing defers while
game.phase === 'battle' (enterGarage already force-finishes via
ensureBuilt, so the workshop is never half-dressed). RESULT (clean-window
double-Lap, load 12): fire red -> GREEN both runs (100% ltf, prog+0);
look red-both -> green/106ms-marginal-one-run (no longer confirmed-
failing); fight worst halved 407 -> 152/176 ms, still red (+12/16 progs =
kill-time unhooked clones, round-3 lead); battle_load 25 -> 17 s side
benefit; garage stations unchanged (pump correctly still runs there).
steady-state shots: births 7 -> 1 (fxcycle). 4+1 selftest suites green,
6/6 pump-adjacent views green. LANDED.

| round | date | worst station (baseline) | attribution (evidence) | fix | result | landed |
|---|---|---|---|---|---|---|
| 0 (baseline) | 2026-08-08 | 6/8 FAIL — tank_switch worst 4890 ms (+142 programs), battle_load 26.5 s (+236 MB heap, worst 2827 ms), fire 435 ms, fight 276 ms, look 217 ms, garage_idle 5.6 >100ms/10s (+35 prog +55 MB while "idle"); drive + reveals clean | — (baseline only) | — | scorecard: scratchpad lap-baseline-1.json | harness landed |
| 3 | 2026-08-08 | fight residual: worst 152-178 ms, ~0.5 >100ms/10s (budget 0), prog +13/16 mid-fight; desktop-viewport profile shows compile family still top real cost ((program) 2.68 s / 20 s) | Kill-clone theory FALSIFIED (slayEnemies under clone logger = 0 clones — setDestroyed swaps to shared burnt, no clones). Pedestal prefetch properly phase-gated (not the leak). Born inventory (real path, 20 s fight): 2x anon#1301 (suspect: shadow-depth RGBA-packing variants for fx meshes absent at warm time), Decor_mesh#313 (hooked-length kit-decor variant on a battle tank), ~12 anon #250-313 incl. same-length pairs. Source system unidentified — Gate 2 NOT satisfied for a code fix; no change landed | none (protocol: no fix without clean attribution) | Rig B pass 1 DONE (see section above: 3 UX findings). Round 4 instrument: bind born programs to owning materials via renderer.properties INSIDE the fight probe (attribute() at capture time), plus castShadow census on fx meshes | docs-only |
| 4 | 2026-08-08 | fight residual (worst ~160 ms, ~0.5 >100ms/10s, prog +13/16) | Owner-binding probe (renderer.properties at capture): TWO named sources. (a) the shared fx_impactDecals MeshBasicMaterial program (#246) is born PER BATTLE at the first live decal stamp — the boot volley warmed it once, but battle entry re-links it (bound owners: fx_impactDecals nodes under challenger2/t90a/chieftain5/t72b3m/m48); (b) hooked (|burn-r6) MeshStandardMaterial variants on m48>rig_turret and challenger2>rig_hull LOD meshes — STRAGGLER GLB swaps landing past the 15 s drain deadline link on first render mid-battle (design tradeoff, round-5 candidate: compile straggler subtrees before making them visible in the swap pipeline). fx casts no shadows (census 0/23) — the #1301 depth-variant suspicion for fx was wrong | (a) only: per-battle decal warm (stamp -> object-form compile -> clearVehicleDecals) | LANDED — decal-bound born owners 0 (falsified); fight births +13/16 -> +5/+5, exactly one >100ms link per 20 s left (straggler-GLB family, round 5); look/fire/drive GREEN both runs; 5 suites + 4 views green | landed |
| 5 | 2026-08-08 | fight residual: exactly one >100 ms link event per 20 s (worst 188-223 ms), prog +5, born owners = GLB tanks' LOD>Mesh nodes (challenger2/t72b3m/m48) | Code-evidence attribution: the swap pipeline's precompileStaged uses renderer.compile(object,...) which SKIPS visible===false subtrees (three projectObject early-out) — a staged GLB's non-active LOD levels never compile; prewarmBurnStaged also skips !o.visible meshes. main.js's own warm passes solved this with a force-visible window (perf-r2d comment); the swap pipeline never got it. Straggler swaps (past the 15 s drain) commit mid-battle -> first LOD flip links the never-compiled variant (~150-220 ms) | modelLoader.js (staged pipeline) + main.js compileHiddenVariantsSteps: force-visible compile windows — the old 'traverses regardless of visibility' claim was FALSE (three projectObject skips hidden subtrees), so every tank's non-active LOD levels never compiled anywhere | LANDED — LOD-bound born owners 7 -> 3; fight worst 188/223 -> 134/170 ms (still 0.5/10s: remaining families enumerated = straggler post-commit camo repaint (challenger2), post-swap kit re-bolt (k2 decor LOD), first de-track gearThrownRibbon (amx30b2), fx pool growth — round-6 targets); look/drive/fire GREEN both runs; 5 suites + 4 views | landed |
| 6 | 2026-08-08 | fight residual micro-families (r5 enumeration); target (c) first de-track ribbon spawn — chosen because (a)/(b) live in modelLoader/camo territory (r5 already landed there; hygiene rule) and (c) has the cleanest code evidence | tankFactory buildThrownKit() is a lazy per-visual latch — the thrown-track ribbon geometry+materials build on the FIRST setBroken(true), i.e. mid-fight at the first live de-track (r5 born owner: amx30b2>gearThrownRibbon). The loading wreck-dance never toggles tracks, so the kit (and its program) cannot exist for the compile passes | main.js loading dance: one-frame setTrackState broken->ok toggle per fielded visual | LANDED — ribbon-bound born owners 0; fight worst 134/170 -> 112/175 ms at 0.5/10s; look/drive/fire GREEN third consecutive round; remaining families are BOTH orchestrator territory (modelLoader straggler post-commit repaint + camoKit post-swap re-bolt) -> hygiene rule: chip filed, loop moves to battle_load/tank_switch/garage_idle | landed |
| 7 | 2026-08-08 | battle_load 17-20 s vs 8 s budget | Stage telemetry (__BATTLE_LOAD across r4-r6 scorecards): world (cold createMapAsync) 7.1-9.0 s + camo bakes 3.6-4.6 s + wreck/compile warms 1.1-1.3 s, SEQUENTIAL and main-thread-bound; warm entries already pass (rematch 2.3 s). Easy fixes falsified by analysis: interleaving chunked CPU-bound stages saves no wall time without workers; garage-dwell world prefetch would regress garage_idle (world chunks are 100-300 ms tasks) | NONE — OWNER DECISION REQUIRED: (a) redefine the budget as warm-entry (cold first battle accepted at ~13-17 s behind the loading screen), (b) authorize worker-offload surgery for world gen/bakes (big, multi-round), or (c) authorize garage-dwell prefetch accepting garage_idle regression | Rig B quick pass: r5+r6 build boots clean on the sim (no DIAG overlay on splash) | docs-only |
| 8 | 2026-08-08 | tank_switch worst 286-366 ms (Lap); switchprof: every switch 3-7 tasks of 50-250 ms; SEPv3 selection spike 632 ms with prog+91 in ONE task | switchprof (6 timed switches under profiler): (1) selection-time program compile storm — getProgramParameter 1828 ms + (program) 821 ms across 6 switches, worst = never-before-selected specs compiling their whole subtree in one slice (SEPv3 +91 programs/632 ms); (2) camo bakes getImageData 1804 ms per-selection; (3) pedestal floor-fit raycasts 880 ms (intersectTriangle/robustFloorY). Pedestal prefetch only reaches +-1 neighbors and a 1.4 s browse cadence outruns it | ATTEMPTED + REVERTED: chunked per-mesh renderer.compile of the hidden cold-built hero made it WORSE (SEPv3 673 ms/+160 programs vs 632/+91 — per-mesh compile against a hidden root DUPLICATES program work rather than pre-linking the reveal set; switch worst regressed to 389/396). Attribution stands; next angle: warm programs at PREFETCH time (the +-1 neighbor build path) where the visual is built anyway, or investigate why compile-produced cacheKeys mismatch the reveal render | attribution landed; fix reverted per gate (target must improve) | docs-only |
| 9 | 2026-08-08 | tank_switch worst 286-396 ms (Lap); switchprof waves: the big spike MOVES between specs per run (r8 SEPv3 632, r9 SEPv2 1314 ms/+201 progs) and lands 1.3-2.0 s AFTER the click | Wave-timing probe: the budget-breaker is NOT the reveal — it is the pedestal GLB swap pipeline's compileMesh phase landing post-reveal: its slice loop has a >=4-mesh FLOOR that ignores the 12 ms time budget (modelLoader.js ~2872: while (mi<len && (batched<4 || now-tc0<12))) — four monster-material hero meshes in one slice = 600-1300 ms task. Same pipeline as the chipped fight families; SECOND consecutive orchestrator-territory attribution | NONE per hygiene rule — chipped (see task chip: compileMesh floor + upgrade-wave). tank_switch joins fight in the PARKED set | switch cadence budget unreachable until the swap pipeline slice floor is fixed in the orchestrator lane | docs-only |
| 10 | 2026-08-08 | garage_idle ltf 44-73% vs >95%, worst idle task 650-1134 ms | Idle profiler (14 s, 1 ms sampling): the monsters (609+1130 ms) coincide with a +157-program wave ~5.5 s in — the BOOT WARM pipeline's monolithic generator steps running on the post-ready idle pump: renderer.compile(scene,camera) as ONE slice, and compileHiddenVariantsSteps compiling a WHOLE TANK (destroyed+live, force-visible since r5 — heavier) per slice. Compile family dominates idle self-time (getProgramParameter 2040 ms + (program) 845 + getParameters 452 over 14 s). Early tex+6 waves = thumbnail baker (benign 100-150 ms class) | Split the monolithic steps finer in main.js: scene-wide compile chunked per top-level child; compileHiddenVariantsSteps yields between destroyed/live passes | PARTIAL LANDED — compile waves now fine-grained (max +73 spread over ~2 s vs one +157 burst); garage_idle over-100 tasks/10s 14 -> 4 (run B), ltf 65.5/75.7 vs 44-73 baseline; still red: remaining ~1100 ms monster = the fx VOLLEY generator step (muzzle+impacts+propBreaks+scars+warmRender in ONE slice) and per-family wreck-dance slices — round 11 splits the volley. (Battery ran under host throttling; fight/battle_load anomalies track sim-time compression, not code.) | landed |
| 11 | 2026-08-08 | garage_idle residual ~1.1-1.2 s worst | Volley split PROBE-PROVEN: after the r11 yields, the post-settle idle tail is [77,51,87] ms worst — the volley atom is gone. The Lap's window (starts AT ready) still catches monsters in the first ~4 s: the STAGED-VISUAL pump's per-family createTank builds (ensureStagedVisuals(game,1) = one whole visual per slice, 150-900 ms a family) — vehicle-builder territory (tankFactory createTank internals), THIRD orchestrator-lane attribution -> chipped per hygiene rule. (look flagged red this battery under visible host throttling — sim-time deltas compressed; not a clean-window confirmation.) | fx volley split landed (yields between effect families; order + resetAll unchanged) | garage_idle joins the PARKED set (chip: staged createTank chunking). ALL remaining red stations now parked -> STOP CRITERIA MET | landed |
| 12 (FEEL) | 2026-08-08 | Owner report: desktop look/aim laggy, battle-start glitches, garage clicks laggy — despite green stall budgets (identification gap: frame RATE was never budgeted; scorecards showed gapP95 20-26 ms all along) | (1) LOOK-LAG: main.js called lighting.updateFrustums() on ANY fov-changed frame — the rig lerps fov continuously (scope zoom, aim transitions, per-shot recoil kick), and updateFrustums' _updateUniforms sweeps EVERY CSM-registered material (~1 ms+/frame, profiled 758 ms/20 s). (2) GARAGE CLICKS: compileMesh >=4-mesh slice floor (chip task_e275927e, taken with owner authority). (3) Identification: Lap gained gapP95<=20ms budgets for battle stations | (1) lighting.updateFov() — fov changes refresh cascade geometry only (splits are fov-independent); (2) floor 4 -> 1 mesh, time budget governs; (3) corner perf HUD (src/ui/perfHud.js, F8, fps/p95/stall/calls/prog/heap/sim%) + Lap gapP95 budgets | FALSIFIED CLEAN: _getExtendedBreaks GONE from fight profile top self-time; switch program-storms dead (worst 632-1314 -> 196-368 ms, prog storms +91/+201 -> ~0); HUD smoke green (stats live, toggle, pointer-events none). Verify Lap ran under load 341 (sim deltas ~50% throttle) — absolute rows carry the environment caveat; 5 suites + 4 views green | landed |
| 13 | 2026-08-08 | Owner report: glitchy first seconds after countdown. Clean double-Lap: first post-countdown `look` birthed +15 programs BOTH runs (later stations +0-3); confirmed protocol failures remain parked `garage_idle` (ltf 79.5/84.0%) and owner-decision `battle_load` (14.9/13.2 s), while gameplay gapP95 held 17-18 ms | **Attribution before fix:** three first-10-s probes (`preBattleS <= 0`, HUD + 0.2 ms CDP + owner binding) reproduced exactly +5 programs at +4.55-8.95 s; `(program)` self-time 884.5/966.4/1079.5 ms + `getProgramParameter` 55.6/78.2/85.2 ms, bound to `tank_type99a` instanced burn materials, `Decor_lens`, `tank_t90a>fitting_lightCluster`, and `tank_type89>LOD`. Exact-object falsification found those variants ALREADY warm for the default framebuffer (`srgb`) but absent for gameplay's post target (`srgb-linear`): binding the compile to post created exactly +5 in 6.1 ms, then opening births were zero. Ordering evidence also confirmed async/sync swap warm ran before `dressTank` bolted Decor LODs and before final burn-hook re-arm. | modelLoader commit is now atomic commit -> dress/camo-kit/burn finalize -> force-visible warm -> reveal; both its late-swap warm and main's loading/countdown tank compile bind post's linear HDR target, then restore the prior target | **LANDED / clean verify:** dedicated first 10 s +5 -> **0**, 0 longtasks, gapP95 18.3 ms (max 25.1). Double Lap: look/drive/fire/fight all 0 >100 ms and gapP95 17-18 ms BOTH; look births +15 -> +9 (the opening five are gone); switch worst 73/72 ms. Only parked garage_idle (ltf 83.3/83.8%) + owner-decision battle_load (13.53/13.43 s) remain red. 5 suites + AFV/autofire + 19 living views green; revive's touched burn/reset assertions green, while its stale `result=defeat` precondition fails identically on untouched `acc0a48` (`null`) | landed (chip `task_fb3e5fc4`) |
| 14 | 2026-08-08 | Ordered backlog target: residual `tank_switch` cold-build work. Two initial baseline Laps were **rejected for FEEL gating**: the host was at 15% battery/discharging and every gameplay station was pinned to HUD 29.4-29.5 fps / gapP95 35 ms (no thermal warning). Once AC returned, untouched `067a5da` controls measured switch worst 103/101 ms (garage remained parked at 74.1/74.4% ltf; owner-decision battle load 16.99/18.11 s). | **Attribution before fix:** two 0.2 ms CDP switch profiles reproduced cold click-to-visible at 211-249 ms (cached neighbor 0 ms). In the repeat six-switch window, `createTank` owned 602.3 ms inclusive: decoration surface placement 358.0 ms, static-preview movement rest-contact scan 114.9 ms (`robustFloorY` 66.5 ms), chunked shared-canvas bake 154.5 ms and hero upgrade 91.5 ms; ray work included `intersectTriangle` 69.3 ms + `getVertexPosition` 78.8 ms. A pedestal visual is never attached to a moving entity, so its 114.9 ms contact solve is dead work; battle/player/AI builds still require it. | `buildPedestalVisual` marks its showroom-only build `staticPreview`; tankFactory skips contact derivation only for that explicit mode. Decoration, render geometry and gear rest matrices are unchanged; every simulated build retains the full solve. | **CLEAN VERIFY:** repeat profiles: `measureRestContact` 114.9 -> **0 ms**, createTank 602.3 -> 541.7/549.8 ms, cold max 249 -> 237/238 ms, first-switch worst 162 -> 86/87 ms. Double Lap: switch 92/94 ms; look/drive/fire/fight zero >100 ms and gapP95 17-18 ms BOTH; rematch green. Only parked garage_idle (74.5/74.0% ltf) + owner-decision battle_load (16.21/16.11 s) red. `npm test`, AFV, autofire, 10-round switch convergence, static-preview contract (decor yes/contact no; 14/14 battle visuals contact yes), and 19 living views green. Revive touched burn/reset/visual/x-ray/aim assertions green; its known stale `result=defeat` precondition remains `null` | landed `e90bc6a` |
| 15 (STOPPED) | 2026-08-08 | Ordered target: staged `createTank` chunking (chip `task_9959fa6e`). **No clean Gate-1 control was obtainable:** first untouched attempt under foreign load returned garage 52.8% ltf / gapP95 83 ms, switch 587 ms, battle load 31.34 s, and gameplay gapP95 22-27 ms. After waiting for load to fall below the 18-core count, a second untouched attempt began at load 14.78 but installed Chrome surged mid-Lap; it returned garage 57.9% ltf (worst 1272 ms), switch 2820 ms, battle load 40.56 s, and look/fire/fight gapP95 25/25/26 ms, ending at load 19.44. Both are quarantined, not product baselines. | **Research gate not satisfied:** persistent out-of-scope Chrome main/GPU/renderers each consumed roughly 80-120% CPU and intermittent foreign geometry/headless gates raised load as high as 130. These runs cannot distinguish the known staged-visual cost from host/GPU contention; Round 11's 150-900 ms whole-family evidence remains the prior attribution but is not substituted for a current clean 0.2 ms profile. | **NONE** — stopped exactly as Gate 2 requires; no product code touched and no contaminated optimization guessed. | Chip remains open. Next round repeats clean double-Lap + 0.2 ms idle profiles before touching `createTank`; owner-decision `battle_load` remains open. | docs-only |
| 16 (STOPPED) | 2026-08-08 | Retry of staged `createTank` chunking (chip `task_9959fa6e`). **Gate 1 never opened:** over a bounded six-minute wait, the same installed-Chrome tab renderer continuously consumed 440-607% CPU, Chrome main + GPU consumed another 175-215%, and 1-minute load stayed 16.1-20.4 on the 18-core host; a foreign geometry/headless gate was also present at round start. Round 15 already proved that launching at load 14.78 under this exact Chrome pattern yields a false 40.56 s battle load and 25-26 ms gameplay p95, so no knowingly invalid Lap was repeated. | **No current attribution:** the clean double-Lap prerequisite and 0.2 ms profile were withheld. A syntax-checked Round 16 idle profiler is ready in the untracked evidence area, but it was not run under contamination and therefore contributes no product claim. This is the second consecutive round stopped by the same external measurement blocker. | **NONE** — no product code touched; `task_9959fa6e` remains open. | Next round requires installed Chrome's sustained renderer workload to pause before double-Lap + profiles. Owner-decision `battle_load` remains open. | docs-only |
| 17 (BLOCKED) | 2026-08-08 | Third retry of staged `createTank` chunking (chip `task_9959fa6e`). **Gate 1 failed for the third consecutive goal turn:** before any Round 17 QA process launched, installed Chrome's same long-lived renderer consumed 565-621% CPU, Chrome main/GPU another ~190-205%, and host load was 20.8-22.0 on 18 cores. This is the identical external condition that invalidated Round 15's control and prevented Round 16 measurement. | **No admissible attribution:** a Lap/profile would knowingly measure owner Chrome contention rather than the product. With Round 15, 16, and 17 all stopped by the same unresolved external state, the loop is at a genuine impasse until that renderer workload pauses. | **NONE** — no product code touched; `task_9959fa6e` remains open. | Goal marked blocked after the required three-turn audit. Resume with a clean double-Lap + two 0.2 ms idle profiles; owner-decision `battle_load` remains open. | docs-only |
| 18 | 2026-08-08 | Resumed owner-directed desktop QA. Untouched double-Lap confirms `garage_idle` red BOTH runs at 77.0%/44.8% ltf (worst 576/1136 ms; +265 programs each); `battle_load` remains the separate owner-decision failure. Desktop cold-battle smoke is functionally green, with known nameplate overlap and two redundant `toNonIndexed()` warnings. | **Attribution before fix:** two clean 0.2 ms idle profiles reproduce whole-family staged construction: `createTank` 668.0/640.1 ms inclusive, `ensureStagedVisuals` 362.4/352.8 ms, decoration attach 209.5/206.6 ms, rest-contact 51.8/52.1 ms and canvas bakes 381.0/369.2 ms; the 0→7 staged wave births +265 programs/+76 textures and emits 302-633 ms long tasks. The pump calls sync `ensureStagedVisuals(game, 1)`, so “one item” still constructs an entire tank atomically. Three.js primary-source review found no correct drop-in for this CPU scheduling problem: official `BatchedMesh`/`mergeGeometries` target draw calls (and merging is already used), while `compileAsync` targets shader links and has a known local disposal race; `three-mesh-bvh` is compatible and merits a separate measured ray-hotspot benchmark, not an unevidenced mixed-cause install here. | **ATTEMPTED + REVERTED:** resumable staged-only `createTank` phases with synchronous takeover. Falsification rejected the first cut because idle-callback timeouts completed only 3/7 visuals inside 10 s. The corrected frame-budgeted cut completed 7/7 by 8.2 s with the full +254 programs/+73 textures (no deferral), but did not split the largest canvas/program atoms. | **FAILED target gate:** verify garage ltf 70.7%/51.6% versus untouched 77.0%/44.8% — improvement did not repeat, so all product changes were reverted. The battery was also externally slowed (same canvas extraction 369-381 ms clean -> 2.84 s candidate; gameplay p95 25-34 ms; final load 22.3), so no follow-on guess was made. `npm test` green; source is byte-identical to `6ee7489`. Next work should separately benchmark `three-mesh-bvh` for repeated decoration/contact rays, then seek a worker/off-main-thread path for builder/canvas atoms rather than more scheduler shuffling. | docs-only; chip `task_9959fa6e` remains open |
| 19 | 2026-08-08 | Owner-directed desktop/dev observability + lower-end testing. Fresh `95f7414` double-Lap repeated `garage_idle` 28.5%/24.5% ltf, `tank_switch` 1143/329 ms, `battle_load` 18.38/22.87 s, and gameplay gapP95 look 41/33, fire 33/26, fight 25/33 ms; drive alone green both. | **Attribution before instrumentation:** the old Lap had aggregates but no event/input/render-liveness timeline, so gaps could not be bound to product work versus host contention. New normal battle-open trace binds an 8.53 s synchronous `startBattle`, 81 first-live-window program births, gapP95 27.8 ms/max 809 ms, and 9 long tasks; V8 top self is `paintPatchRoughness` 2.40 s, `WebGLUniforms` 1.29 s, noise 0.95 s, `onFirstUse` 0.42 s, and `heightToNormal` 0.27 s. 2x-CPU live play worsens to gapP95 82.9 ms/max 1241 ms, 82 births/17 long tasks. SwiftShader needed 66.0 s merely to reach ready and never opened battle before the hard watchdog; an earlier yielding partial profile put `paintPatchRoughness` 29.5 s, `heightToNormal` 19.5 s, Three `onFirstUse` 13.3 s, and synchronous readback 2.0 s. | **LANDED TOOLING:** DEV-only bounded flight recorder: immediate snapshots of every bus event, input edges, typed every-frame phase/sim/render/input columns, long tasks, errors/page/WebGL lifecycle, and explicit screen/sim/render freeze+resume rows; stats/tail/export/download API. Repeatable normal/2x-CPU/SwiftShader CDP probe preserves boot + play traces, V8 top-self, first-10 s window and owns a hard watchdog. Intentional 320 ms block falsifies as 334 ms `screen:freeze` + long task. Static production import is eliminated; production bundle contains no trace strings/hook. Three.js primary-source shortlist ranks worker/OffscreenCanvas or offline glTF-Transform/KTX2 map bakes, async readbacks/initTexture, then material-family reduction; WebGPU/TSL and `three-mesh-bvh` remain isolated benchmarks, not guessed installs. | Verify Lap A/B: all gameplay stations green BOTH at 18 ms gapP95 and zero >100 ms tasks; only known garage (ltf 63.4%/80.9%) and owner-decision battle load (16.4/13.6 s) fail. Selftest proves copied payloads, bounded overwrite accounting, synthetic screen freeze, sim/render freeze+resume, and export schema; full `npm test`, private build + zero production trace strings, visible desktop battle, and 16 living views green. Stale removed-spec `tank_closeup_leo2a7` contract remains separately broken (owner removed it from `ALL_TANK_IDS`). | landed |
| 20 | 2026-08-08 | Untouched `4392991` double-Lap: `garage_idle` fails BOTH at 78.2%/35.1% ltf, worst 495/2846 ms, gapP95 25/75 ms, +269/+289 programs and +80/+87 textures. `battle_load` remains the parked owner-decision failure at 16.21/17.61 s; every gameplay station passes both-run confirmation (one-off fight A and switch B misses do not repeat). Host gate was open at load 6.74/8.71/9.03 on 18 cores, with the owner's long-lived Chrome renderer still consuming ~200% CPU noted as an environment caveat. | **Attribution before fix:** two first-10-s 0.2 ms CDP profiles plus the DEV flight recorder reproduce the same post-ready warm storm. Run A/B: `getProgramParameter` 2106.4/1365.9 ms, `getProgram` 548.4/460.4 ms, +289/+269 programs and +87/+80 textures; after all 7 staged visuals exist, the combat warm births its final +186 programs and records 835/592 ms screen freezes (23/15 long tasks totaling 4406/2558 ms; trace gapP95 84.5/37.2 ms). Parent stacks bind 100% of the native query cost to `WebGLUniforms -> onFirstUse`, not Three's debug checks. Canvas extraction is secondary (`heightToNormal` 1044.4/215.3 ms; `paintPatchRoughness` 306.0/134.7 ms) and deliberately remains the worker/OffscreenCanvas follow-up. | Official Three r185 `compileAsync()`/KHR polling was tested first and **reverted**: ANGLE's advertised `COMPLETION_STATUS_KHR` query itself blocked 11.01 s / froze 10.34 s. A fixed-delay compile also duplicated shader families (+355 programs) and was reverted. Landed driver-safe fallback: compile only visible forward variants one object at a time, consume each newly-created Three program's uniforms, and yield before creating the next; the existing real warm render still owns texture/state/depth initialization. | **PARTIAL LANDED:** exact-code double profiles preserve the original +289/+269 programs and +87/+80 textures while max freeze falls 835/592 -> **427/357 ms**, trace gapP95 84.5/37.2 -> **22.3/22.9 ms**, long-task time 4406/2558 -> **2182/1752 ms**, and freezes 6/3 -> **3/2**. Verify Laps: garage worst 349/310 ms and ltf 74.0/85.0% (still red; staged canvas/build atoms remain), versus baseline 495/2846 and 78.2/35.1%; look/drive/fire/fight all green BOTH at 17-18 ms p95 with zero >100 ms tasks. `battle_load` remains parked at 14.91/13.60 s. Final 2x-live CPU stress plays through without errors at 32.4 ms p95 (Round-19 reference 82.9 ms), and the synthetic freeze falsification passes. Full tests, private build, 19 living views and visible garage/battle inspection green; only the known removed-spec `tank_closeup_leo2a7` recipe is stale. | landed |
| 21 (NO FIX) | 2026-08-08 | Untouched `2ee0a62` double-Lap confirms `garage_idle` red BOTH at 82.8%/83.5% ltf, worst 348/350 ms, +289 programs/+87 textures. Gameplay is not confirmed failing (all green in A; B-only gapP95 misses do not repeat); `battle_load` remains parked for the owner decision at 14.11/18.12 s. Host gate opened at load 8.85 on 18 cores; normal power mode, 51% battery, and the owner's installed Chrome renderer at 43-50% CPU are recorded caveats. | **Attribution before fix:** two first-10-s 0.2 ms CDP profiles reproduce the remaining synchronous procedural-canvas atom: `getImageData` self-time 252.2/1429.9 ms inside `bakeSharedCanvasesSteps` 309.2/1465.4 ms; `heightToNormal` owns 109.8/837.5 ms and `paintPatchRoughness` 152.9/573.9 ms. The same windows complete all 7 staged visuals and the exact +289-program/+87-texture warm while recording 14/23 long tasks, 2063/4180 ms long-task time, and 427.9/542.9 ms max screen gaps. The larger repeatable residual is program uniform discovery: `getProgramParameter` costs 1784.8/2293.9 ms; an 18-frame stack profile binds 1874.7 ms to the normal presented `tick`, only 57.5 ms to the shadow pass, and 943.0 ms to the sliced initializer. | **ALL ATTEMPTS FALSIFIED + REVERTED:** (1) read-heavy CPU canvas backing regressed max gap to 655/651 ms; (2) OffscreenCanvas worker transfer made `createImageBitmap` itself a 529.4 ms main-thread call/max gap 758 ms; (3) sweeping older programs regressed max gap to 719 ms; (4) consuming each object's complete born-program set before yielding left 1950.6 ms in presented `tick` and max gap 776 ms. Three/glTF primary-source review explains the boundary: KTX2Loader's WorkerPool + glTF-Transform `textureCompress` are good static-asset/offline replacements, but cannot replace runtime pattern/roughness generation; Safari/iOS supports the chosen worker APIs from 16.4, yet this measured ANGLE transfer is blocking; `compileAsync` remains rejected by Round 20's 10.34 s completion-query stall. | **NO PRODUCT CHANGE:** source is byte-identical to `2ee0a62`; `npm test`, final private build and diff check green. No redundant living-view battery was run because no render/UI byte changed (Round 20's 19 living views remain the exact-code evidence). Next round must label every program birth with producer (`renderer.compile`, GLB commit, warm stage, or presented render) and owner before another scheduling change; do not retry these four approaches. | docs-only |
| 22 | 2026-08-08 | Untouched `5cfa7cc` double-Lap confirms `garage_idle` red BOTH at 47.4%/59.6% ltf, worst 632/559 ms, and `tank_switch` red BOTH at 335/300 ms because the post-ready warm drains into that station. `battle_load` remains parked at 20.93/21.01 s. Gameplay absolute rows are quarantined for host throttling (fight advanced only 12.4/11.2 sim seconds in 20 s wall); owner Chrome and 30→19% battery on normal power mode are recorded caveats. | **Attribution before fix:** two 0.2 ms producer/owner profiles are numerically identical: final +289 programs/+87 textures split into 172 `renderer.compile` births and **117 ordinary presented-render births**; each records 14 long tasks totaling 1860/1878 ms, worst 333/338 ms, trace gapP95 66.1/65.8 ms, and `getProgramParameter` 1195.5/1141.2 ms. Exact `renderBufferDirect` binding identifies the repeatable wave: `garageDressing.buildCore()` lazily inserts `bayFill` (a `PointLight`) after readiness, changing Three's light-count program key; the following live frame births 40 programs, including a second 20-program color family on the already-visible `tank_m1a1`. Official Three r185 guidance independently requires target-scene lighting configured before `compile()` and recommends stable zero-intensity/dummy features over runtime shader-topology changes, so no replacement dependency fits this cause. | Create the existing whisper-fill light with the initially empty dressing group so the pre-ready post render sees the final light set; the lazy core chunk keeps all geometry/canvas work and battle visibility behavior unchanged. | **PARTIAL LANDED:** exact-code A/B profiles repeat final programs 340→**288** (+289→+237), presented births 117→**65**, hero births 40→**20**, probe gapP95 66.1/65.8→**34.3/34.3 ms**, max 332/333→**268/267 ms**, and long-task time 1860/1878→**1410/1406 ms** while preserving +87 textures and all 7 staged visuals. Lower-end battery Laps repeat garage 88.6/88.3% ltf and switch 216/216 ms; one uncontested AC Lap records garage 73.2%, switch 215 ms, and look/drive/fire 17-18 ms. Later Laps are rejected: owner `tmp-abramsx-rigprobe`/geometry/critic Chromiums and a live-tree build overlapped, or load exceeded 18 cores (peak 43.8); their reds are retained as stress artifacts, not product evidence. Garage still misses 95%, baseline/AC fight remains host-compressed at 21/22/25 ms, and owner-decision `battle_load` remains parked. `npm test`, private build, 19 living views and visible garage/battle inspection green; only the known removed-spec `tank_closeup_leo2a7` recipe remains stale. | partial landed |
| 23 (STOPPED) | 2026-08-09 | Fresh `6e4e4ef` retry of the remaining non-parked failures. **Gate 1 never opened:** across the bounded 23:53-midnight wait, 1-minute load started at 320.45 and stayed between 309.97-368.01 on the 18-core host; process censuses found 143-200 runnable processes. Installed Chrome GPU/renderers consumed roughly 158%/103%/51% CPU, WindowServer 88%, the active Codex renderer 132%, and Recordly GPU 57%. AC power was present, so battery mode is not the cause. | **No admissible control or attribution:** launching the Lap under 17-20x per-core saturation would measure host scheduling, not `garage_idle`, `tank_switch`, or gameplay. The required untouched double-Lap and current 0.2 ms profile were withheld rather than substituting Round 22 evidence. | **NONE** — no product code touched and no Three.js/library change researched or guessed because the measure gate is a prerequisite. | Docs-only stop. This is the first recurrence in the fresh blocked audit after the earlier Round 17 resume; the next round retries Gate 1. `garage_idle` remains non-parked, while owner-decision `battle_load` stays parked and unanswered. | docs-only |
| 24 (STOPPED) | 2026-08-09 | Second fresh-audit retry on untouched `770c5d2`. **Gate 1 again never opened:** after the clean worktree materialized, the 18-core host started at load 355.98; two bounded sample windows stayed at 272.08-327.76 and ended at 314.35, with 104-168 runnable processes and up to 8 uninterruptible tasks. Installed Chrome GPU/renderers consumed roughly 173%/102%/54%, the active Codex renderer 124%, WindowServer 87%, and Recordly GPU 58%; AC power was present. | **No admissible control or attribution for the second consecutive turn:** even the best sample was 15x the core count. Dependency install, the untouched double-Lap, DEV trace and 0.2 ms profile were all withheld, so no product conclusion is drawn from this host state. | **NONE** — no product code touched and no Three.js/library research or speculative substitution performed before the measure gate. | Docs-only stop; this is recurrence 2/3 in the fresh blocked audit. Round 25 retries Gate 1 and must mark the persistent external condition blocked if it recurs. `garage_idle` remains non-parked; owner-decision `battle_load` remains parked and unanswered. | docs-only |
| 25 | 2026-08-09 | Fresh `5b46341` Gate-1 double control after load fell below the 18-core limit. Garage fails BOTH at 62.1%/60.9% ltf (worst 526/588 ms, gapP95 58/75 ms, +10/+30 programs); tank switch fails BOTH at 696/651 ms (gapP95 67/101 ms, +198/+199 programs). `battle_load` remains parked at 26.32/25.65 s. Control-A gameplay is quarantined because fight advanced only 11.1 sim seconds; control B is near-real-time and has look/drive/fight gapP95 27/23/34 ms. No foreign probe/gate overlapped either accepted control, though the installed Chrome/WindowServer/Recordly load caveat remains. | **Attribution before fix:** current 0.2 ms switch profile reproduces 279-980 ms worst tasks over six 1.4 s carousel clicks. `getImageData` owns **3965.9 ms self**, with `bakeSharedCanvasesSteps` 4257.7 ms inclusive; critically, the cosmetic `upgradeSharedTexturesChunked` owns **1299.6 ms** because every intermediate low-res hero schedules a high-res re-bake only 350 ms after reveal, before the next click replaces it. A separate first-10-s producer profile records 18 long tasks, gapP95 25.5 ms/max 983.4 ms, only 5/8 staged visuals complete, +26 presented-render program births and +56 textures; its top self repeats `getImageData` 1734.2 ms. Official Three source confirms `CanvasTexture` only marks a completed canvas for upload, while `KTX2Loader`'s WorkerPool transcodes prebuilt compressed assets; glTF-Transform `textureCompress` is likewise an offline/static-image encoder. None can make this runtime procedural repaint asynchronous, so no replacement dependency matches the measured waste. | Debounce the already-cosmetic hero high-resolution repaint from 350 ms to a true 3 s carousel-settle window. The immediate `ai`-tier hero reveal is unchanged; repeated browsing cancels the prior timer, and the final selected hero still crisps in place after the pause. | **PARTIAL LANDED:** focused falsification makes intermediate hero upgrades 1299.6 -> **0 ms**, `getImageData` 3965.9 -> **2680.2 ms**, and profile worst 980 -> **725 ms**. Clean verify Laps B/C make tank switch 651/696 -> **202/157 ms (GREEN BOTH)**; garage improves to 81.3/86.5% but remains below 95%, and parked battle load is 15.11/14.40 s. Look/drive/fire/fight report zero >100 ms and gapP95 17-18 ms on both runs (fight sim advances only 12.1/12.4 s after early battle completion, so its absolute duration is retained with that caveat). Verify A is quarantined: installed Chrome surged, end load 22.55, and every station regressed together. DEV flight logs are complete with 0 dropped frames/events: normal battle-open p95 17.9 ms; 2x CPU + 4-core/4-GB emulation p95 **35.7 ms**, confirming the steady-frame backlog on the lower-end profile; synthetic 320 ms freezes are detected in both. `npm test`, private build, and all **19 living views** green; the removed-spec `tank_closeup_leo2a7` recipe remains the known stale 20th view. Round 26 returns to staged procedural canvas/build atoms (`task_9959fa6e`) for the still-red garage. | landed |
| 26 | 2026-08-09 | Clean untouched controls B/C confirm `garage_idle` red BOTH at **79.9%/82.7%** ltf, worst **404/371 ms**, gapP95 **33/26 ms**, and identical +237 programs/+87 textures; `tank_switch` remains green at 168/159 ms. Control A is retained only as stress evidence because the owner's geometry gate started exactly as switching began; B's fight is likewise quarantined for foreign-harness overlap. `battle_load` remains parked at 15.41/16.11 s. | **Attribution before fix:** two clean first-10-s 0.2 ms profiles reproduce `getProgramParameter` at **1508.9/1515.1 ms self**: **841.0/819.6 ms** lands in ordinary presented `renderBufferDirect`, ahead of the already-sliced initializer's 621.3/644.5 ms. The owner/producer probe reproduces final +237 programs and classifies **65 births as ordinary draws** versus 172 explicit compiles; staged battle roots are among those draw owners (`tank_m4a3e8` 7, `tank_t90m` 4, `tank_tiger1` 1, plus staged/shared variants). Code identity closes the cause: `ensureTankVisual` adds each post-ready staged root to `scene` while `ent.state === null`, leaves Three's default `visible=true`, and the garage tick cannot hide it because `updateDustAndSync` skips stateless entities. The next garage render therefore forces its lazy program/uniform work before `warmCombatPipelineChunked` can own it. | Keep newly-created stateless visuals hidden until `setupBattle` poses and reveals them; the existing `compileHiddenVariants` force-visible window still warms their hidden variants. Official Three r185 source confirms `Object3D.visible` defaults true and the normal render walk returns immediately for an invisible parent, while `WebGLProgram.getUniforms()` initializes lazily on first use. This is a lifecycle invariant, not a missing-library problem; `compileAsync` remains locally falsified and no replacement dependency fits. | **PARTIAL LANDED:** exact-code producer evidence moves ordinary draw births **65→49**, removes the staged-root owners, and improves trace gapP95 24.5→**17.6 ms**. Double 0.2 ms profiles cut long tasks **17/15→10/9**, worst **377/387→229/233 ms**, total `getProgramParameter` **1508.9/1515.1→714.5/515.0 ms**, and presented-render discovery **841.0/819.6→332.8/131.3 ms**, with all 7 visuals and +87 textures complete. Double Laps improve garage to **91.3%/90.7%** ltf and 238/230 ms worst (still below 95%), keep switch green at 143/145 ms, and keep look/drive/fire/fight green at 17-18 ms p95 with zero >100 ms tasks; parked battle load is 13.40/13.43 s. The lossless DEV 2x-CPU/4-core/4-GB run records 696 frames + 812 events, zero drops/errors, synthetic-freeze detection green, but battle-open p95 **23.6 ms** remains over the lower-end feel bar. SwiftShader is a catastrophic floor: 42.28 s to ready, then the 120 s battle-open watchdog fires; boot alone records 38.85 s long-task time and one frame. `npm test`, private build and all 19 living views green; removed-spec `tank_closeup_leo2a7` remains the known stale 20th recipe. Garage remains the Round 27 target. | landed |
| 27 | 2026-08-09 | Clean untouched controls A/B confirm `garage_idle` red BOTH at **91.2%/90.5%** ltf, worst **235/241 ms**, gapP95 **18/18 ms**, and identical +235 programs/+87 textures; each has three >100 ms atoms, while `tank_switch` and all gameplay stations are green at 17-18 ms gapP95 with zero >100 ms gameplay tasks. `battle_load` remains parked at 13.607/13.608 s. | **Attribution before fix:** repeat 0.2 ms profiles reproduce the initial presented-frame atom at **232-254 ms**, and the owner/producer probe assigns **20 ordinary draw program births** during it to the visible procedural `tank_m1a1`; source identity closes the cause because boot awaits `setPedestalTank`, but its cold path starts `prebakeSharedTextures(...).then(buildPedestalVisual)` without returning that promise, so `bootStage('vehicle')` completes and the existing post warm-render runs before the hero exists, leaving its first post-ready draw to discover those programs. | **ATTEMPTED + REVERTED:** return the existing cold prebake/build promise so the boot vehicle stage waits and the existing post-stage warm render sees the hero. Official Three r185 source confirms `compileAsync()` only wraps `compile()`/`KHR_parallel_shader_compile`, normal draw setup still calls lazy `program.getUniforms()`, and invisible parents are skipped; this is application ordering, not a missing replacement library, and the already-falsified `compileAsync` path was not retried. | **REJECTED CLEAN:** exact producer evidence proves the mechanism—post-ready initial programs **51→70**, ordinary draw births **49→30**, and the M1A1 wave disappears—but both focused 0.2 ms runs expose five later >100 ms atoms. Clean double Laps repeat the regression almost exactly: garage **91.2/90.5→81.5/81.4%** ltf, >100 ms tasks **3/3→5/5**, and gapP95 **18/18→34/42 ms**; switch remains under its worst-task budget at 151/149 ms and all gameplay stations remain green at 18 ms gapP95 with zero >100 ms tasks. Product source is restored byte-for-byte; no npm/view rerun is warranted for the docs-only landing, and the existing lossless DEV/constrained logger remains unchanged. Round 28 must target a later garage atom rather than moving the hero work across readiness. | docs-only |
| 28 | 2026-08-09 | Clean untouched controls A/B confirm `garage_idle` red BOTH at **91.0%/90.9%** ltf, worst **235/236 ms**, gapP95 **19/19 ms**, and three >100 ms atoms; run A finishes only +215 programs/+80 textures inside the window while B reaches the usual +235/+87. `tank_switch` stays green at 154/143 ms, and look/drive/fire/fight stay green at 17-18 ms gapP95 with zero >100 ms tasks. `battle_load` remains parked at 13.604/13.511 s. | **Attribution before fix:** repeat 0.2 ms profiles place the later atoms at **203/205 ms** near 0.89 s and **111/108 ms** near 4.6 s. Two Long Animation Frame runs identify the former as the first post-ready `pumpPostReadyIdle` item, the synchronous six-atlas `fx.warmTextures()`, at **204.2/213.1 ms** script time; aligned CDP slices attribute **135.3/139.4 ms** to `warmTextures`, **122.3/122.7 ms** to `makeFlipbookTexture`, and **80.0/83.4 ms self** to its seeded noise sampler (variable `getImageData` adds 10.8/60.7 ms). A texture-only neighbor-prefetch falsification left the atom at 208 ms, exposing an initial cross-clock slicing error; it was reverted before this corrected attribution and product source is clean. | Official Three r185 source confirms `CanvasTexture` merely marks an already-painted canvas for upload. `KTX2Loader`'s WorkerPool plus glTF-Transform `textureCompress` is the appropriate long-term replacement if the fixed-seed atlases are exported offline, but it requires a new asset/parity pipeline and cannot split today's CPU generator. Smallest fix: preserve the exact RNG/pixel/bake order in generators, yield after each complete 4x4-atlas tile on the post-ready path, and let the existing synchronous battle/shot enforcement drain that same in-flight generator before reveal. | **PARTIAL LANDED:** focused LoAF runs cut the atlas-owning `pumpPostReadyIdle` task **204.2/213.1 -> 93.6/94.3 ms**; the **203/205 ms atlas atom disappears**, with only 16.2/6.1 ms of warm work sampled in the first sliced task. Double Laps keep every gameplay station green BOTH (zero >100 ms, gapP95 18 ms) and switch green, but garage remains red at 91.4/92.1% ltf, worst 245/249 ms and three >100 ms tasks because the unchanged initial hero/staged/compile atoms remain. The immediate-start DEV probe exercises synchronous takeover; under 2x CPU + 4-core/4-GB emulation its lossless log captures 685 frames + 811 events with zero drops/errors, battle-open gapP95 **23.6 ms** (still over the lower-end 20 ms feel bar), and detects the synthetic 320 ms freeze. `npm test`, private build, desktop/mobile autofire, live/composed FX, and all 19 living views green. Revive's touched burn/reset/x-ray/visual/aim assertions are green; its known stale `result=defeat` precondition remains `null`. `battle_load` remains owner-decision parked. | partial landed |
| 29 | 2026-08-09 | Fresh clean controls A/B keep `garage_idle` red BOTH at **92.4%/92.7%** ltf, worst **233/239 ms**, with 2/3 tasks >100 ms; A reaches +215 programs/+80 textures and B +235/+87. `tank_switch` is green at 148/146 ms; look/drive/fire/fight are green BOTH at 18 ms gapP95 with zero >100 ms tasks. `battle_load` remains parked at 13.604/13.605 s. | **Attribution before fix:** two 0.2 ms + LoAF profiles reproduce the final tail atom at **108/107 ms** near 5.07/5.04 s (`fin` rAF script 105.8/105.3 ms). It is the first force-visible warm frame after per-root compilation: both runs end it with exactly **+7 programs/+43 textures**. The producer/owner probe independently records a 110 ms task; all seven births happen as ordinary draws inside that frame, five owned by staged `tank_m1a2` materials and two by `tank_t90m`, after their 26 per-root compile births have already completed. The frame reveals every hidden battle root simultaneously, concentrating remaining texture uploads and draw-only variants into one render. | Official Three r185 source confirms `compile()` prepares materials/programs but normal `renderBufferDirect` performs final binding, while `initTexture()` preloads first-render decode/GPU upload. **Attempted + rejected:** one `initTexture` per frame cut the tail only to 103 ms and uploaded **10 extra textures** (+97 vs real-render +87), including permanently hidden material state. Smallest parity-safe fix: keep real rendering and the synchronous battle drain, but make the first force-visible warm render one staged root per painted frame; later all-root scope/spawn warmups remain unchanged. | **PARTIAL LANDED:** focused repeats preserve all 7 visuals and exact **+235 programs/+87 textures**, while the attributed tail falls **108/107 -> 73/74 ms** and no later warm task exceeds 100 ms. Double Laps reduce garage >100 ms atoms **2/3 -> 1/1**; garage remains red at **93.0%/92.2%** ltf because the separate initial hero atom remains (233/317 ms). Switch stays green at 149/146 ms, and look/drive/fire/fight stay green BOTH with zero >100 ms tasks and 18-19 ms gapP95. The lossless DEV 2x-CPU/4-core/4-GB run captures 684 frames + 808 events with zero drops/errors and detects the synthetic 320 ms freeze; battle-open gapP95 **25.1 ms** remains over the lower-end feel bar. `npm test`, private build, desktop/mobile autofire, and all 19 living views are green. Revive's visual/reset/x-ray/aim assertions are green; its known stale `result=defeat` precondition remains `null`. `battle_load` remains owner-decision parked at 13.608/13.613 s. | partial landed |
| 1 | 2026-08-08 | Gate-1 double-run fails: garage_idle, tank_switch, battle_load, fight. In-battle target = FIGHT (worst 212/323 ms, prog +15 both runs; fire + look passed a run each — baseline fire 435 ms was load contamination) | fightprof (real BATTLE-click entry, 20 s window): 11 programs compile MID-FIGHT — named leak TankWreckShadowProxy (world-prop wreck shadow caster, props.js) + 2× cacheKey-5009 CSM-standard monsters + 8 anonymous classes; compile family getParameters 592 + getProgram 455 + getProgramParameter 428 + (program) 2370 ms over 20 s, worst task 207 ms. Root cause: renderer.compile never builds shadow-DEPTH variant programs — each caster class links on its first shadow-pass render, i.e. when the player first drives it into a CSM cascade mid-fight. (Debug-path entry without loading warms shows the same storm at +33 programs incl. camo-r7 Decor_* kit mats — loading path covers those; the depth-variant class is the real-path residue.) | warmShadowPrograms(): one warm frame behind the loading screen with the far cascade's shadow camera stretched map-wide (existing sun light — adding a light would change light count and recompile every lit program, kill-cam warm lesson) | PARTIAL — TankWreckShadowProxy class provably eliminated from the mid-fight leak (fightprof re-run), fight prog delta run-B +3 (was +15/+15) BUT budget still red both verify runs (worst 407/102 ms; the per-run-variable REVEAL-TIME variant compiles dominate — they hit fire in verify-B too: +13 prog, 568 ms). No regression: drive clean, all 19 living screenshot views green, 5 selftest suites green. Scorecards: r1-verify-a/b.json | landed (kept: correct, evidenced, zero-regression). ROUND 2 TARGET: reveal-time variant compiles (Decor_* kit mats + cacheKey-5009 CSM monsters) — suspect a material define flip on spotting reveal / first visible render; instrument WHICH define differs by diffing cacheKeys of a warmed vs revealed material |

NOTE (r5 Gate-5): live-tree ff DEFERRED — the orchestrator session has
uncommitted modelLoader.js changes (the same file r5 touches); ff aborted
safely per protocol. r5 is on origin/main (7327340); re-run the ff + QA
server restart at the next round Gate-5 once their tree is clean. Watch
for a rebase conflict on their side; r5 diffs are additive force-visible
windows in prewarmBurnStaged/precompileStaged.
