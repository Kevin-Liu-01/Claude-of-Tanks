{
  "note": "PERFORMANCE_BUDGET r6 STATUS — certification pair PENDING the first genuinely quiet machine window; this file is rewritten automatically by tools/quietcert.mjs (r6, running — see docs/quietcert-r6.log) the moment a valid uncontended dsf1+dsf2 PASS pair lands, and a valid-stamp FAIL writes docs/cert-r6-FAILED instead of retrying. WHAT IS PROVEN on the c3fa305 merged tree (60 s pinned worst-case-roster probes; these lines are contention-independent or got WORSE under contention and still passed): drawCalls worst-frame 457-467 vs 900, trianglesMedian 6.42-6.69M vs the FROZEN 7.0M gate (6.0M ratchet NOT met — see handoff performance_budget-r6.md section 2 for why the suggested canopy shadow-proxy was measured net-negative in r5 and what the real next cut is), sceneTextureMB 389.5-389.8 vs the FROZEN 512, retained-set heap +0.06-0.07 MB/s vs 1.0 over 60 s combat, zero console errors. WHAT IS NOT PROVEN and why: fps median/p5 and frameMs p99 collapse only under foreign-GPU time-slicing — fresh r6 dump evidence: the p99-tail frames carry IDENTICAL median draw calls (388=388) and triangles (6.65M=6.65M) and are uniformly distributed across the window with mean fps 143.7 vs median 66.2 (burst/stall), i.e. no game-side hitch source; quiet windows on this content class measured p99 14.4-16.6 ms. loadToReady measured 7.7-8.4 s vs the 8000 ms gate — a REAL regression, fixed this round by docs/handoff/performance_budget-r6.patch: boot attribution counted 6.87M heightAt + 12.6M analytic noise4d per boot (1.26M grass-tuft candidates); the patch shares one 256^2 splat-field bake between the GPU uNoise texture and the CPU placement twin (correlation exact-by-construction, GPU texture bytes bit-identical), swaps three's SimplexNoise for an output-BIT-IDENTICAL fast rewrite (0 mismatches / 2.7M Object.is samples in Chromium across all game seeds), reorders the tuft slope test behind the pure-math culls (rng draws are unconditional, so the accepted set is bit-identical), and turns off the per-program shader-error readback. Controlled alternated A/B: total 7630 -> 6491/6682 ms, vegetation phase 3.27 -> 2.58 s; expected round-close ~6.3-6.7 s contended / ~5.9-6.3 s quiet. The r3-era evidence blocks formerly inlined here are preserved verbatim in docs/cert-r3-dsf1.json / docs/cert-r3-dsf2.json (byte-identical, verified before this rewrite). The FROZEN gates (7.0M triangles, 512MB textures) were NOT raised; no budget line was touched this round.",
  "sources": {
    "r3EvidenceDsf1": "cert-r3-dsf1.json",
    "r3EvidenceDsf2": "cert-r3-dsf2.json",
    "r6Runner": "quietcert-r6.log",
    "r6Handoff": "handoff/performance_budget-r6.md"
  },
  "pendingCertification": {
    "runner": "tools/quietcert.mjs (r6) — after the round verifier applies the handoffs, relaunch on the committed tree: cd <repo> && pkill -f quietcert; nohup node tools/quietcert.mjs --max-hours 24 >> docs/quietcert-r6.log 2>&1 &",
    "willWrite": ["docs/cert-r6-dsf1.json", "docs/cert-r6-dsf2.json", "docs/perf-after.json (replaced on PASS pair)", "docs/cert-r6-FAILED (on valid-stamp FAIL)"]
  }
}
